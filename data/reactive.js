const SEC_REACTIVE = {
  id:"reactive",
  title:"Reactive Programming & WebFlux",
  icon:"🌊",
  ib:"rgba(6,182,212,.12)",
  ic:"#06b6d4",
  grp:"spring",
  bar:"#0891b2",
  tglow:"rgba(8,145,178,.07)",
  desc:"Mono/Flux, backpressure, WebFlux, R2DBC, schedulers, SSE, reactive testing, and production patterns.",
  tags:["Reactive","WebFlux","Project Reactor","Mono","Flux","Backpressure","R2DBC"],
  qs:[
    {q:"What is reactive programming and why use it?",d:"easy",
     a:"<strong>Reactive programming</strong> is an asynchronous, non-blocking programming paradigm centered around data streams and change propagation.<br><br><strong>Core idea:</strong> Instead of blocking threads waiting for I/O (DB queries, HTTP calls), threads are freed immediately and a callback fires when data is ready.<br><br><strong>Traditional blocking model (Spring MVC):</strong><br><pre>// Thread blocked waiting for DB — wastes resources\nUser user = userRepository.findById(id);  // blocks 10ms\nOrder order = orderRepository.findByUser(user);  // blocks 10ms\n// Thread held for 20ms total</pre><br><strong>Reactive model (Spring WebFlux):</strong><br><pre>// Thread freed immediately, callback chain executes when data arrives\nMono&lt;User&gt; user = userRepository.findById(id);    // non-blocking\nMono&lt;Order&gt; order = user.flatMap(u -> orderRepository.findByUser(u));\n// Thread handles thousands of other requests during those 20ms</pre><br><strong>Why use it:</strong><ul><li><strong>Scalability:</strong> 10 threads can serve 10,000 concurrent connections vs 10 with blocking</li><li><strong>Backpressure:</strong> Consumer controls flow rate, preventing buffer overflow</li><li><strong>Resilience:</strong> Non-blocking naturally prevents thread pool exhaustion</li></ul><br><strong>When it shines:</strong> High-concurrency I/O-bound systems (APIs, gateways, chat, streaming)<br><strong>When it hurts:</strong> CPU-bound work, small teams, simple CRUD apps"},

    {q:"What is the difference between Mono and Flux?",d:"easy",
     a:"<strong>Mono</strong> and <strong>Flux</strong> are the two core reactive types in Project Reactor:<br><br><strong>Mono</strong> — 0 or 1 element:<br><pre>Mono&lt;User&gt; findById(Long id);      // 0 (empty) or 1 user\nMono&lt;Void&gt; deleteById(Long id);    // just completion signal\nMono&lt;String&gt; mono = Mono.just(\"Hello\");\nMono&lt;String&gt; empty = Mono.empty();\nMono&lt;String&gt; error = Mono.error(new RuntimeException());</pre><br><strong>Flux</strong> — 0 to N elements:<br><pre>Flux&lt;User&gt; findAll();              // 0 to many users\nFlux&lt;String&gt; flux = Flux.just(\"a\", \"b\", \"c\");\nFlux&lt;Integer&gt; range = Flux.range(1, 100);\nFlux&lt;Long&gt; interval = Flux.interval(Duration.ofSeconds(1)); // infinite</pre><br><strong>Both emit signals:</strong><ul><li><code>onNext(T)</code> — data item</li><li><code>onComplete()</code> — stream finished</li><li><code>onError(Throwable)</code> — error occurred</li></ul><br><strong>Converting between them:</strong><br><pre>// Flux → Mono\nFlux.just(1,2,3).collectList();      // Mono&lt;List&lt;Integer&gt;&gt;\nFlux.just(1,2,3).next();             // Mono of first element\n\n// Mono → Flux\nMono.just(\"hello\").flux();           // Flux of 1 element\nuserMono.flatMapMany(u ->            // Flux from each user\n  orderRepo.findByUser(u));</pre><br><strong>Rule of thumb:</strong> Use <code>Mono</code> when expecting at most one result, <code>Flux</code> for collections or streams."},

    {q:"What is backpressure and why does it matter?",d:"easy",
     a:"<strong>Backpressure</strong> is the mechanism that lets a consumer tell the producer 'slow down, I can't process data this fast'.<br><br><strong>The problem without backpressure:</strong><br><pre>// Producer generates 1M events/sec\n// Consumer processes 1K events/sec\n// Without backpressure → OutOfMemoryError (buffer overflow)</pre><br><strong>Reactive Streams contract:</strong><br>Project Reactor implements the Reactive Streams spec, where subscribers <em>request</em> items:<br><pre>// Subscriber requests 10 items at a time\nflux.subscribe(new BaseSubscriber&lt;Integer&gt;() {\n  @Override\n  protected void hookOnSubscribe(Subscription s) {\n    request(10);  // request first 10\n  }\n  \n  @Override\n  protected void hookOnNext(Integer value) {\n    process(value);\n    if (processedCount % 10 == 0) request(10); // request next 10\n  }\n});</pre><br><strong>Built-in backpressure strategies in Reactor:</strong><br><pre>Flux.range(1, 1_000_000)\n  .onBackpressureBuffer(1000)      // buffer up to 1000\n  .onBackpressureLatest()          // keep only latest\n  .onBackpressureDrop()            // drop if slow\n  .onBackpressureError()           // error if overwhelmed\n  .subscribe(this::process);</pre><br><strong>WebFlux handles it automatically:</strong> HTTP response writer applies backpressure to the reactive chain — data is only fetched from DB when the network can accept it."},

    {q:"When should you use reactive (WebFlux) vs traditional (MVC)?",d:"easy",
     a:"<strong>Choose based on your bottleneck, not hype.</strong><br><br><strong>Use Spring WebFlux when:</strong><ul><li>High concurrency with many simultaneous connections (&gt;1000 concurrent)</li><li>I/O-heavy workloads (multiple DB calls, external HTTP calls per request)</li><li>Streaming data (SSE, WebSocket, real-time feeds)</li><li>API gateway / BFF aggregating multiple services</li><li>Team is comfortable with reactive programming</li></ul><br><strong>Stick with Spring MVC when:</strong><ul><li>Traditional relational DB with JPA/Hibernate (blocking)</li><li>Simpler CRUD applications</li><li>Team is not familiar with reactive patterns</li><li>Debugging simplicity is priority</li><li>Using libraries that don't support reactive</li></ul><br><strong>Performance comparison:</strong><br><table><tr><th>Scenario</th><th>MVC (200 threads)</th><th>WebFlux (8 threads)</th></tr><tr><td>100 req/s</td><td>Comparable</td><td>Comparable</td></tr><tr><td>10,000 concurrent</td><td>Thread starvation</td><td>Handles fine</td></tr><tr><td>CPU-bound</td><td>Better</td><td>Worse (blocking ops)</td></tr></table><br><strong>The golden rule:</strong> If you have <em>one</em> blocking call in your reactive chain (JDBC, Thread.sleep), you've lost most of the benefit. All layers (controller, service, DB) must be reactive.<br><br><strong>Virtual threads (Java 21+):</strong> A third option — blocking code with reactive scalability. Can make WebFlux unnecessary for many use cases."},

    {q:"What are hot vs cold publishers?",d:"easy",
     a:"<strong>Cold publishers</strong> restart the data stream for each subscriber (like a Netflix video file).<br><strong>Hot publishers</strong> emit regardless of subscribers (like a live TV broadcast).<br><br><strong>Cold publisher (default in Reactor):</strong><br><pre>Flux&lt;Integer&gt; cold = Flux.range(1, 5);\n\n// Each subscriber gets its own complete sequence\ncold.subscribe(n -> System.out.print(\"A:\" + n + \" \")); // A:1 A:2 A:3 A:4 A:5\ncold.subscribe(n -> System.out.print(\"B:\" + n + \" \")); // B:1 B:2 B:3 B:4 B:5\n// Each triggers a new HTTP request / DB query!</pre><br><strong>Hot publisher (shared stream):</strong><br><pre>// ConnectableFlux — publish() makes it hot\nConnectableFlux&lt;Integer&gt; hot = Flux.range(1, 5).publish();\n\nhot.subscribe(n -> System.out.print(\"A:\" + n + \" \"));\nhot.subscribe(n -> System.out.print(\"B:\" + n + \" \"));\nhot.connect(); // starts emitting — both subscribers share the stream\n\n// Flux.share() — auto-connect when first subscriber arrives\nFlux&lt;StockPrice&gt; sharedPrices = priceStream.share();</pre><br><strong>Practical example — avoid duplicate DB calls:</strong><br><pre>// ❌ Cold — makes 2 DB queries\nMono&lt;User&gt; user = userRepo.findById(id);\nuser.subscribe(u -> sendEmail(u));\nuser.subscribe(u -> updateAudit(u));  // second DB call!\n\n// ✅ Cache — single DB call, result shared\nMono&lt;User&gt; user = userRepo.findById(id).cache();\nuser.subscribe(u -> sendEmail(u));\nuser.subscribe(u -> updateAudit(u));  // uses cached result</pre>"},

    {q:"What is the difference between map() and flatMap() in Project Reactor?",d:"medium",
     a:"<strong>map()</strong> transforms each element synchronously. <strong>flatMap()</strong> transforms each element into a Publisher and flattens the results asynchronously.<br><br><strong>map() — synchronous 1-to-1 transformation:</strong><br><pre>Flux.just(1, 2, 3)\n  .map(n -> n * 2)          // T → R (synchronous)\n  .subscribe(System.out::println);\n// Output: 2, 4, 6</pre><br><strong>flatMap() — asynchronous 1-to-Publisher transformation:</strong><br><pre>Flux.just(\"user1\", \"user2\", \"user3\")\n  .flatMap(id -> userService.getUser(id))  // T → Mono&lt;R&gt; then flatten\n  .subscribe(user -> System.out.println(user.getName()));\n// All 3 DB calls happen concurrently!</pre><br><strong>Critical difference — wrong vs right:</strong><br><pre>// ❌ Wrong — map with reactive return gives Flux&lt;Mono&lt;User&gt;&gt;\nFlux&lt;Mono&lt;User&gt;&gt; wrong = userIds.map(id -> userRepo.findById(id));\n\n// ✅ Correct — flatMap flattens\nFlux&lt;User&gt; correct = userIds.flatMap(id -> userRepo.findById(id));</pre><br><strong>Other variants:</strong><br><pre>// flatMapSequential — concurrent but preserves order\nuserIds.flatMapSequential(id -> userRepo.findById(id));\n\n// concatMap — sequential, preserves order (slower)\nuserIds.concatMap(id -> userRepo.findById(id));\n\n// switchMap — cancel previous if new element arrives\nsearchInput.switchMap(query -> searchService.search(query));</pre><br><strong>When to use which:</strong><ul><li><code>map()</code> — sync transforms (toString, toDTO)</li><li><code>flatMap()</code> — async calls, max concurrency = 256 by default</li><li><code>flatMapSequential()</code> — async calls but need ordered output</li><li><code>concatMap()</code> — sequential calls (when order AND no concurrency needed)</li></ul>"},

    {q:"How do you handle errors in reactive streams?",d:"medium",
     a:"<strong>Error handling in reactive chains</strong> must be explicit — exceptions don't propagate like in blocking code.<br><br><strong>onErrorReturn — provide fallback value:</strong><br><pre>userRepository.findById(id)\n  .onErrorReturn(User.anonymous())  // return default on any error\n  .onErrorReturn(UserNotFoundException.class, User.anonymous()); // typed</pre><br><strong>onErrorResume — fallback to another publisher:</strong><br><pre>userRepository.findById(id)\n  .onErrorResume(ex -> {\n    log.warn(\"DB error, checking cache\", ex);\n    return cacheService.getUser(id);  // fallback to cache\n  })\n  .onErrorResume(DatabaseException.class, ex ->\n    Mono.error(new ServiceUnavailableException(\"DB down\")));</pre><br><strong>onErrorMap — translate errors:</strong><br><pre>userRepository.findById(id)\n  .onErrorMap(SQLException.class,\n    ex -> new DatabaseException(\"Query failed\", ex));</pre><br><strong>doOnError — side effects (logging) without changing stream:</strong><br><pre>userRepository.findById(id)\n  .doOnError(ex -> log.error(\"Failed to find user {}: {}\", id, ex.getMessage()))\n  .onErrorResume(ex -> Mono.empty());</pre><br><strong>retry and retryWhen:</strong><br><pre>httpClient.get(url)\n  .retry(3)  // retry up to 3 times\n  .retryWhen(Retry.backoff(3, Duration.ofMillis(100))\n    .maxBackoff(Duration.ofSeconds(2))\n    .filter(ex -> ex instanceof ConnectException)); // only retry on network err</pre><br><strong>Global error handling in WebFlux:</strong><br><pre>@Component\npublic class GlobalErrorHandler implements WebExceptionHandler {\n  @Override\n  public Mono&lt;Void&gt; handle(ServerWebExchange ex, Throwable error) {\n    ServerHttpResponse response = ex.getResponse();\n    if (error instanceof UserNotFoundException) {\n      response.setStatusCode(HttpStatus.NOT_FOUND);\n    } else {\n      response.setStatusCode(HttpStatus.INTERNAL_SERVER_ERROR);\n    }\n    return response.setComplete();\n  }\n}</pre>"},

    {q:"How do you implement reactive REST endpoints with WebFlux?",d:"medium",
     a:"<strong>Spring WebFlux</strong> supports two programming models — annotated controllers (familiar) and functional routes.<br><br><strong>Annotated controller style (similar to MVC):</strong><br><pre>@RestController\n@RequestMapping(\"/api/users\")\npublic class UserController {\n  \n  @Autowired private UserRepository userRepo;\n  \n  @GetMapping(\"/{id}\")\n  public Mono&lt;ResponseEntity&lt;User&gt;&gt; getUser(@PathVariable Long id) {\n    return userRepo.findById(id)\n      .map(user -> ResponseEntity.ok(user))\n      .defaultIfEmpty(ResponseEntity.notFound().build());\n  }\n  \n  @GetMapping\n  public Flux&lt;User&gt; getAllUsers(\n      @RequestParam(defaultValue = \"0\") int page,\n      @RequestParam(defaultValue = \"20\") int size) {\n    return userRepo.findAll()\n      .skip((long) page * size)\n      .take(size);\n  }\n  \n  @PostMapping\n  @ResponseStatus(HttpStatus.CREATED)\n  public Mono&lt;User&gt; createUser(@RequestBody @Valid Mono&lt;User&gt; userMono) {\n    return userMono.flatMap(userRepo::save);\n  }\n  \n  @DeleteMapping(\"/{id}\")\n  public Mono&lt;Void&gt; deleteUser(@PathVariable Long id) {\n    return userRepo.deleteById(id);\n  }\n  \n  // Streaming endpoint\n  @GetMapping(value = \"/stream\", produces = MediaType.APPLICATION_NDJSON_VALUE)\n  public Flux&lt;User&gt; streamUsers() {\n    return userRepo.findAll()\n      .delayElements(Duration.ofMillis(100)); // for demo\n  }\n}</pre><br><strong>Functional routing style (more explicit):</strong><br><pre>@Configuration\npublic class UserRouter {\n  \n  @Bean\n  public RouterFunction&lt;ServerResponse&gt; userRoutes(UserHandler handler) {\n    return RouterFunctions.route()\n      .GET(\"/api/users/{id}\", handler::getUser)\n      .GET(\"/api/users\", handler::getAllUsers)\n      .POST(\"/api/users\", handler::createUser)\n      .build();\n  }\n}\n\n@Component\npublic class UserHandler {\n  \n  public Mono&lt;ServerResponse&gt; getUser(ServerRequest request) {\n    Long id = Long.parseLong(request.pathVariable(\"id\"));\n    return userRepo.findById(id)\n      .flatMap(user -> ServerResponse.ok().bodyValue(user))\n      .switchIfEmpty(ServerResponse.notFound().build());\n  }\n}</pre>"},

    {q:"How do you use reactive database access with R2DBC?",d:"medium",
     a:"<strong>R2DBC</strong> (Reactive Relational Database Connectivity) provides non-blocking access to relational databases.<br><br><strong>Setup (PostgreSQL):</strong><br><pre>&lt;dependency&gt;\n  &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;\n  &lt;artifactId&gt;spring-boot-starter-data-r2dbc&lt;/artifactId&gt;\n&lt;/dependency&gt;\n&lt;dependency&gt;\n  &lt;groupId&gt;io.r2dbc&lt;/groupId&gt;\n  &lt;artifactId&gt;r2dbc-postgresql&lt;/artifactId&gt;\n&lt;/dependency&gt;\n\n# application.properties\nspring.r2dbc.url=r2dbc:postgresql://localhost:5432/mydb\nspring.r2dbc.username=user\nspring.r2dbc.password=pass</pre><br><strong>Entity and repository:</strong><br><pre>@Table(\"users\")\npublic class User {\n  @Id private Long id;\n  private String name;\n  private String email;\n}\n\npublic interface UserRepository extends ReactiveCrudRepository&lt;User, Long&gt; {\n  Flux&lt;User&gt; findByEmail(String email);\n  Mono&lt;User&gt; findFirstByName(String name);\n  \n  @Query(\"SELECT * FROM users WHERE active = :active ORDER BY name\")\n  Flux&lt;User&gt; findActiveUsers(@Param(\"active\") boolean active);\n}</pre><br><strong>Service layer:</strong><br><pre>@Service\npublic class UserService {\n  \n  @Autowired private UserRepository userRepo;\n  @Autowired private OrderRepository orderRepo;\n  \n  // Combine user + orders in a single reactive chain\n  public Mono&lt;UserWithOrders&gt; getUserWithOrders(Long userId) {\n    return userRepo.findById(userId)\n      .zipWith(orderRepo.findByUserId(userId).collectList())\n      .map(tuple -> new UserWithOrders(tuple.getT1(), tuple.getT2()));\n  }\n  \n  // Reactive transaction\n  @Transactional\n  public Mono&lt;User&gt; transferCredits(Long fromId, Long toId, int amount) {\n    return userRepo.findById(fromId)\n      .flatMap(from -> {\n        from.setCredits(from.getCredits() - amount);\n        return userRepo.save(from);\n      })\n      .flatMap(from -> userRepo.findById(toId))\n      .flatMap(to -> {\n        to.setCredits(to.getCredits() + amount);\n        return userRepo.save(to);\n      });\n  }\n}</pre><br><strong>Important:</strong> R2DBC doesn't support JPA/lazy loading. Use explicit queries and joins via <code>DatabaseClient</code> for complex queries."},

    {q:"What are Schedulers and how do they work in Project Reactor?",d:"medium",
     a:"<strong>Schedulers</strong> control which threads execute parts of your reactive pipeline.<br><br><strong>Available schedulers:</strong><br><pre>// Schedulers.parallel() — fixed pool of CPU-count threads\n// Good for: CPU-bound work, default for Flux operators\nFlux.range(1, 10)\n  .publishOn(Schedulers.parallel())\n  .map(n -> heavyComputation(n))\n\n// Schedulers.boundedElastic() — elastic thread pool (capped)\n// Good for: wrapping blocking I/O calls\nMono.fromCallable(() -> blockingFileRead())\n  .subscribeOn(Schedulers.boundedElastic())\n\n// Schedulers.single() — single reusable thread\n// Good for: serialized operations\n\n// Schedulers.immediate() — current thread (no switch)\n// Schedulers.fromExecutor(myExecutor) — custom thread pool</pre><br><strong>publishOn vs subscribeOn:</strong><br><pre>// publishOn — switches execution for DOWNSTREAM operators\nFlux.just(1, 2, 3)\n  .map(n -> { System.out.println(Thread.currentThread().getName()); return n; })\n  // prints: main (upstream)\n  .publishOn(Schedulers.parallel())\n  .map(n -> { System.out.println(Thread.currentThread().getName()); return n; })\n  // prints: parallel-1 (downstream)\n  .subscribe();\n\n// subscribeOn — affects where subscription starts (entire chain)\nMono.fromCallable(() -> jdbcQuery())  // blocking!\n  .subscribeOn(Schedulers.boundedElastic())  // offload to elastic pool\n  .subscribe();\n\n// Multiple publishOn — pipeline switches threads\nFlux.range(1, 100)\n  .subscribeOn(Schedulers.parallel())\n  .filter(n -> n % 2 == 0)   // parallel thread\n  .publishOn(Schedulers.boundedElastic())\n  .flatMap(n -> callBlockingDb(n))  // elastic thread\n  .publishOn(Schedulers.parallel())\n  .map(this::toDto)  // parallel thread\n  .subscribe();</pre><br><strong>Key rule:</strong> <code>subscribeOn</code> only one matters per chain (usually lowest one wins). <code>publishOn</code> can be used multiple times to switch contexts."},

    {q:"How do you combine multiple reactive streams?",d:"medium",
     a:"<strong>Reactor provides multiple operators for combining streams:</strong><br><br><strong>zip — combine streams element-by-element:</strong><br><pre>Mono&lt;User&gt; user = userRepo.findById(1L);\nMono&lt;Account&gt; account = accountRepo.findByUserId(1L);\n\n// Both execute concurrently\nMono&lt;UserProfile&gt; profile = Mono.zip(user, account)\n  .map(t -> new UserProfile(t.getT1(), t.getT2()));\n\n// With 3+ streams\nMono.zip(user, account, settings)\n  .map(t -> new Dashboard(t.getT1(), t.getT2(), t.getT3()));</pre><br><strong>merge — interleave streams as elements arrive:</strong><br><pre>// Both streams run concurrently, results interleaved\nFlux&lt;Event&gt; merged = Flux.merge(\n  clickEventStream,\n  keyboardEventStream\n);</pre><br><strong>concat — sequential (second starts after first completes):</strong><br><pre>Flux&lt;User&gt; combined = Flux.concat(\n  userRepo.findActiveUsers(),\n  userRepo.findInactiveUsers()\n);</pre><br><strong>combineLatest — emit when any stream updates:</strong><br><pre>// Useful for UI-like state management\nFlux.combineLatest(userStream, settingsStream,\n  (user, settings) -> applySettings(user, settings));</pre><br><strong>Real-world — parallel API calls:</strong><br><pre>public Mono&lt;DashboardDTO&gt; getDashboard(Long userId) {\n  // All 3 calls execute in parallel\n  return Mono.zip(\n    userService.getUser(userId),\n    orderService.getRecentOrders(userId),\n    notificationService.getUnread(userId)\n  ).map(t -> DashboardDTO.builder()\n    .user(t.getT1())\n    .orders(t.getT2())\n    .notifications(t.getT3())\n    .build());\n}</pre><br><strong>whenComplete — run in parallel, ignore results:</strong><br><pre>Mono.whenDelayError(\n  auditService.log(event),\n  metricsService.record(event)\n).subscribe();</pre>"},

    {q:"How do you test reactive code with StepVerifier?",d:"medium",
     a:"<strong>StepVerifier</strong> is the standard tool for testing reactive streams — it subscribes to the publisher and verifies events step by step.<br><br><strong>Add dependency:</strong><br><pre>&lt;dependency&gt;\n  &lt;groupId&gt;io.projectreactor&lt;/groupId&gt;\n  &lt;artifactId&gt;reactor-test&lt;/artifactId&gt;\n  &lt;scope&gt;test&lt;/scope&gt;\n&lt;/dependency&gt;</pre><br><strong>Basic verification:</strong><br><pre>@Test\nvoid testFlux() {\n  Flux&lt;String&gt; flux = Flux.just(\"a\", \"b\", \"c\");\n  \n  StepVerifier.create(flux)\n    .expectNext(\"a\")\n    .expectNext(\"b\")\n    .expectNext(\"c\")\n    .expectComplete()\n    .verify();\n}\n\n@Test\nvoid testError() {\n  Flux&lt;String&gt; flux = Flux.error(new RuntimeException(\"oops\"));\n  \n  StepVerifier.create(flux)\n    .expectErrorMessage(\"oops\")\n    .verify();\n}</pre><br><strong>Testing with virtual time (avoid real delays):</strong><br><pre>@Test\nvoid testRetryWithBackoff() {\n  Flux&lt;Integer&gt; flux = Flux.just(1)\n    .delayElements(Duration.ofSeconds(5));\n  \n  StepVerifier.withVirtualTime(() -> flux)\n    .expectSubscription()\n    .thenAwait(Duration.ofSeconds(5))  // advance virtual time\n    .expectNext(1)\n    .verifyComplete();\n}\n\n@Test\nvoid testRetry() {\n  AtomicInteger count = new AtomicInteger();\n  Mono&lt;String&gt; mono = Mono.fromCallable(() -> {\n    if (count.getAndIncrement() &lt; 2) throw new RuntimeException();\n    return \"success\";\n  }).retry(3);\n  \n  StepVerifier.create(mono)\n    .expectNext(\"success\")\n    .verifyComplete();\n}</pre><br><strong>Testing WebFlux controllers:</strong><br><pre>@WebFluxTest(UserController.class)\nclass UserControllerTest {\n  \n  @Autowired private WebTestClient webClient;\n  @MockBean private UserRepository userRepo;\n  \n  @Test\n  void getUser_found() {\n    when(userRepo.findById(1L)).thenReturn(Mono.just(new User(1L, \"Alice\")));\n    \n    webClient.get().uri(\"/api/users/1\")\n      .exchange()\n      .expectStatus().isOk()\n      .expectBody(User.class)\n      .value(user -> assertThat(user.getName()).isEqualTo(\"Alice\"));\n  }\n}</pre>"},

    {q:"How do you handle blocking calls in reactive code?",d:"medium",
     a:"<strong>Blocking calls in reactive chains</strong> are the #1 reactive anti-pattern — they stall the small event-loop thread pool.<br><br><strong>The problem:</strong><br><pre>// ❌ NEVER do this — blocks event loop thread for entire JDBC call duration\n@GetMapping(\"/users/{id}\")\npublic Mono&lt;User&gt; getUser(@PathVariable Long id) {\n  User user = jdbcTemplate.queryForObject(  // BLOCKS event loop!\n    \"SELECT * FROM users WHERE id = ?\", User.class, id\n  );\n  return Mono.just(user);\n}</pre><br><strong>Solution 1 — subscribeOn with boundedElastic:</strong><br><pre>// ✅ Offload blocking call to elastic thread pool\n@GetMapping(\"/users/{id}\")\npublic Mono&lt;User&gt; getUser(@PathVariable Long id) {\n  return Mono.fromCallable(() ->\n    jdbcTemplate.queryForObject(\n      \"SELECT * FROM users WHERE id = ?\", User.class, id\n    )\n  ).subscribeOn(Schedulers.boundedElastic());\n}</pre><br><strong>Solution 2 — Use R2DBC instead of JDBC:</strong><br><pre>// ✅ Best solution — truly non-blocking\npublic interface UserRepository extends ReactiveCrudRepository&lt;User, Long&gt; {}\n\nreturn userRepository.findById(id);</pre><br><strong>Solution 3 — Virtual threads (Java 21+):</strong><br><pre># application.properties\nspring.threads.virtual.enabled=true\n# Now blocking is OK — virtual threads handle it cheaply</pre><br><strong>Detecting blocking calls (BlockHound):</strong><br><pre>&lt;dependency&gt;\n  &lt;groupId&gt;io.projectreactor.tools&lt;/groupId&gt;\n  &lt;artifactId&gt;blockhound&lt;/artifactId&gt;\n  &lt;scope&gt;test&lt;/scope&gt;\n&lt;/dependency&gt;\n\n// In tests\nBlockHound.install();\n// Throws BlockingOperationError when blocking detected on non-blocking thread</pre><br><strong>Rule:</strong> Isolate ALL blocking operations to <code>Schedulers.boundedElastic()</code>. Never call JDBC, Thread.sleep(), or synchronized blocks on the parallel scheduler."},

    {q:"How do you implement Server-Sent Events (SSE) with WebFlux?",d:"medium",
     a:"<strong>Server-Sent Events (SSE)</strong> stream real-time updates from server to client over HTTP (one-way).<br><br><strong>WebFlux SSE endpoint:</strong><br><pre>@RestController\n@RequestMapping(\"/api\")\npublic class NotificationController {\n  \n  private final Sinks.Many&lt;Notification&gt; sink =\n    Sinks.many().multicast().onBackpressureBuffer();\n  \n  // SSE stream endpoint\n  @GetMapping(value = \"/notifications/stream\",\n               produces = MediaType.TEXT_EVENT_STREAM_VALUE)\n  public Flux&lt;ServerSentEvent&lt;Notification&gt;&gt; streamNotifications(\n      @RequestHeader(value = \"Last-Event-ID\", required = false) String lastId) {\n    \n    return sink.asFlux()\n      .map(notification -> ServerSentEvent.&lt;Notification&gt;builder()\n        .id(notification.getId())\n        .event(\"notification\")\n        .data(notification)\n        .comment(\"keep-alive\")\n        .build())\n      .doOnSubscribe(s -> log.info(\"Client connected\"))\n      .doOnCancel(() -> log.info(\"Client disconnected\"));\n  }\n  \n  // Endpoint to push notifications\n  @PostMapping(\"/notifications\")\n  public Mono&lt;Void&gt; pushNotification(\n      @RequestBody Notification notification) {\n    sink.tryEmitNext(notification);\n    return Mono.empty();\n  }\n}\n\n// Scheduled heartbeat to prevent connection timeout\n@Scheduled(fixedDelay = 30_000)\nvoid sendHeartbeat() {\n  sink.tryEmitNext(Notification.heartbeat());\n}</pre><br><strong>Client (JavaScript):</strong><br><pre>const evtSource = new EventSource('/api/notifications/stream');\nevtSource.addEventListener('notification', (e) => {\n  const data = JSON.parse(e.data);\n  displayNotification(data);\n});\nevtSource.onerror = () => evtSource.close();</pre><br><strong>SSE vs WebSocket:</strong><ul><li><strong>SSE:</strong> Server→Client only, HTTP/1.1 compatible, auto-reconnect, simpler</li><li><strong>WebSocket:</strong> Bidirectional, lower overhead per message, required for chat</li></ul>"},

    {q:"What is the threading model in Project Reactor?",d:"medium",
     a:"<strong>Project Reactor uses an event-loop threading model</strong> — a small, fixed pool of threads handles all work.<br><br><strong>Default thread pools in WebFlux:</strong><br><pre>// Netty (default WebFlux server) uses:\n// - N event-loop threads (N = CPU cores)\n// - Each thread runs a non-blocking event loop\n// - Thread pool is NOT elastic — blocking kills it!\n\nSystem.out.println(Runtime.getRuntime().availableProcessors());\n// On 8-core machine: 8 Netty I/O threads serve all traffic</pre><br><strong>How it works for an HTTP request:</strong><br><pre>1. Request arrives → assigned to event-loop thread (e.g., reactor-http-nio-3)\n2. Controller returns Mono/Flux — no blocking, returns immediately\n3. Thread goes to handle another request\n4. When DB responds (async callback) → subscription chain resumes\n5. Response written, thread freed again</pre><br><strong>Thread switching with Schedulers:</strong><br><pre>@GetMapping(\"/users/{id}\")\npublic Mono&lt;User&gt; getUser(@PathVariable Long id) {\n  return Mono.fromCallable(() -> jdbcRepo.findById(id))  // blocking!\n    .subscribeOn(Schedulers.boundedElastic())  // offload to elastic pool\n    .publishOn(Schedulers.parallel())  // back to event loop for response\n    .map(this::toDto);\n  // Sequence: nio-3 → elastic-5 (blocking) → parallel-2 → response\n}</pre><br><strong>Schedulers and their thread pools:</strong><br><table><tr><th>Scheduler</th><th>Pool size</th><th>Use for</th></tr><tr><td>parallel()</td><td>CPU cores</td><td>CPU-bound, default</td></tr><tr><td>boundedElastic()</td><td>10x CPU, max 200k</td><td>Blocking I/O</td></tr><tr><td>single()</td><td>1 thread</td><td>Serialized ops</td></tr><tr><td>immediate()</td><td>Current thread</td><td>Inline, no switch</td></tr></table><br><strong>Key insight:</strong> All reactive operators run on whichever thread emits the item, unless <code>publishOn()</code> or <code>subscribeOn()</code> says otherwise. There's no magic — you must reason about threads explicitly."},

    {q:"How do you propagate context in reactive chains?",d:"medium",
     a:"<strong>Reactor Context</strong> is the reactive equivalent of ThreadLocal — it carries request-scoped data (trace IDs, user info) across async boundaries where ThreadLocal breaks.<br><br><strong>Why ThreadLocal fails in reactive:</strong><br><pre>// ❌ ThreadLocal is lost when Reactor switches threads\nMDC.put(\"requestId\", \"abc-123\");\nMono.fromCallable(() -> dbQuery())\n  .subscribeOn(Schedulers.boundedElastic())\n  .map(r -> {\n    MDC.get(\"requestId\"); // null! — different thread\n    return r;\n  })</pre><br><strong>Reactor Context solution:</strong><br><pre>// Write to Context\nMono.just(\"data\")\n  .flatMap(data -> \n    Mono.deferContextual(ctx -> {\n      String requestId = ctx.get(\"requestId\");\n      log.info(\"Processing with requestId: {}\", requestId);\n      return processData(data);\n    })\n  )\n  .contextWrite(Context.of(\"requestId\", \"abc-123\"));  // context flows upstream\n\n// Read context anywhere in chain\nMono.deferContextual(ctx -> \n  Mono.just(ctx.getOrDefault(\"userId\", \"anonymous\"))\n);</pre><br><strong>Automatic context propagation with Spring Security + WebFlux:</strong><br><pre>// Spring Security automatically stores Authentication in Reactor Context\n@GetMapping(\"/secure\")\npublic Mono&lt;String&gt; secure() {\n  return ReactiveSecurityContextHolder.getContext()\n    .map(sc -> sc.getAuthentication().getName())\n    .flatMap(username -> processForUser(username));\n}</pre><br><strong>Micrometer tracing (automatic in Spring Boot 3):</strong><br><pre># Spring Boot 3 auto-propagates trace/span IDs through Reactor Context\n# Visible in Zipkin, Grafana Tempo\nmanagement.tracing.sampling.probability=1.0\n\n// Manually access trace\nMono.deferContextual(ctx -> {\n  TraceContext traceCtx = ctx.get(TraceContext.class);\n  return doWork();\n})</pre><br><strong>Rule:</strong> <code>contextWrite()</code> is at the bottom of the chain (subscriber side), <code>deferContextual()</code> reads it anywhere above. Context is immutable and flows upstream."},

    {q:"How do you implement reactive security with Spring Security?",d:"hard",
     a:"<strong>Spring Security WebFlux</strong> uses Reactor Context instead of SecurityContextHolder (which uses ThreadLocal).<br><br><strong>Dependencies:</strong><br><pre>&lt;dependency&gt;\n  &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;\n  &lt;artifactId&gt;spring-boot-starter-security&lt;/artifactId&gt;\n&lt;/dependency&gt;</pre><br><strong>Security configuration (WebFlux style):</strong><br><pre>@Configuration\n@EnableWebFluxSecurity\npublic class SecurityConfig {\n  \n  @Bean\n  public SecurityWebFilterChain securityFilterChain(\n      ServerHttpSecurity http) {\n    \n    return http\n      .csrf(csrf -> csrf.disable())\n      .authorizeExchange(exchanges -> exchanges\n        .pathMatchers(\"/public/**\").permitAll()\n        .pathMatchers(\"/admin/**\").hasRole(\"ADMIN\")\n        .anyExchange().authenticated()\n      )\n      .oauth2ResourceServer(oauth2 -> oauth2\n        .jwt(Customizer.withDefaults())  // JWT validation\n      )\n      .build();\n  }\n  \n  @Bean\n  public ReactiveUserDetailsService userDetailsService(\n      UserRepository userRepo) {\n    return username -> userRepo.findByUsername(username)\n      .map(user -> User.withUsername(user.getUsername())\n        .password(user.getPasswordHash())\n        .roles(user.getRoles().toArray(String[]::new))\n        .build());\n  }\n}\n\n// Custom JWT converter\n@Bean\npublic ReactiveJwtAuthenticationConverter jwtConverter() {\n  JwtGrantedAuthoritiesConverter authoritiesConverter =\n    new JwtGrantedAuthoritiesConverter();\n  authoritiesConverter.setAuthorityPrefix(\"ROLE_\");\n  authoritiesConverter.setAuthoritiesClaimName(\"roles\");\n  \n  ReactiveJwtAuthenticationConverter converter =\n    new ReactiveJwtAuthenticationConverter();\n  converter.setJwtGrantedAuthoritiesConverter(\n    Mono.just(authoritiesConverter)::apply);\n  return converter;\n}</pre><br><strong>Accessing security context in reactive chain:</strong><br><pre>@GetMapping(\"/profile\")\npublic Mono&lt;UserProfile&gt; getProfile() {\n  return ReactiveSecurityContextHolder.getContext()\n    .map(SecurityContext::getAuthentication)\n    .flatMap(auth -> {\n      String userId = ((JwtAuthenticationToken)auth)\n        .getToken().getSubject();\n      return userService.getProfile(userId);\n    });\n}\n\n// Method security\n@GetMapping(\"/admin/users\")\n@PreAuthorize(\"hasRole('ADMIN')\")\npublic Flux&lt;User&gt; getAllUsers() {\n  return userRepository.findAll();\n}</pre><br><strong>Reactive method security setup:</strong><br><pre>@Configuration\n@EnableReactiveMethodSecurity\npublic class MethodSecurityConfig {}\n\n// Usage in service\n@Service\npublic class UserService {\n  @PreAuthorize(\"#userId == authentication.name or hasRole('ADMIN')\")\n  public Mono&lt;User&gt; getUser(String userId) {\n    return userRepository.findById(userId);\n  }\n}</pre>"},

    {q:"Design a real-time notification system using WebFlux and SSE",d:"hard",
     a:"<strong>Architecture:</strong><br><pre>Browser ←──SSE──── WebFlux Server ←─── Redis Pub/Sub ←─── Microservices\n                         │\n                    Notification DB (Mongo/Postgres)</pre><br><strong>Core components:</strong><br><br><strong>1. Notification Sink Manager (per-user streams):</strong><br><pre>@Service\npublic class NotificationSinkManager {\n  \n  // Per-user multicast sinks\n  private final Map&lt;String, Sinks.Many&lt;Notification&gt;&gt; userSinks\n    = new ConcurrentHashMap&lt;&gt;();\n  \n  public Flux&lt;Notification&gt; getStream(String userId) {\n    Sinks.Many&lt;Notification&gt; sink = userSinks.computeIfAbsent(userId,\n      k -> Sinks.many().multicast().onBackpressureBuffer(1000));\n    \n    return sink.asFlux()\n      .doFinally(signal -> {\n        if (userSinks.get(userId).currentSubscriberCount() == 0)\n          userSinks.remove(userId);  // cleanup when no subscribers\n      });\n  }\n  \n  public void emit(String userId, Notification notification) {\n    Sinks.Many&lt;Notification&gt; sink = userSinks.get(userId);\n    if (sink != null) sink.tryEmitNext(notification);\n  }\n}</pre><br><strong>2. SSE Controller:</strong><br><pre>@RestController\npublic class NotificationController {\n  \n  @Autowired private NotificationSinkManager sinkManager;\n  @Autowired private NotificationRepository notifRepo;\n  \n  @GetMapping(value = \"/notifications/stream\",\n               produces = MediaType.TEXT_EVENT_STREAM_VALUE)\n  public Flux&lt;ServerSentEvent&lt;Notification&gt;&gt; stream(\n      Authentication auth,\n      @RequestHeader(value = \"Last-Event-ID\", required = false) String lastId) {\n    \n    String userId = auth.getName();\n    \n    // Replay missed notifications from DB, then live stream\n    Flux&lt;Notification&gt; missed = lastId != null\n      ? notifRepo.findAfter(userId, lastId)\n      : Flux.empty();\n    \n    return Flux.concat(missed, sinkManager.getStream(userId))\n      .map(n -> ServerSentEvent.&lt;Notification&gt;builder()\n        .id(n.getId())\n        .event(n.getType())\n        .data(n)\n        .build());\n  }\n}</pre><br><strong>3. Redis Pub/Sub for multi-instance delivery:</strong><br><pre>@Service\npublic class RedisNotificationRelay {\n  \n  @Autowired private ReactiveRedisMessageListenerContainer listenerContainer;\n  @Autowired private NotificationSinkManager sinkManager;\n  \n  @PostConstruct\n  public void subscribe() {\n    listenerContainer.receive(\n      PatternTopic.of(\"notifications:*\"),\n      new Jackson2JsonRedisSerializer&lt;&gt;(Notification.class)\n    ).subscribe(msg -> {\n      String userId = msg.getChannel().replace(\"notifications:\", \"\");\n      sinkManager.emit(userId, msg.getMessage());\n    });\n  }\n}\n\n// Producer service\npublic Mono&lt;Void&gt; notify(String userId, Notification notification) {\n  return notifRepo.save(notification)\n    .then(redis.convertAndSend(\"notifications:\" + userId, notification))\n    .then();\n}</pre><br><strong>Scaling considerations:</strong><ul><li>Sticky sessions OR Redis Pub/Sub for multi-instance</li><li>Connection limit per server (~50K SSE with reactive)</li><li>Heartbeat every 30s to detect dead connections</li><li>Last-Event-ID for reconnect recovery</li></ul>"},

    {q:"How do you migrate a blocking Spring MVC app to reactive WebFlux?",d:"hard",
     a:"<strong>Migration strategy</strong> — incremental, layer by layer:<br><br><strong>Phase 1: Assess feasibility (check blockers):</strong><ul><li>Is the database supported by R2DBC? (MySQL, PostgreSQL, H2 — yes; Oracle — limited)</li><li>Are all libraries reactive-friendly? (Spring Security, Micrometer — yes; some legacy libs — no)</li><li>Team reactive experience?</li></ul><br><strong>Phase 2: Add WebFlux alongside MVC:</strong><br><pre>&lt;!-- Add WebFlux alongside Spring MVC temporarily --&gt;\n&lt;dependency&gt;\n  &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;\n  &lt;artifactId&gt;spring-boot-starter-webflux&lt;/artifactId&gt;\n&lt;/dependency&gt;</pre><br><strong>Phase 3: Convert service layer first (easiest):</strong><br><pre>// Before\npublic User getUser(Long id) {\n  return userRepository.findById(id).orElseThrow();\n}\n\n// After\npublic Mono&lt;User&gt; getUser(Long id) {\n  return reactiveUserRepository.findById(id)\n    .switchIfEmpty(Mono.error(new UserNotFoundException(id)));\n}</pre><br><strong>Phase 4: Replace JDBC with R2DBC:</strong><br><pre>// Before: JPA Repository\npublic interface UserRepository extends JpaRepository&lt;User, Long&gt; {}\n\n// After: R2DBC Repository\npublic interface UserRepository extends ReactiveCrudRepository&lt;User, Long&gt; {}\n\n// Note: JPA @ManyToOne won't work — must use explicit joins</pre><br><strong>Phase 5: Convert controllers:</strong><br><pre>// Before\n@GetMapping(\"/users/{id}\")\npublic ResponseEntity&lt;User&gt; getUser(@PathVariable Long id) {\n  return ResponseEntity.ok(userService.getUser(id));\n}\n\n// After\n@GetMapping(\"/users/{id}\")\npublic Mono&lt;ResponseEntity&lt;User&gt;&gt; getUser(@PathVariable Long id) {\n  return userService.getUser(id)\n    .map(ResponseEntity::ok)\n    .defaultIfEmpty(ResponseEntity.notFound().build());\n}</pre><br><strong>Common pitfalls:</strong><ul><li>Calling <code>.block()</code> — defeats the purpose</li><li>Mixing JPA (blocking) with reactive — wrap in <code>subscribeOn(boundedElastic())</code></li><li>Forgetting to subscribe — reactive chains are lazy!</li><li>Testing with real delays — use <code>StepVerifier.withVirtualTime()</code></li></ul>"},

    {q:"Compare reactive programming vs virtual threads (Java 21)",d:"hard",
     a:"<strong>Java 21 virtual threads</strong> are a game-changer that challenges the reactive model's raison d'être.<br><br><strong>Core comparison:</strong><br><table><tr><th>Aspect</th><th>Reactive (WebFlux)</th><th>Virtual Threads (MVC)</th></tr><tr><td>Code style</td><td>Functional chains</td><td>Familiar imperative</td></tr><tr><td>Debugging</td><td>Hard (no stack trace)</td><td>Normal stack traces</td></tr><tr><td>Learning curve</td><td>High</td><td>Low</td></tr><tr><td>Blocking I/O</td><td>Must avoid / wrap</td><td>Fine — VT parks cheaply</td></tr><tr><td>CPU-bound</td><td>No advantage</td><td>No advantage</td></tr><tr><td>Scalability</td><td>Excellent</td><td>Excellent (millions of VTs)</td></tr><tr><td>Memory</td><td>Low (few threads)</td><td>Very low (~few KB per VT)</td></tr><tr><td>Ecosystem</td><td>Requires reactive libs</td><td>Works with all existing libs</td></tr></table><br><strong>Virtual threads in Spring Boot 3.2+:</strong><br><pre># application.properties\nspring.threads.virtual.enabled=true\n\n# Now this blocking code scales to thousands of concurrent requests!\n@GetMapping(\"/users/{id}\")\npublic User getUser(@PathVariable Long id) {\n  User user = jdbcTemplate.queryForObject(...);\n  // Virtual thread parks here — no OS thread wasted\n  Order order = orderClient.getOrders(user.getId());\n  return buildResponse(user, order);\n}</pre><br><strong>When reactive STILL wins:</strong><ul><li><strong>Streaming:</strong> Flux/SSE/WebSocket — reactive model is natural</li><li><strong>Backpressure:</strong> Reactive has built-in mechanisms; virtual threads don't</li><li><strong>Complex transformations:</strong> Operators like <code>flatMap</code>, <code>zip</code> are elegant</li><li><strong>Existing reactive codebase:</strong> Don't migrate for the sake of it</li></ul><br><strong>When virtual threads win:</strong><ul><li>New projects — simpler code, same scalability</li><li>Teams without reactive experience</li><li>Libraries that don't support reactive (JDBC, legacy SDKs)</li></ul><br><strong>Recommendation:</strong> New projects should start with virtual threads + Spring MVC unless streaming/backpressure are requirements."},

    {q:"How do you debug reactive streams in production?",d:"hard",
     a:"<strong>Reactive debugging challenges:</strong> Async execution loses stack trace context across thread boundaries.<br><br><strong>Problem — useless stack traces:</strong><br><pre>// Exception shows internal Reactor frames, not your code:\njava.lang.NullPointerException\n  at reactor.core.publisher.FluxMap$MapSubscriber.onNext\n  at reactor.core.publisher.FluxFilter$FilterSubscriber.onNext\n  // Where is YOUR code?!</pre><br><strong>Solution 1 — Hooks.onOperatorDebug() (dev/staging only):</strong><br><pre>// Add to application startup\nHooks.onOperatorDebug();\n// Now stack traces show the assembly point:\n// UserService.java:45 - where you called .map()\n// Warning: significant performance overhead — dev/staging only</pre><br><strong>Solution 2 — ReactorDebugAgent (production-safe):</strong><br><pre>&lt;dependency&gt;\n  &lt;groupId&gt;io.projectreactor&lt;/groupId&gt;\n  &lt;artifactId&gt;reactor-tools&lt;/artifactId&gt;\n&lt;/dependency&gt;\n\n// In main class\nReactorDebugAgent.init(); // bytecode instrumentation, &lt;5% overhead</pre><br><strong>Solution 3 — checkpoint() for named operators:</strong><br><pre>userRepo.findById(id)\n  .checkpoint(\"findUser\")\n  .flatMap(user -> orderRepo.findByUser(user))\n  .checkpoint(\"findOrders\")\n  .map(this::toDto)\n  .checkpoint(\"toDto\", true); // true = include full stack</pre><br><strong>Solution 4 — log() operator for stream events:</strong><br><pre>userRepo.findAll()\n  .log(\"UserFlux\", Level.INFO)  // logs onNext, onError, onComplete\n  .log()  // logs with default category\n  .doOnNext(u -> log.debug(\"Processing user: {}\", u.getId()))\n  .doOnError(e -> log.error(\"Error in user stream\", e))\n  .subscribe();</pre><br><strong>Micrometer tracing with reactive context:</strong><br><pre>// Spring Boot 3 auto-instruments reactive chains\n// Trace IDs propagate through Reactor Context\n// Visible in Zipkin/Jaeger distributed traces</pre><br><strong>Tip:</strong> Use <code>doOnEach</code> signal-based logging that accesses the subscriber context for request correlation IDs."},

    {q:"When NOT to use reactive programming?",d:"hard",
     a:"<strong>Reactive has real costs — knowing when to avoid it is as important as knowing when to use it.</strong><br><br><strong>1. CPU-bound workloads:</strong><br><pre>// ❌ Reactive doesn't help here\n// Image processing, ML inference, cryptography, heavy computation\n// Thread-per-request is fine; reactive adds overhead without benefit\n\n// ✅ Better: Use virtual threads or CompletableFuture for parallel CPU work</pre><br><strong>2. Simple CRUD with a relational database:</strong><br><pre>// ❌ R2DBC lacks JPA's features (no lazy loading, complex mappings)\n// For simple CRUD: JPA + virtual threads = simpler AND scalable\n// Reactive wins only when: high concurrency + non-blocking DB driver exists</pre><br><strong>3. Teams without reactive experience:</strong><ul><li>Reactive bugs are harder to debug (non-linear execution)</li><li>Code review is harder</li><li>Onboarding time increases significantly</li><li>Common mistakes: forgetting to subscribe, mixing blocking code, misunderstanding hot/cold publishers</li></ul><br><strong>4. When you need to mix blocking libraries:</strong><br><pre>// If you have 1 blocking call that can't be avoided:\nMono.fromCallable(() -> legacyLib.blockingCall())\n  .subscribeOn(Schedulers.boundedElastic());\n// The entire benefit of reactive is undermined\n// You now have complexity WITHOUT full non-blocking benefit</pre><br><strong>5. Transactional workflows:</strong><br><pre>// Saga patterns in reactive are complex\n// Rollback logic is harder to reason about\n// @Transactional works with R2DBC but has limitations</pre><br><strong>Decision framework:</strong><br><table><tr><th>Scenario</th><th>Recommendation</th></tr><tr><td>&gt;10K concurrent I/O connections</td><td>WebFlux ✅</td></tr><tr><td>Streaming data</td><td>WebFlux ✅</td></tr><tr><td>Simple CRUD, small team</td><td>MVC + Virtual Threads ✅</td></tr><tr><td>CPU-bound</td><td>MVC + CompletableFuture ✅</td></tr><tr><td>Legacy blocking libs</td><td>MVC + Virtual Threads ✅</td></tr></table><br><strong>Rule of thumb:</strong> The scalability problem reactive solves is real — but virtual threads solve the same problem with simpler code for most applications."}
  ]
};
