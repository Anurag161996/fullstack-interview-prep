const SEC_DOCKER = {
  id:"docker",title:"Docker & Containers",icon:"🐳",ib:"rgba(56,189,248,.12)",ic:"#38bdf8",
  grp:"infra",bar:"#0ea5e9",tglow:"rgba(14,165,233,.07)",
  desc:"Images, multi-stage builds, layer caching, Dockerfile best practices, volumes, and health checks.",
  tags:["Multi-stage","Layer cache","Volumes","Networking","Health checks","Best practices"],
  qs:[
    {q:"What is the difference between a Docker image and a container?",d:"easy", id:1,
     a:"An <strong>image</strong> is a read-only, layered filesystem built from a Dockerfile. Each instruction (<code>RUN</code>, <code>COPY</code>, <code>ADD</code>) creates an immutable layer. Images are stored in registries (Docker Hub, ECR, GCR).<br><br>A <strong>container</strong> is a running instance of an image. Docker adds a thin writable layer on top (copy-on-write). Multiple containers share the same underlying image layers — no duplication.<br><br>Lifecycle: <code>docker build</code> → image. <code>docker run</code> → container from image. <code>docker stop</code> → stopped container. <code>docker rm</code> → container deleted. <code>docker rmi</code> → image deleted.<br><br><strong>Analogy:</strong> image = class, container = instance. You can run many containers from one image, each with independent state."},

    {q:"What is a multi-stage Docker build and why is it critical for Java?",d:"medium", id:2,
     a:"Multi-stage builds use multiple <code>FROM</code> instructions. The final image only contains what you copy in the last stage — earlier stages are discarded.<br><br><strong>Java example:</strong><br><pre>FROM eclipse-temurin:21-jdk AS builder\nWORKDIR /app\nCOPY pom.xml .\nRUN mvn dependency:go-offline -q\nCOPY src ./src\nRUN mvn package -q -DskipTests\n\nFROM eclipse-temurin:21-jre-alpine\nWORKDIR /app\nCOPY --from=builder /app/target/app.jar app.jar\nUSER 1001\nENTRYPOINT [\"java\", \"-jar\", \"app.jar\"]</pre><br><strong>Result:</strong> production image ~120MB (JRE + jar) vs ~600MB (JDK + Maven + source code). The smaller image:<ul><li>Pulls faster from registry</li><li>Has smaller attack surface (no compiler, no Maven)</li><li>Costs less in registry storage</li></ul>"},

    {q:"How does Docker layer caching work and how do you optimise a Java Dockerfile?",d:"medium", id:3,
     a:"Each Dockerfile instruction creates a layer. Docker caches each layer. If the instruction and all inputs are unchanged, Docker reuses the cached layer — skipping the expensive step. If a layer changes, all subsequent layers are <em>invalidated</em>.<br><br><strong>Optimisation — put slow, rarely-changing steps first:</strong><br><pre># Step 1: copy dependency file only (changes rarely)\nCOPY pom.xml .\n# Step 2: download deps (cached until pom.xml changes)\nRUN mvn dependency:go-offline -q\n# Step 3: copy source (changes often — invalidates from here)\nCOPY src ./src\n# Step 4: compile\nRUN mvn package -q -DskipTests</pre><br>Without this order: every source change redownloads all dependencies (~2 min). With this order: source change only triggers steps 3+4 (~10s).<br><br><strong>.dockerignore:</strong> exclude <code>target/</code>, <code>.git/</code>, <code>*.md</code>, <code>.idea/</code> to prevent unnecessary cache invalidation."},

    {q:"What is the difference between ENTRYPOINT and CMD?",d:"medium", id:4,
     a:"<code>ENTRYPOINT</code> is the main executable. It cannot be overridden with <code>docker run</code> arguments (unless you use <code>--entrypoint</code>).<br><br><code>CMD</code> provides default arguments to ENTRYPOINT. These ARE overridden by any arguments passed to <code>docker run</code>.<br><br><strong>Best practice for Java:</strong><br><pre>ENTRYPOINT [\"java\"]\nCMD [\"-jar\", \"app.jar\"]\n# Run with default: docker run myapp\n# Override CMD: docker run myapp -jar app.jar -Xmx512m</pre><br><strong>Always use exec form</strong> (<code>[\"java\", \"-jar\"]</code>) not shell form (<code>java -jar app.jar</code>). Shell form wraps in <code>/bin/sh -c</code> — the Java process is NOT PID 1 and won't receive <code>SIGTERM</code> from Docker for graceful shutdown.<br><br><strong>Graceful shutdown in Spring Boot:</strong> set <code>server.shutdown=graceful</code> — Spring waits for in-flight requests to complete when it receives SIGTERM."},

    {q:"What are Docker volumes vs bind mounts vs tmpfs?",d:"medium", id:5,
     a:"<strong>Volumes</strong> (preferred for production): managed by Docker, stored under <code>/var/lib/docker/volumes/</code>. Portable, survive container removal, can be shared between containers, work on all platforms.<br><pre>docker run -v mydata:/var/lib/postgresql/data postgres</pre><br><strong>Bind mounts:</strong> map a specific host path into the container. Tightly coupled to host filesystem structure. Good for development (live code reload without rebuild).<br><pre>docker run -v $(pwd)/src:/app/src myapp</pre><br><strong>tmpfs:</strong> in-memory only, never written to disk. Use for sensitive temp data (secrets), or high-speed scratch space. Lost when container stops.<br><pre>docker run --tmpfs /tmp myapp</pre><br><strong>Summary:</strong><ul><li>Production DB data → volume</li><li>Development source code → bind mount</li><li>Temporary secrets → tmpfs</li></ul>"},

    {q:"How do you add a health check to a Dockerfile and why does it matter for Kubernetes?",d:"medium", id:6,
     a:"<strong>Dockerfile HEALTHCHECK:</strong><br><pre>HEALTHCHECK \\\n  --interval=30s \\\n  --timeout=5s \\\n  --start-period=15s \\\n  --retries=3 \\\n  CMD curl -f http://localhost:8080/actuator/health || exit 1</pre><br>States: starting → healthy / unhealthy. Docker marks unhealthy containers and Swarm/k8s can act on them.<br><br><strong>Kubernetes probes</strong> (supersede Docker HEALTHCHECK in k8s):<ul><li><strong>livenessProbe:</strong> is the app alive? If it fails N times → container restarted. Map to <code>/actuator/health/liveness</code></li><li><strong>readinessProbe:</strong> is the app ready to receive traffic? If it fails → removed from Service endpoints (no traffic sent). Map to <code>/actuator/health/readiness</code></li><li><strong>startupProbe:</strong> give slow-starting apps time to boot before liveness kicks in.</li></ul><br>Spring Boot: <code>management.health.probes.enabled=true</code> enables <code>/liveness</code> and <code>/readiness</code> endpoints automatically."}
  ]
};

SEC_DOCKER.qs.push(
  {q:"How do you reduce Docker image size for a Spring Boot application?",d:"medium", id:7,
     a:"<strong>1. Multi-stage build</strong> (biggest win): compile with JDK, run with slim JRE. Saves ~400MB.<br><br><strong>2. Use JRE instead of JDK:</strong> <code>eclipse-temurin:21-jre-alpine</code> (~90MB) vs <code>eclipse-temurin:21-jdk</code> (~450MB).<br><br><strong>3. Use Distroless images:</strong> <code>gcr.io/distroless/java21-debian12</code> — no shell, no package manager, no OS tools. Smallest attack surface (~50MB with Java). Difficult to debug (no shell access).<br><br><strong>4. Layered JARs</strong> (Spring Boot 2.3+): Spring Boot fat JARs have a layering feature:<br><pre>COPY --from=builder /app/target/app.jar app.jar\nRUN java -Djarmode=layertools -jar app.jar extract\n# Results in layers:\nCOPY --from=builder /app/dependencies ./\nCOPY --from=builder /app/spring-boot-loader ./\nCOPY --from=builder /app/snapshot-dependencies ./\nCOPY --from=builder /app/application ./</pre>Dependencies layer (rarely changes) is cached separately from application layer (changes every build).<br><br><strong>5. jlink</strong>: create a custom minimal JRE with only the modules your app needs — can get down to 30–40MB."},

  {q:"What is Docker Compose and when do you use it vs Kubernetes?",d:"medium", id:8,
     a:"<strong>Docker Compose</strong> defines and runs multi-container applications using a YAML file. Starts all services, networks, and volumes with a single command.<br><pre>services:\n  app:\n    build: .\n    ports: [\"8080:8080\"]\n    environment:\n      DB_URL: jdbc:postgresql://db:5432/mydb\n    depends_on:\n      db:\n        condition: service_healthy\n  db:\n    image: postgres:16\n    healthcheck:\n      test: [\"CMD-SHELL\", \"pg_isready\"]\n    volumes:\n      - pgdata:/var/lib/postgresql/data\nvolumes:\n  pgdata:</pre><br><strong>Use Docker Compose for:</strong><ul><li>Local development — spin up the full stack with <code>docker compose up</code></li><li>Integration tests in CI (alongside Testcontainers)</li><li>Simple single-host deployments (small apps, side projects)</li></ul><br><strong>Use Kubernetes for:</strong><ul><li>Production workloads needing HA, auto-scaling, rolling deployments</li><li>Multi-node clusters</li><li>Pod health management, service discovery, secret management</li><li>Resource quotas, namespace isolation across teams</li></ul>"}
);

SEC_DOCKER.qs.push(
  {q:"What are container resource limits and why must you set them?",d:"medium", id:9,
     a:"Without resource limits, a single container can consume all CPU and memory on the host — starving other containers and potentially crashing the node.<br><br><strong>Docker run limits:</strong><br><pre>docker run \\\n  --memory=\"512m\"          # hard memory limit (OOMKilled if exceeded)\n  --memory-reservation=\"256m\"  # soft limit (target under pressure)\n  --cpus=\"0.5\"            # use at most 0.5 CPU cores\n  --cpu-shares=512        # relative CPU weight (default 1024)\n  myapp</pre><br><strong>Kubernetes resources (production standard):</strong><br><pre>resources:\n  requests:      # minimum guaranteed\n    memory: \"256Mi\"\n    cpu: \"250m\"  # 250 millicores = 0.25 CPU\n  limits:        # maximum allowed\n    memory: \"512Mi\"\n    cpu: \"500m\"</pre><br><strong>Why both requests AND limits:</strong><ul><li>Requests: used by the scheduler to place pods on nodes with enough capacity</li><li>Limits: enforced at runtime — container is OOMKilled if memory exceeds limit; CPU throttled if over CPU limit</li></ul><br><strong>Java JVM pitfall:</strong> without <code>-XX:+UseContainerSupport</code> (auto in Java 11+), the JVM reads the host's total RAM and sets its heap accordingly — can easily exceed the container's memory limit and get OOMKilled."},

  {q:"What are Kubernetes Pods, Deployments, Services, and ConfigMaps?",d:"medium", id:10,
     a:"<strong>Pod:</strong> the smallest deployable unit in k8s. One or more containers sharing network and storage. Ephemeral — if it dies, it's replaced by a new Pod with a new IP.<br><br><strong>Deployment:</strong> manages a set of identical Pods (replicas). Handles rolling updates, rollbacks, and self-healing (restarts crashed Pods).<br><pre>spec:\n  replicas: 3\n  strategy:\n    type: RollingUpdate\n    rollingUpdate:\n      maxSurge: 1         # max extra pods during update\n      maxUnavailable: 0   # no downtime</pre><br><strong>Service:</strong> a stable DNS name and virtual IP (ClusterIP) for a set of Pods. Load balances traffic across healthy Pods. Types:<ul><li><code>ClusterIP</code> — internal only</li><li><code>NodePort</code> — exposed on every node's port</li><li><code>LoadBalancer</code> — provisions cloud load balancer (AWS ALB)</li></ul><br><strong>ConfigMap:</strong> store non-sensitive configuration as key-value pairs. Mounted as environment variables or files into Pods.<br><br><strong>Secret:</strong> same as ConfigMap but base64-encoded and access-controlled. For passwords, API keys, TLS certs.<br><br>Spring Boot reads ConfigMap values via env vars: <code>MY_APP_DB_URL</code> → <code>spring.datasource.url</code> (kebab to underscore conversion)."},

  {q:"What is a Dockerfile security checklist?",d:"medium", id:11,
     a:"Common security mistakes in Dockerfiles and how to fix them:<br><br><strong>1. Don't run as root:</strong><br><pre># Add a non-root user\nRUN addgroup --system appgroup && adduser --system --ingroup appgroup appuser\nUSER appuser\n# Container process runs as UID 1000+, not 0</pre><br><strong>2. Use specific image tags — never :latest:</strong><br><pre>FROM eclipse-temurin:21.0.2_13-jre-alpine  # exact version\n# NOT: FROM eclipse-temurin:latest</pre><br><strong>3. Scan images for vulnerabilities:</strong><br><pre>trivy image myapp:1.0.0\n# or in CI:\ndocker scout cves myapp:1.0.0</pre><br><strong>4. Don't copy .env or secret files:</strong><br><pre># .dockerignore\n.env\n*.key\n*.pem\n.aws/</pre><br><strong>5. Use multi-stage builds</strong> — no build tools or source code in production image.<br><br><strong>6. Read-only filesystem where possible:</strong><br><pre>docker run --read-only --tmpfs /tmp myapp</pre><br><strong>7. Don't store secrets in image layers:</strong> use Kubernetes Secrets, AWS Secrets Manager, or Docker secrets. Never <code>COPY credentials.json .</code><br><br><strong>8. HEALTHCHECK</strong> so orchestrators know when the container is ready."},

  {q:"What is Kubernetes Horizontal Pod Autoscaler (HPA)?",d:"medium", id:12,
     a:"HPA automatically scales the number of Pod replicas based on observed metrics — CPU, memory, or custom metrics.<br><br><strong>Basic CPU-based HPA:</strong><br><pre>apiVersion: autoscaling/v2\nkind: HorizontalPodAutoscaler\nmetadata:\n  name: order-service-hpa\nspec:\n  scaleTargetRef:\n    apiVersion: apps/v1\n    kind: Deployment\n    name: order-service\n  minReplicas: 2\n  maxReplicas: 20\n  metrics:\n    - type: Resource\n      resource:\n        name: cpu\n        target:\n          type: Utilization\n          averageUtilization: 60  # scale up when avg CPU > 60%</pre><br><strong>Custom metrics HPA</strong> (e.g., scale on Kafka consumer lag):<br><pre>metrics:\n  - type: External\n    external:\n      metric:\n        name: kafka_consumer_lag\n        selector:\n          matchLabels:\n            consumer_group: order-group\n      target:\n        type: Value\n        value: \"1000\"  # scale up when lag > 1000 messages</pre><br><strong>Scale-down stabilisation window:</strong> prevent flapping by requiring metrics to stay below threshold for 5 minutes before scaling down. Always set <code>minReplicas ≥ 2</code> for HA."}
);

SEC_DOCKER.qs.push(
  {q:"How do you set resource limits on a Docker container?",d:"medium", id:13,
     a:"Without limits, a single container can consume all host CPU/memory — starving other containers or the host OS.<br><br><strong>Memory limits:</strong><br><pre>docker run -m 512m --memory-swap 512m myapp\n# -m: max RAM\n# --memory-swap = -m means no swap (swap = memory-swap - m)\n# If container exceeds limit → OOMKilled</pre><br><strong>CPU limits:</strong><br><pre>docker run --cpus=1.5 myapp         # max 1.5 CPU cores\ndocker run --cpu-shares=512 myapp   # relative weight (default 1024)</pre><br><strong>In Docker Compose:</strong><br><pre>services:\n  app:\n    deploy:\n      resources:\n        limits:\n          cpus: '1.0'\n          memory: 512M\n        reservations:\n          cpus: '0.25'\n          memory: 128M</pre><br><strong>Java + containers:</strong> JVM respects container memory limits since Java 11 (<code>-XX:+UseContainerSupport</code> enabled by default). Set <code>-XX:MaxRAMPercentage=75.0</code> to use 75% of container memory — leave 25% for OS and non-heap.<br><br><strong>Kubernetes resource requests and limits:</strong> same concept with <code>resources.requests</code> (scheduling guarantee) and <code>resources.limits</code> (hard cap)."},

  {q:"What is a distroless Docker image and when should you use it?",d:"hard", id:14,
     a:"A <strong>distroless</strong> image contains only the application runtime and its dependencies — no shell, no package manager, no OS utilities.<br><br><strong>Comparison:</strong><ul><li><code>eclipse-temurin:21-jre</code> — full JRE + Ubuntu base (~230MB, has bash, apt)</li><li><code>eclipse-temurin:21-jre-alpine</code> — JRE + Alpine (~90MB, has sh, apk)</li><li><code>gcr.io/distroless/java21-debian12</code> — JRE only, no shell (~50MB)</li></ul><br><strong>Security benefits:</strong><ul><li>No shell = no RCE via shell injection</li><li>Smaller attack surface — fewer packages to CVE-scan</li><li>Harder to exploit: attacker can't run commands interactively</li></ul><br><strong>Dockerfile example:</strong><br><pre>FROM eclipse-temurin:21-jdk AS builder\nWORKDIR /app\nCOPY pom.xml .\nRUN mvn dependency:go-offline -q\nCOPY src ./src\nRUN mvn package -DskipTests -q\n\nFROM gcr.io/distroless/java21-debian12\nCOPY --from=builder /app/target/app.jar /app.jar\nEXPOSE 8080\nENTRYPOINT [\"java\", \"-jar\", \"/app.jar\"]</pre><br><strong>Debugging challenge:</strong> no shell means you can't <code>exec</code> into the container to debug. Use ephemeral debug containers in Kubernetes (<code>kubectl debug</code>) or sidecar containers."},

  {q:"What are Kubernetes Deployments, Services, and ConfigMaps?",d:"medium", id:15,
     a:"<strong>Deployment:</strong> manages a set of identical pods. Handles rolling updates, rollbacks, and scaling.<br><pre>apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: order-service\nspec:\n  replicas: 3\n  selector:\n    matchLabels:\n      app: order-service\n  template:\n    metadata:\n      labels:\n        app: order-service\n    spec:\n      containers:\n        - name: order-service\n          image: myrepo/order-service:1.2.3\n          ports:\n            - containerPort: 8080\n          resources:\n            limits:\n              memory: \"512Mi\"\n              cpu: \"500m\"</pre><br><strong>Service:</strong> provides a stable DNS name and load-balanced IP for a set of pods (selected by label). Types: ClusterIP (internal), NodePort, LoadBalancer (creates cloud LB).<br><pre>apiVersion: v1\nkind: Service\nspec:\n  selector:\n    app: order-service  # routes to matching pods\n  ports:\n    - port: 80\n      targetPort: 8080</pre><br><strong>ConfigMap:</strong> stores non-secret configuration as key-value pairs. Mounted as environment variables or files into pods. Change ConfigMap → rolling restart required (or use Spring Cloud Config for live refresh)."}
);

SEC_DOCKER.qs.push(
  {q:"What are Kubernetes core concepts — Pod, Deployment, Service, ConfigMap?",d:"medium", id:16,
     a:"<strong>Pod:</strong> the smallest deployable unit in Kubernetes — one or more containers sharing a network namespace and storage. Containers in a pod communicate via localhost. Pods are ephemeral — they can be killed and replaced.<br><br><strong>Deployment:</strong> manages a desired number of pod replicas. Handles rolling updates, rollback, and self-healing (replaces failed pods).<br><pre>apiVersion: apps/v1\nkind: Deployment\nspec:\n  replicas: 3\n  strategy:\n    type: RollingUpdate\n    rollingUpdate:\n      maxSurge: 1\n      maxUnavailable: 0  # zero-downtime\n  template:\n    spec:\n      containers:\n        - name: order-service\n          image: myrepo/order-service:v2.1\n          resources:\n            requests: { memory: \"256Mi\", cpu: \"250m\" }\n            limits:   { memory: \"512Mi\", cpu: \"500m\" }</pre><br><strong>Service:</strong> stable DNS name and IP for a set of pods. Types: ClusterIP (internal), NodePort (external via node IP), LoadBalancer (provisions cloud LB), ExternalName.<br><br><strong>ConfigMap:</strong> stores non-sensitive config (app.properties, feature flags). Mounted as volumes or env vars. <strong>Secret:</strong> same but base64-encoded (use external secrets management in production — AWS Secrets Manager, Vault)."},

  {q:"What is Horizontal Pod Autoscaling (HPA) in Kubernetes?",d:"medium", id:17,
     a:"HPA automatically scales the number of pod replicas based on observed metrics — keeping your service responsive under variable load without over-provisioning.<br><br><strong>CPU-based HPA:</strong><br><pre>apiVersion: autoscaling/v2\nkind: HorizontalPodAutoscaler\nspec:\n  scaleTargetRef:\n    apiVersion: apps/v1\n    kind: Deployment\n    name: order-service\n  minReplicas: 2\n  maxReplicas: 20\n  metrics:\n    - type: Resource\n      resource:\n        name: cpu\n        target:\n          type: Utilization\n          averageUtilization: 70  # scale when avg CPU &gt; 70%</pre><br><strong>Custom metric HPA</strong> (scale on Kafka consumer lag):<br><pre>metrics:\n  - type: External\n    external:\n      metric:\n        name: kafka_consumer_lag_sum\n        selector:\n          matchLabels:\n            topic: orders\n      target:\n        type: AverageValue\n        averageValue: 1000  # scale when lag &gt; 1000 per replica</pre><br><strong>Key settings:</strong> <code>--horizontal-pod-autoscaler-sync-period</code> (default 15s), scale-up stabilization (60s), scale-down stabilization (300s) — prevents thrashing."},

  {q:"What are Kubernetes resource requests and limits and why do they matter?",d:"medium", id:18,
     a:"<strong>Requests:</strong> the minimum resources a container needs. Kubernetes uses requests for scheduling — it finds a node with at least this much available capacity.<br><br><strong>Limits:</strong> the maximum resources a container can use. Kubernetes enforces limits — the container is throttled (CPU) or killed (memory OOM) if it exceeds them.<br><br><pre>resources:\n  requests:\n    memory: \"256Mi\"  # guaranteed\n    cpu: \"250m\"      # 0.25 CPU cores guaranteed\n  limits:\n    memory: \"512Mi\"  # killed if exceeded (OOMKilled)\n    cpu: \"1000m\"     # throttled if exceeded (not killed)</pre><br><strong>QoS classes (affects eviction priority):</strong><ul><li><strong>Guaranteed:</strong> requests == limits (both set equally) — last to be evicted</li><li><strong>Burstable:</strong> requests &lt; limits — evicted after BestEffort</li><li><strong>BestEffort:</strong> no requests or limits — first to be evicted under pressure</li></ul><br><strong>Java-specific:</strong> always set <code>-XX:MaxRAMPercentage=75</code> (use 75% of container memory limit for heap) and configure <code>-XX:+UseContainerSupport</code> (default in Java 11+) so JVM respects container limits rather than host memory."},

  {q:"What is the difference between liveness, readiness, and startup probes?",d:"medium", id:19,
     a:"Kubernetes health checks ensure pods are functioning correctly and ready to serve traffic.<br><br><strong>Liveness probe:</strong> \"Is the container alive?\" If it fails, Kubernetes kills and restarts the container. Use for detecting deadlock or unrecoverable states.<br><pre>livenessProbe:\n  httpGet:\n    path: /actuator/health/liveness\n    port: 8080\n  initialDelaySeconds: 60\n  periodSeconds: 10\n  timeoutSeconds: 5\n  failureThreshold: 3  # restart after 3 failures</pre><br><strong>Readiness probe:</strong> \"Is the container ready to serve traffic?\" If it fails, Kubernetes removes the pod from Service endpoints (no traffic routed). Use for warm-up, dependency checks (DB connection).<br><pre>readinessProbe:\n  httpGet:\n    path: /actuator/health/readiness\n    port: 8080\n  initialDelaySeconds: 10\n  periodSeconds: 5</pre><br><strong>Startup probe:</strong> \"Has the container finished starting?\" Used for slow-starting apps (Spring Boot). While startup probe is running, liveness/readiness are disabled. Once startup succeeds, liveness/readiness take over.<br><pre>startupProbe:\n  httpGet:\n    path: /actuator/health\n    port: 8080\n  failureThreshold: 30  # 30 attempts\n  periodSeconds: 10     # = 5 minutes total startup time</pre><br><strong>Spring Boot endpoints:</strong> <code>/actuator/health/liveness</code>, <code>/actuator/health/readiness</code> (Spring Boot 2.3+)."},

  {q:"What is PodDisruptionBudget and why does it matter?",d:"hard", id:20,
     a:"<strong>PodDisruptionBudget (PDB)</strong> limits how many pods can be voluntarily disrupted at once during cluster maintenance (node drain, upgrades, scaling) — ensures your service remains available.<br><br><strong>Problem without PDB:</strong> during a node drain, Kubernetes evicts all pods on that node simultaneously. If that node hosts all replicas of your service → downtime.<br><br><strong>PDB example — ensure at least 2 replicas always available:</strong><br><pre>apiVersion: policy/v1\nkind: PodDisruptionBudget\nmetadata:\n  name: order-service-pdb\nspec:\n  minAvailable: 2  # always keep 2 pods running\n  selector:\n    matchLabels:\n      app: order-service</pre><br><strong>Or specify maximum disruption:</strong><br><pre>maxUnavailable: 1  # never evict more than 1 pod at a time</pre><br><strong>When PDB applies:</strong><ul><li>Voluntary disruptions: <code>kubectl drain</code>, cluster autoscaler scale-down, rolling update</li><li><strong>Does NOT apply to:</strong> pod crashes, OOMKills, node failures (involuntary)</li></ul><br><strong>Effect:</strong> if draining a node would violate the PDB, the drain operation waits or fails. Prevents accidental full outages during maintenance.<br><br><strong>Best practice:</strong> always set a PDB for production services with <code>minAvailable: N-1</code> where N is your desired replica count."},

  {q:"How do you do zero-downtime rolling deployment in Kubernetes with Spring Boot?",d:"hard", id:21,
     a:"<strong>Rolling deployment:</strong> gradually replace old pods with new ones — some old pods serve traffic while new ones start.<br><br><strong>Deployment strategy:</strong><br><pre>spec:\n  replicas: 3\n  strategy:\n    type: RollingUpdate\n    rollingUpdate:\n      maxSurge: 1        # max 1 extra pod during update (4 total)\n      maxUnavailable: 0  # never go below 3 pods</pre><br><strong>Health checks (critical for zero-downtime):</strong><br><pre>readinessProbe:\n  httpGet:\n    path: /actuator/health/readiness\n    port: 8080\n  initialDelaySeconds: 20  # Spring Boot warm-up\n  periodSeconds: 5\n  failureThreshold: 3\n\nlivenessProbe:\n  httpGet:\n    path: /actuator/health/liveness\n    port: 8080\n  initialDelaySeconds: 60</pre><br><strong>Graceful shutdown in Spring Boot:</strong><br><pre>server.shutdown=graceful\nspring.lifecycle.timeout-per-shutdown-phase=30s</pre>When Kubernetes sends SIGTERM, Spring Boot stops accepting new requests, finishes in-flight requests (up to 30s), then exits.<br><br><strong>PreStop hook (optional delay before SIGTERM):</strong><br><pre>lifecycle:\n  preStop:\n    exec:\n      command: [\"sh\", \"-c\", \"sleep 10\"]  # delay before shutdown</pre>Gives load balancers time to remove the pod from endpoints before it starts shutting down.<br><br><strong>Testing:</strong> <code>kubectl rollout status deployment/order-service</code> to verify all pods became ready."},

  {q:"What is ResourceQuota and LimitRange in Kubernetes?",d:"medium", id:22,
     a:"<strong>ResourceQuota</strong> limits the total resources a namespace can consume — prevents one team from starving others.<br><br><strong>Example — limit namespace to 10 cores, 20GB RAM:</strong><br><pre>apiVersion: v1\nkind: ResourceQuota\nmetadata:\n  name: team-a-quota\n  namespace: team-a\nspec:\n  hard:\n    requests.cpu: \"10\"       # max 10 CPU cores requested\n    requests.memory: 20Gi     # max 20GB memory requested\n    limits.cpu: \"20\"          # max 20 CPU cores limit\n    limits.memory: 40Gi\n    pods: \"50\"                # max 50 pods\n    services.loadbalancers: \"2\"  # max 2 LoadBalancer services</pre><br><strong>Effect:</strong> if a Deployment tries to exceed the quota, pod creation is rejected until resources are freed.<br><br><strong>LimitRange</strong> enforces default and min/max resource constraints per container — ensures every pod has reasonable limits.<br><br><strong>Example — default limits for containers:</strong><br><pre>apiVersion: v1\nkind: LimitRange\nmetadata:\n  name: default-limits\n  namespace: team-a\nspec:\n  limits:\n    - type: Container\n      default:           # default limit if not specified\n        cpu: 500m\n        memory: 512Mi\n      defaultRequest:    # default request if not specified\n        cpu: 200m\n        memory: 256Mi\n      max:               # maximum allowed\n        cpu: 2\n        memory: 2Gi\n      min:               # minimum required\n        cpu: 50m\n        memory: 64Mi</pre><br><strong>Use case:</strong> prevent accidental resource hogs (someone deploys a pod with no limits and consumes all node memory)."},

  {q:"How do Kubernetes ConfigMap and Secret mount into a Pod?",d:"medium", id:23,
     a:"Both ConfigMap and Secret inject configuration into pods — ConfigMap for non-sensitive data, Secret for sensitive data (base64-encoded at rest).<br><br><strong>Mount as environment variables:</strong><br><pre>apiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: app-config\ndata:\n  DATABASE_URL: \"jdbc:postgresql://db:5432/orders\"\n  LOG_LEVEL: \"INFO\"\n\n---\nspec:\n  containers:\n    - name: app\n      envFrom:\n        - configMapRef:\n            name: app-config  # all keys become env vars</pre><br><strong>Mount as a volume (files):</strong><br><pre>spec:\n  containers:\n    - name: app\n      volumeMounts:\n        - name: config-volume\n          mountPath: /etc/config  # ConfigMap mounted here\n  volumes:\n    - name: config-volume\n      configMap:\n        name: app-config\n# Files: /etc/config/DATABASE_URL, /etc/config/LOG_LEVEL</pre><br><strong>Secret example (TLS certificate):</strong><br><pre>apiVersion: v1\nkind: Secret\nmetadata:\n  name: tls-secret\ntype: kubernetes.io/tls\ndata:\n  tls.crt: LS0t...  # base64-encoded certificate\n  tls.key: LS0t...  # base64-encoded private key\n\n---\nvolumeMounts:\n  - name: tls\n    mountPath: /etc/tls\n    readOnly: true\nvolumes:\n  - name: tls\n    secret:\n      secretName: tls-secret</pre><br><strong>Auto-reload on change:</strong> when mounted as a volume, changes to the ConfigMap/Secret are eventually reflected in the container (kubelet sync, ~60s). For environment variables, you must restart the pod."}
);