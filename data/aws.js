const SEC_AWS = {
  id:"aws",title:"AWS Core Services",icon:"☁",ib:"rgba(251,146,60,.12)",ic:"#fb923c",
  grp:"infra",bar:"#fb923c",tglow:"rgba(251,146,60,.07)",
  desc:"EC2, ECS/Fargate, Lambda, SQS, SNS, ALB, IAM, S3, RDS, Auto Scaling, VPC, and monitoring.",
  tags:["EC2/ECS","SQS/SNS","IAM","S3","RDS","Auto Scaling","VPC"],
  qs:[
    {q:"What is the difference between EC2, ECS Fargate, and Lambda?",d:"medium",
     a:"<strong>EC2 (Elastic Compute Cloud):</strong> raw virtual machines. You choose the OS, instance type, storage. Full control. You manage: OS patching, auto-scaling (ASG), AMI updates, SSH access.<br>Best for: workloads needing specific hardware, custom OS configuration, stateful applications.<br><br><strong>ECS Fargate:</strong> run Docker containers without managing EC2 instances. AWS provisions and scales the underlying infrastructure. Define vCPU and memory per task. Pay per vCPU/memory-second only while running.<br>Best for: containerised microservices where you want container orchestration without k8s complexity.<br><br><strong>Lambda:</strong> serverless functions. No servers to manage. Code runs in response to events (API Gateway, SQS, S3, CloudWatch Events). Pay per invocation + per 100ms execution. Max 15 min runtime, limited memory (10GB), ephemeral filesystem.<br>Best for: event-driven processing, scheduled tasks, API backends with variable traffic, glue code.<br><br><strong>Rule of thumb:</strong> Lambda for short tasks, Fargate for services, EC2 for maximum control."},

    {q:"What is the difference between SQS and SNS and what is the fan-out pattern?",d:"medium",
     a:"<strong>SQS (Simple Queue Service):</strong> point-to-point queue. One consumer group processes each message. Messages persisted until consumed or TTL expires. Pull model — consumers poll. At-least-once delivery.<br><ul><li>Standard: best-effort ordering, near-unlimited throughput</li><li>FIFO: exactly-once processing, strict ordering, 3,000 TPS (or 300 without batching)</li></ul><br><strong>SNS (Simple Notification Service):</strong> pub/sub. One message fan-out to multiple subscriber types: SQS queues, Lambda, HTTP/S endpoints, email, SMS. Push model — SNS pushes to subscribers.<br><br><strong>Fan-out pattern:</strong><br><pre>Order Service → SNS topic: OrderPlaced\n                    ├── SQS: EmailQueue → Email Service\n                    ├── SQS: InventoryQueue → Inventory Service\n                    └── SQS: AnalyticsQueue → Analytics Service</pre>Each downstream service gets its own SQS queue: independent scaling, retry policies, and DLQs. Producer sends one message to SNS — all consumers receive it."},

    {q:"What is the difference between ALB and NLB?",d:"medium",
     a:"<strong>ALB (Application Load Balancer) — Layer 7:</strong> understands HTTP/HTTPS/gRPC.<ul><li>Route by path: <code>/api/*</code> → API servers, <code>/static/*</code> → S3/CDN</li><li>Route by hostname: <code>api.example.com</code> vs <code>app.example.com</code></li><li>Route by headers or query params</li><li>Native integration with WAF (web application firewall), Cognito, ECS, Lambda</li><li>HTTP/2 and WebSocket support</li><li>Connection terminates at ALB (can inspect and modify requests)</li></ul><br><strong>NLB (Network Load Balancer) — Layer 4:</strong> TCP/UDP/TLS.<ul><li>Ultra-low latency (microseconds vs milliseconds)</li><li>Handles millions of connections per second</li><li>Preserves client source IP (ALB doesn't — use X-Forwarded-For)</li><li>Static Elastic IP per AZ — required for IP whitelisting in firewalls</li><li>VPC PrivateLink backend</li></ul><br><strong>Choose ALB</strong> for web apps and microservices. <strong>Choose NLB</strong> for non-HTTP, extreme performance, or when you need static IPs."},

    {q:"What are IAM roles and what are the best practices?",d:"medium",
     a:"IAM (Identity and Access Management) controls who can do what in AWS.<br><br><strong>Key concepts:</strong><ul><li><strong>Policy:</strong> JSON document with Allow/Deny on Actions (e.g., <code>s3:GetObject</code>) and Resources (e.g., <code>arn:aws:s3:::my-bucket/*</code>)</li><li><strong>Role:</strong> an identity that can be assumed by services (EC2, ECS task, Lambda) or other AWS accounts. Credentials auto-rotate via instance metadata service.</li><li><strong>User:</strong> for human identities. Should have MFA enabled. Use roles instead of users for applications.</li></ul><br><strong>Best practices:</strong><ol><li>Never use root account for day-to-day operations</li><li>Assign IAM roles to EC2/ECS/Lambda — never hardcode credentials in code or environment variables</li><li>Principle of least privilege — grant only the minimum required permissions</li><li>Enable MFA for all human IAM users</li><li>Use IAM Access Analyzer to identify overly permissive policies</li><li>Use VPC endpoints for S3/DynamoDB to keep traffic private (no public internet)</li><li>Audit with CloudTrail — every API call is logged</li></ol>"},

    {q:"How does S3 work and how do you handle large file uploads?",d:"medium",
     a:"S3 (Simple Storage Service) is object storage — a flat namespace of buckets and keys. Virtually unlimited storage, 11 nines of durability, global replication options.<br><br><strong>Multipart upload (for files &gt;100MB):</strong><br><pre>// Initiate\nInitiateMultipartUploadResult init = s3.initiateMultipartUpload(bucket, key);\nString uploadId = init.getUploadId();\n// Upload parts (min 5MB each, parallel)\nUploadPartResult part1 = s3.uploadPart(new UploadPartRequest()\n  .withBucketName(bucket).withKey(key).withUploadId(uploadId)\n  .withPartNumber(1).withInputStream(stream).withPartSize(partSize));\n// Complete\ns3.completeMultipartUpload(new CompleteMultipartUploadRequest(\n  bucket, key, uploadId, etags));</pre><br><strong>Pre-signed URLs</strong> — let clients upload/download directly to S3 without routing through your servers:<br><pre>URL url = s3.generatePresignedUrl(bucket, key,\n  Date.from(Instant.now().plusSeconds(900)),\n  HttpMethod.PUT);</pre><br>Share the URL with the client — they PUT directly to S3. TTL prevents abuse. Reduces your bandwidth costs significantly."},

    {q:"What is an Auto Scaling Group and what triggers scaling?",d:"medium",
     a:"An ASG maintains a fleet of EC2 or ECS instances between configured min/desired/max bounds. It automatically replaces unhealthy instances and adjusts capacity based on demand.<br><br><strong>Scaling policies:</strong><ul><li><strong>Target Tracking</strong> (simplest): maintain avg CPU at 60%. AWS calculates the required capacity change automatically.</li><li><strong>Step Scaling:</strong> scale by specific amounts based on CloudWatch alarm severity. E.g., +2 instances when CPU 60-80%, +4 when CPU &gt;80%.</li><li><strong>Scheduled Scaling:</strong> pre-configure known load patterns. E.g., scale up at 8am weekdays, scale down at 8pm.</li><li><strong>Predictive Scaling:</strong> ML-based forecast — pre-scales before predicted load spike.</li></ul><br><strong>Custom metrics:</strong> you can scale on SQS queue depth (<code>ApproximateNumberOfMessagesVisible</code>) — scale consumers based on backlog.<br><br><strong>Integration:</strong> pair ASG with an ALB — new instances register automatically and receive traffic only once healthy (pass health checks). Draining removes them from load balancer gracefully before termination."},

    {q:"What is a VPC and what are the key networking concepts?",d:"medium",
     a:"A <strong>VPC</strong> (Virtual Private Cloud) is your own isolated section of the AWS network — a private subnet of IP addresses that you fully control.<br><br><strong>Key concepts:</strong><ul><li><strong>Subnet:</strong> divide the VPC into public (has route to Internet Gateway) and private (no direct internet) segments. App servers go in private subnets; only load balancers in public.</li><li><strong>Security Group:</strong> stateful firewall on instances. Return traffic is automatically allowed. Define inbound: allow port 8080 from ALB security group only.</li><li><strong>NACL:</strong> stateless firewall at the subnet level. Both inbound and outbound must be explicitly allowed. Defence-in-depth layer.</li><li><strong>Internet Gateway:</strong> allows public subnet resources to reach the internet.</li><li><strong>NAT Gateway:</strong> allows private subnet instances to reach the internet for updates/downloads (outbound only) — they remain unreachable from outside.</li><li><strong>VPC Peering / Transit Gateway:</strong> connect multiple VPCs for cross-account or cross-region communication.</li></ul>"}
  ]
};

SEC_AWS.qs.push(
  {q:"What is AWS SQS Dead Letter Queue (DLQ) and how do you use it?",d:"medium",
   a:"A <strong>DLQ</strong> receives messages that couldn't be successfully processed after N attempts — preventing a poison pill from blocking the queue forever.<br><br><strong>Configuration:</strong><br><pre>// CDK / CloudFormation:\nconst dlq = new sqs.Queue(this, 'OrdersDlq', {\n  retentionPeriod: Duration.days(14)\n});\nconst main = new sqs.Queue(this, 'Orders', {\n  deadLetterQueue: {\n    queue: dlq,\n    maxReceiveCount: 3  // move to DLQ after 3 failures\n  }\n});</pre><br><strong>Spring Boot consumer:</strong><br><pre>@SqsListener(value = \"${orders.queue}\", deletionPolicy = ON_SUCCESS)\nvoid processOrder(Order order) {\n  // if this throws 3 times, SQS moves the message to DLQ\n  orderService.process(order);\n}</pre><br><strong>DLQ monitoring:</strong> CloudWatch alarm on <code>ApproximateNumberOfMessagesVisible &gt; 0</code> — alert your team immediately.<br><br><strong>Redriving:</strong> after fixing the bug, use SQS Dead Letter Queue Redrive to move messages back to the source queue for reprocessing."},

  {q:"What is AWS Lambda cold start and how do you minimise it?",d:"hard",
   a:"A <strong>cold start</strong> occurs when Lambda needs to provision a new execution environment — download the container image, initialise the runtime, and run your initialisation code. This can take 500ms–3s for Java with Spring.<br><br><strong>Why Java is slow:</strong> JVM startup + Spring context initialisation is expensive. JavaScript/Python cold starts are typically &lt;100ms.<br><br><strong>Mitigation strategies:</strong><ul><li><strong>Provisioned Concurrency:</strong> keeps N instances warm and ready — eliminates cold starts for those instances. Costs money.</li><li><strong>Lambda SnapStart</strong> (Java, AWS 2022): Lambda snapshots the JVM state after initialisation and restores it for new instances — cold start reduced to ~200ms</li><li><strong>Spring Cloud Function + Spring AOT:</strong> Spring generates native image-friendly code; combine with GraalVM native image for sub-100ms cold start</li><li><strong>Reduce package size:</strong> smaller JARs initialise faster. Use jlink or Quarkus/Micronaut for lighter Java runtimes</li><li><strong>Move initialisation to static blocks:</strong> Lambda reuses execution environments — one-time initialisation in static context is free after warmup</li></ul>"}
);

SEC_AWS.qs.push(
  {q:"What is the difference between RDS Multi-AZ and Read Replicas?",d:"medium",
   a:"<strong>RDS Multi-AZ:</strong> high availability (HA) feature — keeps a synchronous standby replica in a different AZ. On primary failure (hardware, AZ outage), RDS automatically promotes the standby in ~1–2 minutes. The CNAME endpoint switches transparently — applications reconnect to the new primary.<ul><li>✅ Automatic failover — no manual intervention</li><li>✅ Same endpoint for the application</li><li>❌ Standby is NOT readable — it only exists for failover</li><li>❌ Synchronous replication adds slight write latency</li></ul><br><strong>Read Replicas:</strong> read scaling — asynchronous replicas that serve SELECT queries, offloading read traffic from the primary.<ul><li>✅ Scale read throughput (up to 5 replicas in RDS, 15 in Aurora)</li><li>✅ Can be in different regions (cross-region read replicas)</li><li>❌ Asynchronous — slight replication lag (usually &lt;1s but can be more)</li><li>❌ Must update application to route reads to replica endpoint</li></ul><br><strong>Aurora:</strong> automatically combines both — 1 writer endpoint + 1 reader endpoint (load-balanced across up to 15 read replicas). Failover in ~30 seconds. Recommended over standard RDS for production."},

  {q:"What are AWS Secrets Manager and Parameter Store, and when do you use each?",d:"medium",
   a:"Both store configuration and secrets, but they serve different use cases:<br><br><strong>AWS Systems Manager Parameter Store:</strong><ul><li>Free for standard parameters (up to 10,000)</li><li>SecureString parameters encrypted with KMS</li><li>No automatic rotation</li><li>Best for: configuration values, feature flags, non-secret config</li></ul><br><strong>AWS Secrets Manager:</strong><ul><li>Paid ($0.40/secret/month + API call costs)</li><li>Automatic secret rotation via Lambda (built-in for RDS, ElastiCache, Redshift)</li><li>Cross-account secret sharing</li><li>Best for: database credentials, API keys, OAuth tokens — anything needing rotation</li></ul><br><strong>Spring Boot integration (AWS Secrets Manager):</strong><br><pre>&lt;dependency&gt;\n  &lt;groupId&gt;io.awspring.cloud&lt;/groupId&gt;\n  &lt;artifactId&gt;spring-cloud-aws-starter-secrets-manager&lt;/artifactId&gt;\n&lt;/dependency&gt;\n\n# application.yml\nspring:\n  config:\n    import: \"aws-secretsmanager:/myapp/prod/db-credentials\"\n\n# Spring Boot reads the JSON secret and binds properties:\n# {\"username\":\"app\",\"password\":\"secret\"} → spring.datasource.username/password</pre><br><strong>Never:</strong> store secrets in environment variables in code, git, or Docker image layers."},

  {q:"What is AWS CloudFront and how does it improve application performance?",d:"medium",
   a:"CloudFront is AWS's Content Delivery Network (CDN) — a global network of 400+ edge locations that cache your content close to users.<br><br><strong>How it works:</strong><ol><li>User requests <code>https://cdn.myapp.com/logo.png</code></li><li>DNS resolves to the nearest CloudFront edge location (e.g., Mumbai for Indian users)</li><li>Edge checks cache: HIT → return cached response (sub-10ms)</li><li>Cache MISS → edge fetches from origin (S3, ALB, EC2) → caches → returns</li></ol><br><strong>What to cache:</strong><ul><li>Static assets: JS, CSS, images, fonts (TTL: days/weeks)</li><li>API responses that are same for all users (product catalogue, TTL: minutes)</li><li>HTML pages for SSR apps</li></ul><br><strong>Spring Boot + S3 + CloudFront pattern:</strong><br><pre># Store assets in S3 (private)\n# CloudFront origin: S3 bucket\n# Serve assets via: https://d1234.cloudfront.net/assets/app.js\n# Set Cache-Control: max-age=31536000 for versioned assets</pre><br><strong>Other CloudFront features:</strong> Lambda@Edge (run code at edge), AWS WAF integration (block attacks at edge), SSL/TLS termination, geo-restriction, signed URLs for private content."},

  {q:"What is AWS EventBridge and how does it differ from SNS and SQS?",d:"medium",
   a:"<strong>EventBridge</strong> is a serverless event bus for routing events between AWS services, SaaS apps, and your applications using a flexible rule-based routing system.<br><br><strong>Key difference — content-based routing:</strong><br><pre>// EventBridge rule — route based on event content:\n{\n  \"source\": [\"com.myapp.orders\"],\n  \"detail-type\": [\"OrderPlaced\"],\n  \"detail\": {\n    \"amount\": [{\"numeric\": [\">\", 1000]}],  // route high-value orders\n    \"region\": [\"US\", \"EU\"]\n  }\n}</pre><br><strong>Comparison:</strong><table><tr><td></td><td>SNS</td><td>SQS</td><td>EventBridge</td></tr><tr><td>Routing</td><td>Topic/attr filter</td><td>None</td><td>Content-based rules</td></tr><tr><td>Targets</td><td>Limited</td><td>Consumer only</td><td>20+ AWS services</td></tr><tr><td>SaaS integration</td><td>❌</td><td>❌</td><td>✅ Salesforce, Zendesk...</td></tr><tr><td>Schema registry</td><td>❌</td><td>❌</td><td>✅</td></tr></table><br><strong>Use EventBridge for:</strong> complex event routing between services, SaaS integrations, scheduled events (cron), audit event archiving. <strong>Use SQS</strong> for reliable task queuing. <strong>Use SNS</strong> for simple fan-out pub/sub."}
);

SEC_AWS.qs.push(
  {q:"What is the difference between RDS Multi-AZ and Read Replicas?",d:"medium",
   a:"<strong>RDS Multi-AZ:</strong> synchronous replication to a standby instance in a different Availability Zone. Purpose: <strong>high availability / failover</strong>.<ul><li>Standby is NOT accessible for reads — it only exists for failover</li><li>Failover is automatic (~60-120s): DNS endpoint is updated to point to the standby</li><li>Protects against: AZ outage, instance failure, OS patching</li><li>~2× cost but zero data loss (synchronous)</li></ul><br><strong>Read Replicas:</strong> asynchronous replication to one or more read-only instances. Purpose: <strong>read scaling</strong>.<ul><li>Replicas can be in the same AZ, different AZ, or different region</li><li>Application must explicitly route read queries to replica endpoints</li><li>Slight replication lag (usually &lt;1s) — reads may be slightly stale</li><li>Can be promoted to standalone DB (useful for DR or migration)</li><li>Up to 5 replicas for MySQL/PostgreSQL</li></ul><br><strong>Use both together:</strong> Multi-AZ for HA + Read Replica for read scaling. The standby in Multi-AZ can itself have a read replica.<br><br><strong>Aurora:</strong> combines both — up to 15 low-latency read replicas, automatic failover to a replica in &lt;30s."},

  {q:"What is AWS Secrets Manager and how does Spring Boot integrate with it?",d:"medium",
   a:"Secrets Manager securely stores secrets (DB passwords, API keys, JWT secrets) and rotates them automatically. Eliminates hardcoded credentials in code or environment variables.<br><br><strong>Spring Cloud AWS integration:</strong><br><pre>&lt;dependency&gt;\n  &lt;groupId&gt;io.awspring.cloud&lt;/groupId&gt;\n  &lt;artifactId&gt;spring-cloud-aws-secrets-manager-config&lt;/artifactId&gt;\n&lt;/dependency&gt;</pre><br><pre># bootstrap.yml or application.yml\nspring:\n  config:\n    import: \"aws-secretsmanager:/myapp/prod/db-credentials\"\n# Secret JSON: {\"username\": \"admin\", \"password\": \"s3cr3t\"}\n# Bound to: ${username} and ${password} in properties</pre><br><strong>Automatic rotation:</strong><ol><li>Secrets Manager calls a Lambda rotation function</li><li>Lambda creates a new password, updates the DB, updates the secret</li><li>Spring Cloud AWS refreshes the secret on the next property access (with <code>@RefreshScope</code>) or on next app restart</li></ol><br><strong>IAM policy needed:</strong><br><pre>{\n  \"Action\": [\"secretsmanager:GetSecretValue\"],\n  \"Resource\": \"arn:aws:secretsmanager:region:acct:secret:myapp/*\"\n}</pre>Never hardcode credentials. Always use IAM roles + Secrets Manager or Parameter Store."},

  {q:"What is AWS EventBridge and how does it differ from SQS and SNS?",d:"medium",
   a:"EventBridge is a serverless event bus for routing events between AWS services, your own apps, and SaaS applications with rich filtering rules.<br><br><strong>Comparison:</strong><ul><li><strong>SQS:</strong> point-to-point queue, reliable delivery, pull model, great for task queues and work distribution</li><li><strong>SNS:</strong> pub/sub fan-out, push to multiple subscriber types (SQS, Lambda, HTTP), simple routing</li><li><strong>EventBridge:</strong> event bus with content-based routing, event archiving, replay, schema registry, SaaS integrations (Stripe, Salesforce, GitHub)</li></ul><br><strong>EventBridge content-based routing:</strong><br><pre>// Rule: route only high-value orders to fraud detection\n{\n  \"source\": [\"myapp.orders\"],\n  \"detail-type\": [\"OrderPlaced\"],\n  \"detail\": {\n    \"amount\": [{\"numeric\": [\">\", 10000]}]\n  }\n}\n// Target: SQS queue for fraud service</pre><br><strong>Event Archive + Replay:</strong> archive all events for 90 days. Replay to a new consumer (e.g., new microservice needs to process historical events) — powerful for event sourcing catch-up.<br><br><strong>Use EventBridge for:</strong> complex event routing across services, SaaS event integration, event-driven orchestration with filtering. Use SNS for simple fan-out, SQS for reliable work queues."}
);

SEC_AWS.qs.push(
  {q:"What is the difference between RDS Multi-AZ and Read Replicas?",d:"medium",
   a:"<strong>Multi-AZ (High Availability):</strong> RDS maintains a synchronous standby replica in a different Availability Zone. On primary failure, RDS automatically fails over to the standby — typically 1–2 minutes. The standby is NOT accessible for reads — it's purely for failover. Doubles the cost.<br><br><strong>Read Replicas (Read Scaling):</strong> asynchronous copies of the primary. Can be in the same AZ, different AZ, or different region. Accessible for read traffic — offload reporting queries. Can have up to 15 replicas (Aurora). Replication lag is typically &lt;1s but can be higher under heavy write load.<br><br><strong>Can you use Read Replicas for HA?</strong> Yes, but manually — you must promote a read replica to become the new primary (takes ~5 min). Multi-AZ is fully automatic.<br><br><strong>Combined strategy:</strong> use Multi-AZ for HA AND Read Replicas for read scaling — both are independent features.<br><br><strong>In Spring:</strong> configure separate datasources for read and write:<br><pre>@Primary @Bean DataSource writeDataSource() { ... } // Multi-AZ primary\n@Bean DataSource readDataSource() { ... }           // Read replica</pre>Use <code>@Transactional(readOnly=true)</code> routing to automatically direct read transactions to the replica."},

  {q:"What is AWS Secrets Manager and how do you integrate it with Spring Boot?",d:"medium",
   a:"AWS Secrets Manager stores, rotates, and audits secrets (DB passwords, API keys, certificates) — replacing hardcoded credentials in application.yml.<br><br><strong>Store a secret:</strong><br><pre>aws secretsmanager create-secret \\\n  --name /prod/myapp/db \\\n  --secret-string '{\"username\":\"app\",\"password\":\"s3cr3t\"}'</pre><br><strong>Integration with Spring Boot (spring-cloud-aws):</strong><br><pre>&lt;!-- pom.xml --&gt;\n&lt;dependency&gt;\n  &lt;groupId&gt;io.awspring.cloud&lt;/groupId&gt;\n  &lt;artifactId&gt;spring-cloud-aws-secrets-manager-config&lt;/artifactId&gt;\n&lt;/dependency&gt;</pre><br><pre># application.yml\nspring:\n  config:\n    import: \"aws-secretsmanager:/prod/myapp/db\"</pre>Spring fetches the secret at startup and maps JSON keys to properties: <code>username</code> and <code>password</code> become available as <code>${username}</code> and <code>${password}</code>.<br><br><strong>Automatic rotation:</strong> configure a Lambda function to rotate the DB password every 30 days. Spring Cloud AWS supports dynamic refresh — call <code>/actuator/refresh</code> after rotation.<br><br><strong>IAM policy:</strong> grant the EC2/ECS/Lambda IAM role only <code>secretsmanager:GetSecretValue</code> on the specific secret ARN."},

  {q:"What is AWS CloudFront and when would you use it?",d:"medium",
   a:"CloudFront is AWS's CDN (Content Delivery Network) — a globally distributed network of edge locations that cache content close to users, reducing latency and origin load.<br><br><strong>Common use cases:</strong><ul><li><strong>Static assets:</strong> serve CSS/JS/images from S3 via CloudFront. Edge servers cache content globally — users in India get assets from Mumbai instead of us-east-1.</li><li><strong>API acceleration:</strong> CloudFront can proxy API calls, using edge locations to terminate TCP connections closer to users (reduces RTT)</li><li><strong>DDoS protection:</strong> AWS Shield Standard is included. WAF can be attached to CloudFront to filter malicious traffic at the edge.</li><li><strong>HTTPS everywhere:</strong> attach an ACM certificate to CloudFront — free SSL termination.</li></ul><br><strong>Cache behaviour:</strong> configure TTLs per path pattern:<br><pre>Path: /static/*   → TTL: 1 year (immutable assets)\nPath: /api/*      → TTL: 0 (no caching, just acceleration)\nPath: /           → TTL: 5 minutes</pre><br><strong>Cache invalidation:</strong> <code>aws cloudfront create-invalidation --paths \"/static/app.js\"</code> — propagates in ~1 minute globally."},

  {q:"What is the difference between SQS, SNS, and EventBridge?",d:"medium",
   a:"All three are AWS messaging services — each suited for different patterns:<br><br><strong>SQS:</strong> simple queue. Point-to-point. Messages consumed once. At-least-once delivery. Best for task queues, work distribution, decoupling producer/consumer.<br><br><strong>SNS:</strong> pub/sub. Fan-out to multiple subscribers simultaneously (SQS, Lambda, email, SMS, HTTP). Messages not persisted after delivery. Best for broadcasting events to multiple consumers.<br><br><strong>EventBridge:</strong> serverless event bus. Routes events based on content-based rules — filter by event type, source, specific field values.<br><pre>// Only route payment events over $1000 to fraud service\n{\n  \"source\": [\"com.myapp.payments\"],\n  \"detail-type\": [\"PaymentProcessed\"],\n  \"detail\": {\n    \"amount\": [{ \"numeric\": [\"&gt;\", 1000] }]\n  }\n}</pre>EventBridge connects AWS services, third-party SaaS (Shopify, Zendesk), and custom apps. Schema registry for all events. Best for event-driven architectures with complex routing.<br><br><strong>Decision tree:</strong> task queue → SQS. Broadcast → SNS. Content-based routing + AWS integrations → EventBridge."},

  {q:"How does AWS ECS task definition work and how do you inject secrets?",d:"hard",
   a:"<strong>ECS Task Definition</strong> describes how Docker containers should run — like a Kubernetes Pod spec.<br><br><strong>Key components:</strong><br><pre>{\n  \"family\": \"order-service\",\n  \"taskRoleArn\": \"arn:aws:iam::123:role/OrderServiceTaskRole\",\n  \"executionRoleArn\": \"arn:aws:iam::123:role/EcsTaskExecutionRole\",\n  \"containerDefinitions\": [{\n    \"name\": \"app\",\n    \"image\": \"123.dkr.ecr.us-east-1.amazonaws.com/order-service:v1.2.3\",\n    \"cpu\": 512,\n    \"memory\": 1024,\n    \"portMappings\": [{ \"containerPort\": 8080 }],\n    \"logConfiguration\": {\n      \"logDriver\": \"awslogs\",\n      \"options\": {\n        \"awslogs-group\": \"/ecs/order-service\",\n        \"awslogs-region\": \"us-east-1\"\n      }\n    },\n    \"secrets\": [\n      {\n        \"name\": \"DB_PASSWORD\",\n        \"valueFrom\": \"arn:aws:secretsmanager:us-east-1:123:secret:db-password-abc123\"\n      }\n    ]\n  }]\n}</pre><br><strong>Injecting secrets:</strong><ul><li><strong>Secrets Manager:</strong> <code>valueFrom</code> references secret ARN — ECS retrieves and injects as env var at runtime</li><li><strong>Systems Manager Parameter Store:</strong> <code>valueFrom: arn:aws:ssm:region:account:parameter/db/password</code></li></ul><br><strong>Task Role vs Execution Role:</strong><ul><li><strong>executionRoleArn:</strong> permissions ECS needs to pull image from ECR, fetch secrets, write logs</li><li><strong>taskRoleArn:</strong> permissions the application needs (S3 access, DynamoDB, etc.)</li></ul>"},

  {q:"What is AWS X-Ray and how do you trace requests across Lambda/ECS?",d:"medium",
   a:"<strong>AWS X-Ray</strong> provides distributed tracing — visualize request flow across services (Lambda, ECS, API Gateway, DynamoDB) and identify bottlenecks.<br><br><strong>How it works:</strong> each service adds trace segments to X-Ray. A correlation ID (<code>X-Amzn-Trace-Id</code> header) ties segments together into a single trace.<br><br><strong>Enable X-Ray in Spring Boot on ECS:</strong><br><pre>&lt;dependency&gt;\n  &lt;groupId&gt;com.amazonaws&lt;/groupId&gt;\n  &lt;artifactId&gt;aws-xray-recorder-sdk-spring&lt;/artifactId&gt;\n&lt;/dependency&gt;\n\n@Configuration\n@EnableXRay\npublic class XRayConfig { }</pre><br>Spring Boot auto-instruments HTTP requests, JDBC, AWS SDK calls.<br><br><strong>Enable X-Ray in Lambda:</strong><br><pre># SAM template\nOrderFunction:\n  Type: AWS::Serverless::Function\n  Properties:\n    Tracing: Active  # enables X-Ray</pre><br>Lambda runtime automatically creates trace segments. Use X-Ray SDK to add custom subsegments:<br><pre>Subsegment subsegment = AWSXRay.beginSubsegment(\"DynamoDB Query\");\ntry {\n  dynamoDB.getItem(...);\n} finally {\n  AWSXRay.endSubsegment();\n}</pre><br><strong>View traces:</strong> X-Ray console shows service map (which services call which) and trace timeline (where time is spent). Identify slow DB queries, high-latency downstream calls."},

  {q:"What are S3 storage classes and lifecycle policies?",d:"medium",
   a:"S3 has multiple <strong>storage classes</strong> with different cost/performance trade-offs — optimize costs by moving infrequently accessed data to cheaper tiers.<br><br><strong>Storage classes:</strong><br><ul><li><strong>S3 Standard:</strong> hot data, frequently accessed. ~$0.023/GB/month. Millisecond latency.</li><li><strong>S3 Intelligent-Tiering:</strong> automatically moves objects between tiers based on access patterns. Small monitoring fee.</li><li><strong>S3 Standard-IA (Infrequent Access):</strong> accessed &lt;1/month. ~$0.0125/GB/month. Retrieval fee per GB.</li><li><strong>S3 One Zone-IA:</strong> like Standard-IA but single AZ (less durable). ~$0.01/GB/month.</li><li><strong>S3 Glacier Instant Retrieval:</strong> archive, millisecond retrieval. ~$0.004/GB/month.</li><li><strong>S3 Glacier Flexible Retrieval:</strong> archive, retrieval in minutes-hours. ~$0.0036/GB/month.</li><li><strong>S3 Glacier Deep Archive:</strong> long-term archive, retrieval in 12 hours. ~$0.00099/GB/month (cheapest).</li></ul><br><strong>Lifecycle policy — auto-transition after 30/90/180 days:</strong><br><pre>{\n  \"Rules\": [{\n    \"Id\": \"archive-old-logs\",\n    \"Filter\": { \"Prefix\": \"logs/\" },\n    \"Status\": \"Enabled\",\n    \"Transitions\": [\n      { \"Days\": 30, \"StorageClass\": \"STANDARD_IA\" },\n      { \"Days\": 90, \"StorageClass\": \"GLACIER\" }\n    ],\n    \"Expiration\": { \"Days\": 365 }  # delete after 1 year\n  }]\n}</pre>"},

  {q:"When do you choose EKS over ECS?",d:"medium",
   a:"<strong>ECS (Elastic Container Service):</strong> AWS-native container orchestrator. Simpler, tighter AWS integration, lower operational overhead.<br><br><strong>EKS (Elastic Kubernetes Service):</strong> managed Kubernetes. Portable across clouds, richer ecosystem, steeper learning curve.<br><br><strong>Choose ECS when:</strong><ul><li>You're all-in on AWS (no multi-cloud plans)</li><li>You want simpler ops (no Kubernetes YAML complexity)</li><li>Tight AWS integration is a priority (IAM roles, ALB, CloudWatch)</li><li>Team is small and doesn't know Kubernetes</li><li>Fargate support (serverless containers) is sufficient</li></ul><br><strong>Choose EKS when:</strong><ul><li>You need Kubernetes-native tooling (Helm, Operators, StatefulSets)</li><li>Multi-cloud portability matters (avoid vendor lock-in)</li><li>Team already knows Kubernetes</li><li>You need advanced features (custom schedulers, service mesh, GitOps with ArgoCD/Flux)</li><li>Running complex stateful workloads (databases, message queues)</li></ul><br><strong>Cost:</strong> EKS control plane costs $0.10/hour (~$73/month). ECS control plane is free. Both charge for compute (EC2 or Fargate).<br><br><strong>Recommendation:</strong> start with ECS unless you have a specific Kubernetes requirement or multi-cloud strategy."},

  {q:"What is the difference between IAM Role, Instance Profile, and IAM User for apps?",d:"medium",
   a:"<strong>IAM User:</strong> a permanent identity with long-lived credentials (access key + secret). <strong>Never use for applications</strong> — credentials can leak, no automatic rotation, violates least-privilege (same creds everywhere).<br><br><strong>IAM Role:</strong> a set of permissions that can be assumed. No long-lived credentials — temporary session tokens issued via STS (Security Token Service). Expires after 1-12 hours.<br><br><strong>Instance Profile:</strong> attaches an IAM Role to an EC2 instance. The instance can assume the role and retrieve temporary credentials from the metadata service.<br><br><strong>How it works:</strong><br><pre># Attach role to EC2 instance via instance profile\naws ec2 run-instances --iam-instance-profile Name=MyAppInstanceProfile ...\n\n# Inside the EC2 instance, the AWS SDK automatically:\n# 1. Retrieves credentials from http://169.254.169.254/latest/meta-data/iam/security-credentials/MyRole\n# 2. Refreshes them before expiry</pre><br><strong>For ECS/Lambda:</strong> directly attach <code>taskRoleArn</code> (ECS) or <code>Role</code> (Lambda) — no instance profile needed.<br><br><strong>Best practice:</strong> always use roles + temporary credentials. Never hardcode access keys in code or environment variables. Use IAM Roles Anywhere for on-prem workloads."}
);