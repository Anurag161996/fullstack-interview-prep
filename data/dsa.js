const SEC_DSA = {
  id:"dsa",title:"DSA for Backend Interviews",icon:"📊",ib:"rgba(56,132,255,.12)",ic:"#38bdf8",
  grp:"java",bar:"#38bdf8",tglow:"rgba(56,189,248,.07)",
  desc:"Time/space complexity, collections internals, sorting, trees, graphs, and dynamic programming.",
  tags:["Big-O","HashMap internals","BFS/DFS","Sorting","Dynamic programming"],
  qs:[
    {q:"What is the time and space complexity of common Java collections?",d:"medium",
     a:"<strong>ArrayList:</strong> get O(1), add-end O(1) amortised (occasional resize), insert-mid O(n), contains O(n). Space O(n).<br><br><strong>LinkedList:</strong> get O(n), add-head/tail O(1), add-mid O(1) if you have the node. Space O(n) + pointer overhead.<br><br><strong>HashMap / HashSet:</strong> get/put/contains O(1) average, O(n) worst case (all keys in same bucket). Space O(n).<br><br><strong>TreeMap / TreeSet:</strong> get/put/contains O(log n). Keys always sorted. Space O(n).<br><br><strong>PriorityQueue:</strong> offer/poll O(log n), peek O(1). Not sorted — only top element guaranteed. Space O(n).<br><br><strong>ArrayDeque:</strong> addFirst/addLast/pollFirst/pollLast O(1) amortised. Prefer over Stack and LinkedList for queue/stack operations — more memory-efficient.<br><br><strong>ConcurrentHashMap:</strong> same O(1) avg as HashMap but thread-safe. No null keys/values."},

    {q:"What is the difference between BFS and DFS and when do you use each?",d:"medium",
     a:"<strong>BFS (Breadth-First Search):</strong> explores level by level using a queue. Visits all nodes at depth d before depth d+1.<br><ul><li>Shortest path in unweighted graph</li><li>Level-order traversal of a tree</li><li>Finding all nodes within distance K</li><li>Connected components</li></ul><br><strong>DFS (Depth-First Search):</strong> explores as deep as possible using a stack (or recursion). Backtracks when it hits a dead end.<br><ul><li>Cycle detection in directed/undirected graphs</li><li>Topological sort (for build dependencies, task scheduling)</li><li>All paths between two nodes</li><li>Tree traversals (preorder, inorder, postorder)</li><li>Island counting (flood fill)</li></ul><br><strong>Memory comparison:</strong> BFS uses O(width) queue space (can be large for wide graphs). DFS uses O(height) stack space (can be large for deep graphs).<br><br><strong>Quick rule:</strong> shortest path → BFS. Exploring all possibilities / cycle detection → DFS."},

    {q:"What is dynamic programming and how do you approach DP problems?",d:"hard",
     a:"DP solves problems with <strong>overlapping subproblems</strong> and <strong>optimal substructure</strong> by caching (memoising) subproblem results instead of recomputing them.<br><br><strong>How to identify DP problems:</strong><ul><li>\"Find the maximum/minimum...\"</li><li>\"Count the number of ways...\"</li><li>\"Is it possible to...\" (boolean)</li><li>Choices at each step + optimal combination</li></ul><br><strong>Top-down (Memoisation):</strong><br><pre>int[] memo = new int[n + 1];\nArrays.fill(memo, -1);\n\nint climbStairs(int n) {\n  if (n &lt;= 1) return 1;\n  if (memo[n] != -1) return memo[n];\n  return memo[n] = climbStairs(n-1) + climbStairs(n-2);\n}</pre><br><strong>Bottom-up (Tabulation):</strong><br><pre>int[] dp = new int[n + 1];\ndp[0] = 1; dp[1] = 1;\nfor (int i = 2; i &lt;= n; i++)\n  dp[i] = dp[i-1] + dp[i-2];</pre><br>Classic problems: Fibonacci, Knapsack, Coin Change, Longest Common Subsequence, Edit Distance, House Robber."},

    {q:"What sorting algorithms are used in Java and when?",d:"hard",
     a:"<strong>Arrays.sort(int[]) — Dual-Pivot Quicksort:</strong> O(n log n) average, O(n²) worst case (rare with dual-pivot). In-place, not stable. Used for primitive arrays — fastest in practice.<br><br><strong>Arrays.sort(Object[]) / Collections.sort() — Timsort:</strong> O(n log n) worst, O(n) best case (nearly sorted). Stable (equal elements maintain original order). Used for object arrays and Collections.<br><br><strong>When stability matters:</strong> sorting a list of students by grade — if two students have the same grade, their original order should be preserved. Timsort guarantees this; quicksort doesn't.<br><br><strong>Manual algorithm complexity:</strong><ul><li>Quicksort: O(n log n) avg, O(n²) worst, O(log n) space</li><li>Mergesort: O(n log n) always, O(n) space, stable</li><li>Heapsort: O(n log n) always, O(1) space, not stable</li><li>Insertion sort: O(n²), O(n) best (sorted input), O(1) space — fast for small/nearly-sorted arrays</li></ul>"}
  ]
};

SEC_DSA.qs.push(
  {q:"How do you detect a cycle in a linked list?",d:"medium",
   a:"<strong>Floyd's Cycle Detection (fast/slow pointer — tortoise and hare):</strong><br><pre>public boolean hasCycle(ListNode head) {\n  ListNode slow = head, fast = head;\n  while (fast != null && fast.next != null) {\n    slow = slow.next;       // move 1 step\n    fast = fast.next.next;  // move 2 steps\n    if (slow == fast) return true; // cycle!\n  }\n  return false; // fast reached end — no cycle\n}</pre><br><strong>Why it works:</strong> if there's a cycle, the fast pointer (moving 2 steps) will eventually catch up to the slow pointer (1 step) inside the cycle. If no cycle, fast reaches null.<br><br><strong>Time:</strong> O(n). <strong>Space:</strong> O(1) — no extra data structures.<br><br><strong>Extension — find cycle start:</strong> after detection, reset slow to head and move both at 1 step. Where they meet = the start of the cycle.<br><br><strong>Related problems:</strong> Find duplicate number in array (Floyd's on index-as-pointer), linked list palindrome check."},

  {q:"How do you find the top-K most frequent elements in an array?",d:"medium",
   a:"<strong>Approach 1 — Min Heap (O(n log k)):</strong><br><pre>public int[] topKFrequent(int[] nums, int k) {\n  // Count frequencies\n  Map&lt;Integer, Integer&gt; freq = new HashMap&lt;&gt;();\n  for (int n : nums) freq.merge(n, 1, Integer::sum);\n\n  // Min heap of size k — least frequent at top\n  PriorityQueue&lt;Integer&gt; heap = new PriorityQueue&lt;&gt;(\n    Comparator.comparingInt(freq::get));\n\n  for (int num : freq.keySet()) {\n    heap.offer(num);\n    if (heap.size() &gt; k) heap.poll(); // remove least frequent\n  }\n\n  int[] result = new int[k];\n  for (int i = k - 1; i &gt;= 0; i--) result[i] = heap.poll();\n  return result;\n}</pre><br><strong>Approach 2 — Bucket Sort (O(n)):</strong> create buckets indexed by frequency (0 to n). Place each number in its frequency bucket. Read from high to low until k elements collected.<br><br><strong>Time comparison:</strong> heap O(n log k), bucket O(n). Use bucket sort when k is close to n; heap when k is small."}
);

SEC_DSA.qs.push(
  {q:"What is the two-pointer technique and when do you use it?",d:"medium",
   a:"Two pointers use two indices moving through a data structure — often in opposite directions or at different speeds — to solve problems in O(n) instead of O(n²).<br><br><strong>Pattern 1 — opposite ends (sorted array):</strong><br><pre>// Two Sum II — find pair summing to target in sorted array\npublic int[] twoSum(int[] nums, int target) {\n  int left = 0, right = nums.length - 1;\n  while (left &lt; right) {\n    int sum = nums[left] + nums[right];\n    if (sum == target) return new int[]{left+1, right+1};\n    else if (sum &lt; target) left++;   // need bigger sum\n    else right--;                     // need smaller sum\n  }\n  return new int[]{};\n}</pre><br><strong>Pattern 2 — fast/slow pointer (cycle detection, middle of list):</strong><br><pre>// Find middle of linked list\nListNode slow = head, fast = head;\nwhile (fast != null && fast.next != null) {\n  slow = slow.next;\n  fast = fast.next.next;\n}\n// slow is at the middle</pre><br><strong>Use cases:</strong> remove duplicates from sorted array, container with most water, trapping rain water, palindrome check, linked list cycle, three-sum (two pointers inside a loop)."},

  {q:"What is the sliding window technique?",d:"medium",
   a:"Sliding window maintains a contiguous subarray/substring of variable or fixed size by sliding a window across the data — O(n) instead of O(n²) for subarray problems.<br><br><strong>Fixed-size window — max sum of k elements:</strong><br><pre>public int maxSumSubarray(int[] arr, int k) {\n  int windowSum = 0;\n  for (int i = 0; i &lt; k; i++) windowSum += arr[i]; // initial window\n\n  int maxSum = windowSum;\n  for (int i = k; i &lt; arr.length; i++) {\n    windowSum += arr[i] - arr[i-k]; // slide: add new, remove old\n    maxSum = Math.max(maxSum, windowSum);\n  }\n  return maxSum;\n}</pre><br><strong>Variable-size window — longest substring without repeating characters:</strong><br><pre>public int lengthOfLongestSubstring(String s) {\n  Map&lt;Character, Integer&gt; seen = new HashMap&lt;&gt;();\n  int max = 0, left = 0;\n  for (int right = 0; right &lt; s.length(); right++) {\n    char c = s.charAt(right);\n    if (seen.containsKey(c)) {\n      left = Math.max(left, seen.get(c) + 1); // shrink from left\n    }\n    seen.put(c, right);\n    max = Math.max(max, right - left + 1);\n  }\n  return max;\n}</pre><br><strong>Key insight:</strong> expand the right boundary to grow the window; shrink the left boundary when the constraint is violated."},

  {q:"How do you implement an LRU Cache?",d:"hard",
   a:"LRU (Least Recently Used) cache evicts the least recently accessed entry when the cache is full. Requires O(1) get and O(1) put.<br><br><strong>Data structure:</strong> HashMap + Doubly Linked List<ul><li>HashMap for O(1) key lookup → node reference</li><li>Doubly Linked List to maintain access order — most recent at head, LRU at tail</li></ul><br><pre>class LRUCache {\n  private final int capacity;\n  private final Map&lt;Integer, Node&gt; map = new HashMap&lt;&gt;();\n  private final Node head = new Node(), tail = new Node();\n\n  LRUCache(int capacity) {\n    this.capacity = capacity;\n    head.next = tail; tail.prev = head;\n  }\n\n  int get(int key) {\n    if (!map.containsKey(key)) return -1;\n    Node node = map.get(key);\n    moveToHead(node); // mark as recently used\n    return node.val;\n  }\n\n  void put(int key, int val) {\n    if (map.containsKey(key)) {\n      Node node = map.get(key);\n      node.val = val;\n      moveToHead(node);\n    } else {\n      Node node = new Node(key, val);\n      map.put(key, node);\n      addToHead(node);\n      if (map.size() &gt; capacity) {\n        Node lru = removeTail();\n        map.remove(lru.key);\n      }\n    }\n  }\n}</pre><br><strong>Java built-in shortcut:</strong> <code>LinkedHashMap</code> with access-order and <code>removeEldestEntry()</code> override — same behaviour in 5 lines."},

  {q:"What is binary search and what are the common edge case patterns?",d:"medium",
   a:"Binary search finds a target in a sorted array in O(log n) by halving the search space each iteration.<br><br><strong>Standard implementation:</strong><br><pre>int binarySearch(int[] arr, int target) {\n  int lo = 0, hi = arr.length - 1;\n  while (lo &lt;= hi) {\n    int mid = lo + (hi - lo) / 2; // avoids integer overflow vs (lo+hi)/2\n    if (arr[mid] == target) return mid;\n    else if (arr[mid] &lt; target) lo = mid + 1;\n    else hi = mid - 1;\n  }\n  return -1;\n}</pre><br><strong>Find first/last position of target (boundary binary search):</strong><br><pre>// Find leftmost (first) occurrence:\nint lo = 0, hi = arr.length;\nwhile (lo &lt; hi) {\n  int mid = lo + (hi - lo) / 2;\n  if (arr[mid] &lt; target) lo = mid + 1;\n  else hi = mid;  // don't exclude mid — might be the answer\n}\n// lo is the first occurrence (or arr.length if not found)</pre><br><strong>Binary search on the answer</strong> (not on the array):<br><pre>// Find minimum speed such that all bananas eaten in H hours\nint lo = 1, hi = maxPile;\nwhile (lo &lt; hi) {\n  int mid = lo + (hi - lo) / 2;\n  if (canFinish(piles, mid, H)) hi = mid;\n  else lo = mid + 1;\n}\n</pre><br>Common uses: search insert position, find peak element, rotated sorted array, capacity problems."},

  {q:"How do you find the shortest path in a weighted graph — Dijkstra's algorithm?",d:"hard",
   a:"Dijkstra's algorithm finds the shortest path from a source node to all other nodes in a weighted graph with non-negative edge weights. Time: O((V + E) log V) with a priority queue.<br><br><pre>int[] dijkstra(int[][] graph, int src) {\n  int V = graph.length;\n  int[] dist = new int[V];\n  Arrays.fill(dist, Integer.MAX_VALUE);\n  dist[src] = 0;\n\n  // Min-heap: [distance, node]\n  PriorityQueue&lt;int[]&gt; pq = new PriorityQueue&lt;&gt;(Comparator.comparingInt(a -&gt; a[0]));\n  pq.offer(new int[]{0, src});\n\n  while (!pq.isEmpty()) {\n    int[] curr = pq.poll();\n    int d = curr[0], u = curr[1];\n\n    if (d &gt; dist[u]) continue; // stale entry\n\n    for (int v = 0; v &lt; V; v++) {\n      if (graph[u][v] != 0) { // edge exists\n        int newDist = dist[u] + graph[u][v];\n        if (newDist &lt; dist[v]) {\n          dist[v] = newDist;\n          pq.offer(new int[]{newDist, v});\n        }\n      }\n    }\n  }\n  return dist;\n}</pre><br><strong>Limitations:</strong> doesn't work with negative weights (use Bellman-Ford). For unweighted graphs, BFS gives shortest path in O(V+E).<br><br><strong>Backend use:</strong> network routing, GPS shortest path, dependency resolution order."}
);

SEC_DSA.qs.push(
  {q:"What is the two-pointer technique and what problems does it solve?",d:"medium",
   a:"The two-pointer technique uses two index pointers that traverse an array (or string) simultaneously — typically from opposite ends or at different speeds — reducing O(n²) brute-force to O(n).<br><br><strong>Pattern 1 — Opposite ends (sorted array):</strong><br><pre>// Two Sum II: find pair that sums to target in sorted array\nint left = 0, right = arr.length - 1;\nwhile (left &lt; right) {\n  int sum = arr[left] + arr[right];\n  if (sum == target) return new int[]{left, right};\n  else if (sum &lt; target) left++;\n  else right--;\n}\n</pre><br><strong>Pattern 2 — Fast/slow (linked list cycle detection, above).</strong><br><br><strong>Pattern 3 — Same direction (remove duplicates):</strong><br><pre>// Remove duplicates from sorted array in-place:\nint slow = 0;\nfor (int fast = 1; fast &lt; arr.length; fast++) {\n  if (arr[fast] != arr[slow]) {\n    arr[++slow] = arr[fast];\n  }\n}\nreturn slow + 1; // new length</pre><br><strong>Classic problems:</strong> Two Sum (sorted), Three Sum, Container With Most Water, Trapping Rain Water, Palindrome check, Valid Parentheses, Remove Nth node from end of list."},

  {q:"What is the sliding window technique?",d:"medium",
   a:"Sliding window maintains a dynamic window over a sequence, expanding/shrinking it to find an optimal subarray or substring — avoiding the O(n²) of nested loops.<br><br><strong>Fixed-size window</strong> (sum of k elements):<br><pre>int maxSum = 0, windowSum = 0;\nfor (int i = 0; i &lt; k; i++) windowSum += arr[i];\nmaxSum = windowSum;\nfor (int i = k; i &lt; arr.length; i++) {\n  windowSum += arr[i] - arr[i - k]; // slide: add new, remove old\n  maxSum = Math.max(maxSum, windowSum);\n}</pre><br><strong>Variable-size window</strong> (longest substring with at most K distinct chars):<br><pre>Map&lt;Character, Integer&gt; freq = new HashMap&lt;&gt;();\nint left = 0, maxLen = 0;\nfor (int right = 0; right &lt; s.length(); right++) {\n  freq.merge(s.charAt(right), 1, Integer::sum); // expand right\n  while (freq.size() &gt; k) { // shrink if invalid\n    freq.merge(s.charAt(left), -1, Integer::sum);\n    if (freq.get(s.charAt(left)) == 0) freq.remove(s.charAt(left));\n    left++;\n  }\n  maxLen = Math.max(maxLen, right - left + 1);\n}</pre><br><strong>Classic problems:</strong> Max sum subarray of size k, Longest substring without repeating characters, Minimum window substring, Fruit into baskets."},

  {q:"How do you implement an LRU cache in Java?",d:"hard",
   a:"LRU (Least Recently Used) cache evicts the least recently accessed entry when capacity is full. O(1) get and put operations required.<br><br><strong>Approach: LinkedHashMap (built-in):</strong><br><pre>class LRUCache&lt;K, V&gt; extends LinkedHashMap&lt;K, V&gt; {\n  private final int capacity;\n\n  LRUCache(int capacity) {\n    super(capacity, 0.75f, true); // accessOrder=true\n    this.capacity = capacity;\n  }\n\n  @Override\n  protected boolean removeEldestEntry(Map.Entry&lt;K,V&gt; eldest) {\n    return size() &gt; capacity; // auto-evict oldest\n  }\n}\n\n// Usage:\nLRUCache&lt;Integer, String&gt; cache = new LRUCache&lt;&gt;(100);\ncache.put(1, \"one\");\ncache.get(1);    // moves to most-recently-used end\ncache.put(101, \"x\"); // evicts least recently used</pre><br><strong>Manual approach (HashMap + doubly linked list) — for interviews:</strong><ul><li>HashMap for O(1) key lookup → node reference</li><li>Doubly linked list for O(1) move-to-front and remove-from-tail</li><li>get: lookup in map, move node to head → O(1)</li><li>put: add node at head; if size &gt; capacity, remove tail node + map entry → O(1)</li></ul><br>This is LeetCode #146 — extremely common interview question."},

  {q:"What is a trie (prefix tree) and when do you use it?",d:"hard",
   a:"A trie is a tree data structure where each node represents a character, and paths from root to nodes represent prefixes. All children of a node share a common prefix.<br><br><strong>Implementation:</strong><br><pre>class TrieNode {\n  Map&lt;Character, TrieNode&gt; children = new HashMap&lt;&gt;();\n  boolean isEndOfWord = false;\n}\n\nclass Trie {\n  TrieNode root = new TrieNode();\n\n  void insert(String word) {\n    TrieNode node = root;\n    for (char c : word.toCharArray()) {\n      node.children.putIfAbsent(c, new TrieNode());\n      node = node.children.get(c);\n    }\n    node.isEndOfWord = true;\n  }\n\n  boolean search(String word) {\n    TrieNode node = root;\n    for (char c : word.toCharArray()) {\n      if (!node.children.containsKey(c)) return false;\n      node = node.children.get(c);\n    }\n    return node.isEndOfWord;\n  }\n\n  boolean startsWith(String prefix) {\n    TrieNode node = root;\n    for (char c : prefix.toCharArray()) {\n      if (!node.children.containsKey(c)) return false;\n      node = node.children.get(c);\n    }\n    return true; // prefix exists\n  }\n}</pre><br><strong>Use cases:</strong> autocomplete (return all words with prefix), spell checker, IP routing tables, word search, phone book prefix lookup. O(L) operations where L = word length."}
);

SEC_DSA.qs.push(
  {q:"What is the two-pointer technique and what problems does it solve?",d:"medium",
   a:"Two pointers use two indices simultaneously to reduce O(n²) brute-force solutions to O(n) — one pointer moves forward, the other inward/backward.<br><br><strong>Pattern 1 — Opposite ends (sorted array):</strong><br><pre>// Two Sum in sorted array\nint[] twoSum(int[] arr, int target) {\n  int left = 0, right = arr.length - 1;\n  while (left &lt; right) {\n    int sum = arr[left] + arr[right];\n    if (sum == target) return new int[]{left, right};\n    else if (sum &lt; target) left++;   // need bigger\n    else right--;                    // need smaller\n  }\n  return new int[]{-1, -1};\n}</pre><br><strong>Pattern 2 — Fast/Slow (same direction):</strong><br><pre>// Remove duplicates from sorted array in-place\nint removeDuplicates(int[] nums) {\n  int slow = 0;\n  for (int fast = 1; fast &lt; nums.length; fast++) {\n    if (nums[fast] != nums[slow]) {\n      nums[++slow] = nums[fast];\n    }\n  }\n  return slow + 1;\n}</pre><br><strong>Classic problems using two pointers:</strong> container with most water, 3Sum, palindrome check, merge sorted arrays, trapping rain water, linked list cycle detection (Floyd's)."},

  {q:"What is the sliding window technique?",d:"medium",
   a:"Sliding window efficiently processes a subset (window) of a sequence by expanding/contracting the window rather than recomputing from scratch — turns O(n²) into O(n).<br><br><strong>Fixed-size window — max sum subarray of size k:</strong><br><pre>int maxSum(int[] arr, int k) {\n  int windowSum = 0;\n  for (int i = 0; i &lt; k; i++) windowSum += arr[i]; // init window\n\n  int maxSum = windowSum;\n  for (int i = k; i &lt; arr.length; i++) {\n    windowSum += arr[i] - arr[i - k]; // slide: add new, remove old\n    maxSum = Math.max(maxSum, windowSum);\n  }\n  return maxSum;\n}</pre><br><strong>Variable-size window — longest substring without repeating chars:</strong><br><pre>int lengthOfLongestSubstring(String s) {\n  Map&lt;Character, Integer&gt; seen = new HashMap&lt;&gt;();\n  int maxLen = 0, left = 0;\n  for (int right = 0; right &lt; s.length(); right++) {\n    char c = s.charAt(right);\n    if (seen.containsKey(c) && seen.get(c) &gt;= left)\n      left = seen.get(c) + 1; // shrink window\n    seen.put(c, right);\n    maxLen = Math.max(maxLen, right - left + 1);\n  }\n  return maxLen;\n}</pre><br><strong>Classic problems:</strong> minimum window substring, max fruits in baskets, longest subarray with sum ≤ k, count anagrams in string."},

  {q:"How do you implement an LRU cache?",d:"hard",
   a:"LRU (Least Recently Used) cache evicts the least recently accessed item when capacity is full. Required operations: O(1) get and O(1) put.<br><br><strong>Approach — HashMap + Doubly Linked List:</strong><ul><li>HashMap: key → node (O(1) lookup)</li><li>Doubly linked list: maintains access order (head = most recent, tail = least recent)</li><li>On get: move the node to head</li><li>On put: add to head; if capacity exceeded, remove tail</li></ul><br><pre>class LRUCache {\n  private final int capacity;\n  private final Map&lt;Integer, Node&gt; map = new HashMap&lt;&gt;();\n  private final Node head = new Node(0,0), tail = new Node(0,0);\n  // head &lt;-&gt; [most recent] &lt;-&gt; ... &lt;-&gt; [least recent] &lt;-&gt; tail\n\n  LRUCache(int capacity) {\n    this.capacity = capacity;\n    head.next = tail; tail.prev = head;\n  }\n\n  int get(int key) {\n    if (!map.containsKey(key)) return -1;\n    Node node = map.get(key);\n    moveToHead(node);\n    return node.val;\n  }\n\n  void put(int key, int val) {\n    if (map.containsKey(key)) {\n      Node node = map.get(key);\n      node.val = val;\n      moveToHead(node);\n    } else {\n      Node node = new Node(key, val);\n      map.put(key, node);\n      addToHead(node);\n      if (map.size() &gt; capacity) {\n        Node removed = removeTail();\n        map.remove(removed.key);\n      }\n    }\n  }\n}</pre><br><strong>In Java:</strong> <code>LinkedHashMap</code> with <code>accessOrder=true</code> is a built-in LRU — override <code>removeEldestEntry()</code>."},

  {q:"What is a Trie (prefix tree) and how do you implement one?",d:"medium",
   a:"A Trie (pronounced \"try\") is a tree data structure for storing strings where each node represents a character. Efficient for prefix-based operations — autocomplete, spell checker, IP routing.<br><br><strong>Structure:</strong> root is empty, each path from root to leaf represents a word. Nodes store children (Map&lt;Character, TrieNode&gt;) and a boolean <code>isWord</code>.<br><br><strong>Implementation:</strong><br><pre>class TrieNode {\n  Map&lt;Character, TrieNode&gt; children = new HashMap&lt;&gt;();\n  boolean isWord = false;\n}\n\nclass Trie {\n  private TrieNode root = new TrieNode();\n\n  void insert(String word) {\n    TrieNode node = root;\n    for (char c : word.toCharArray()) {\n      node.children.putIfAbsent(c, new TrieNode());\n      node = node.children.get(c);\n    }\n    node.isWord = true;\n  }\n\n  boolean search(String word) {\n    TrieNode node = findNode(word);\n    return node != null && node.isWord;\n  }\n\n  boolean startsWith(String prefix) {\n    return findNode(prefix) != null;\n  }\n\n  private TrieNode findNode(String s) {\n    TrieNode node = root;\n    for (char c : s.toCharArray()) {\n      node = node.children.get(c);\n      if (node == null) return null;\n    }\n    return node;\n  }\n}</pre><br><strong>Time complexity:</strong> insert/search/startsWith = O(m) where m = word length. Space: O(ALPHABET_SIZE × N × M) worst case.<br><br><strong>Use cases:</strong> autocomplete (return all words with prefix), longest common prefix, word break problem."},

  {q:"What is topological sort and what problems does it solve?",d:"medium",
   a:"<strong>Topological sort</strong> orders the vertices of a Directed Acyclic Graph (DAG) such that for every edge u → v, u comes before v in the ordering. Only possible if the graph has no cycles.<br><br><strong>Use cases:</strong><ul><li>Build systems (compile dependencies — A must be built before B)</li><li>Course prerequisites (take CS101 before CS201)</li><li>Task scheduling with dependencies</li></ul><br><strong>Algorithm — Kahn's (BFS-based):</strong><br><pre>List&lt;Integer&gt; topologicalSort(int n, List&lt;int[]&gt; edges) {\n  int[] inDegree = new int[n];\n  Map&lt;Integer, List&lt;Integer&gt;&gt; graph = new HashMap&lt;&gt;();\n  for (int i = 0; i &lt; n; i++) graph.put(i, new ArrayList&lt;&gt;());\n\n  for (int[] edge : edges) {\n    graph.get(edge[0]).add(edge[1]);\n    inDegree[edge[1]]++;\n  }\n\n  Queue&lt;Integer&gt; queue = new LinkedList&lt;&gt;();\n  for (int i = 0; i &lt; n; i++)\n    if (inDegree[i] == 0) queue.offer(i);\n\n  List&lt;Integer&gt; result = new ArrayList&lt;&gt;();\n  while (!queue.isEmpty()) {\n    int node = queue.poll();\n    result.add(node);\n    for (int neighbor : graph.get(node)) {\n      inDegree[neighbor]--;\n      if (inDegree[neighbor] == 0) queue.offer(neighbor);\n    }\n  }\n\n  return result.size() == n ? result : List.of(); // cycle detected if size &lt; n\n}</pre><br><strong>DFS-based alternative:</strong> run DFS, add node to result on backtrack (post-order), then reverse the result."},

  {q:"How do you represent a graph with adjacency list vs adjacency matrix?",d:"easy",
   a:"<strong>Adjacency List:</strong> each node stores a list of its neighbors. Memory-efficient for sparse graphs.<br><pre>// Unweighted graph\nMap&lt;Integer, List&lt;Integer&gt;&gt; graph = new HashMap&lt;&gt;();\ngraph.put(1, List.of(2, 3));\ngraph.put(2, List.of(1, 4));\n\n// Weighted graph\nMap&lt;Integer, List&lt;int[]&gt;&gt; graph = new HashMap&lt;&gt;();\ngraph.put(1, List.of(new int[]{2, 5}, new int[]{3, 10})); // [neighbor, weight]</pre><br><strong>Adjacency Matrix:</strong> 2D array where <code>matrix[i][j] = 1</code> if edge exists (or weight for weighted graphs). Fast edge lookup but space-inefficient for sparse graphs.<br><pre>int[][] matrix = new int[n][n];\nmatrix[1][2] = 5; // edge from 1 to 2 with weight 5\nmatrix[2][1] = 5; // undirected edge</pre><br><strong>Comparison:</strong><br><table><tr><th>Operation</th><th>Adjacency List</th><th>Adjacency Matrix</th></tr><tr><td>Space</td><td>O(V + E)</td><td>O(V²)</td></tr><tr><td>Check edge (u,v)</td><td>O(degree(u))</td><td>O(1)</td></tr><tr><td>Get all neighbors</td><td>O(degree(u))</td><td>O(V)</td></tr></table><br><strong>When to use:</strong> adjacency list for most real-world graphs (social networks, web graphs). Adjacency matrix for dense graphs or when fast edge lookup is critical."},

  {q:"What is the backtracking pattern and how do you solve permutation/combination problems?",d:"medium",
   a:"Backtracking explores all possible solutions by building candidates incrementally and abandoning (backtracking) when a candidate cannot lead to a valid solution.<br><br><strong>Template:</strong><br><pre>void backtrack(List&lt;List&lt;T&gt;&gt; result, List&lt;T&gt; current, ...) {\n  if (isComplete(current)) {\n    result.add(new ArrayList&lt;&gt;(current)); // found a solution\n    return;\n  }\n  for (T candidate : getCandidates()) {\n    if (!isValid(candidate)) continue; // prune\n    current.add(candidate);            // choose\n    backtrack(result, current, ...);   // explore\n    current.remove(current.size() - 1); // unchoose (backtrack)\n  }\n}</pre><br><strong>Example — permutations of [1,2,3]:</strong><br><pre>List&lt;List&lt;Integer&gt;&gt; permute(int[] nums) {\n  List&lt;List&lt;Integer&gt;&gt; result = new ArrayList&lt;&gt;();\n  backtrack(result, new ArrayList&lt;&gt;(), nums, new boolean[nums.length]);\n  return result;\n}\n\nvoid backtrack(List&lt;List&lt;Integer&gt;&gt; result, List&lt;Integer&gt; current,\n               int[] nums, boolean[] used) {\n  if (current.size() == nums.length) {\n    result.add(new ArrayList&lt;&gt;(current));\n    return;\n  }\n  for (int i = 0; i &lt; nums.length; i++) {\n    if (used[i]) continue;\n    current.add(nums[i]);\n    used[i] = true;\n    backtrack(result, current, nums, used);\n    current.remove(current.size() - 1);\n    used[i] = false;\n  }\n}</pre><br><strong>Classic problems:</strong> permutations, combinations, subsets, N-Queens, Sudoku solver, word search, generate parentheses."},

  {q:"What is memoization vs tabulation — when do you choose each?",d:"medium",
   a:"Both are dynamic programming techniques to avoid recomputing overlapping subproblems. The difference is top-down vs bottom-up.<br><br><strong>Memoization (top-down, recursive):</strong> solve the problem recursively, caching results of subproblems in a map/array. Only computes subproblems that are actually needed.<br><pre>// Fibonacci with memoization\nMap&lt;Integer, Long&gt; memo = new HashMap&lt;&gt;();\nlong fib(int n) {\n  if (n &lt;= 1) return n;\n  if (memo.containsKey(n)) return memo.get(n);\n  long result = fib(n - 1) + fib(n - 2);\n  memo.put(n, result);\n  return result;\n}</pre><br><strong>Tabulation (bottom-up, iterative):</strong> solve all subproblems in order, filling a table from base case upward. No recursion, no stack overflow risk.<br><pre>// Fibonacci with tabulation\nlong fib(int n) {\n  if (n &lt;= 1) return n;\n  long[] dp = new long[n + 1];\n  dp[0] = 0; dp[1] = 1;\n  for (int i = 2; i &lt;= n; i++)\n    dp[i] = dp[i - 1] + dp[i - 2];\n  return dp[n];\n}</pre><br><strong>When to use:</strong><ul><li><strong>Memoization:</strong> easier to implement (just add caching to naive recursion), good when not all subproblems are needed</li><li><strong>Tabulation:</strong> faster (no recursion overhead), better space optimization (rolling array), predictable performance</li></ul><br><strong>Recommendation:</strong> start with memoization for interviews (quicker to code). Optimize to tabulation if needed for space/performance."}
);