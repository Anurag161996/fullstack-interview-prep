const SEC_TESTING = {
  id:"testing",title:"Testing (Unit, Integration, E2E)",icon:"🧪",ib:"rgba(74,222,128,.12)",ic:"#4ade80",
  grp:"test",bar:"#4ade80",tglow:"rgba(74,222,128,.07)",
  desc:"JUnit 5, Mockito, Spring Boot test slices, Testcontainers, WireMock, TDD, and async testing.",
  tags:["JUnit 5","Mockito","@WebMvcTest","Testcontainers","WireMock","TDD"],
  qs:[
    {q:"What is the test pyramid and why does it matter?",d:"medium",
     a:"The test pyramid describes the ideal distribution of test types by quantity and speed:<br><br><strong>Unit tests (base — many, milliseconds):</strong> test a single class with all dependencies mocked. Fastest feedback. Catch 80% of bugs. Easy to maintain.<br><br><strong>Integration tests (middle — fewer, seconds):</strong> test how components interact — real DB (via Testcontainers), real HTTP endpoints, real message broker. Catch integration bugs that unit tests miss.<br><br><strong>E2E / UI tests (top — fewest, minutes):</strong> test full user flows through the deployed system. Slowest, most brittle, most expensive to maintain.<br><br><strong>Why the pyramid matters:</strong> if you invert it (many E2E, few unit tests) you get:<ul><li>Slow feedback cycles (20min test suite instead of 2min)</li><li>Flaky tests (E2E tests have many failure modes)</li><li>Expensive maintenance (E2E breaks on any UI/API change)</li></ul><br><strong>Ideal ratio:</strong> ~70% unit, ~20% integration, ~10% E2E. Adjust based on your system's boundaries."},

    {q:"What is the difference between @Mock and @MockBean in Spring Boot testing?",d:"medium",
     a:"<strong>@Mock (Mockito):</strong> creates a plain Mockito mock object. No Spring application context involved. Used with <code>@ExtendWith(MockitoExtension.class)</code>.<br><pre>@ExtendWith(MockitoExtension.class)\nclass OrderServiceTest {\n  @Mock\n  OrderRepository orderRepo;  // pure mock, no Spring\n\n  @InjectMocks\n  OrderService orderService;  // injects mocks into this\n}</pre>Fastest tests — no Spring context startup cost.<br><br><strong>@MockBean (Spring Boot Test):</strong> creates a Mockito mock AND registers it as a bean in the Spring application context — replacing any existing bean of that type.<br><pre>@SpringBootTest\nclass OrderControllerTest {\n  @MockBean\n  OrderService orderService;  // replaces real bean in context\n\n  @Autowired\n  MockMvc mockMvc;\n}</pre>Use when you need the Spring context (full integration test, controller test) but want to stub out a specific dependency.<br><br><strong>Rule:</strong> prefer <code>@Mock</code> for unit tests (fast), use <code>@MockBean</code> only when you need Spring's machinery."},

    {q:"What is the difference between @SpringBootTest, @WebMvcTest, and @DataJpaTest?",d:"medium",
     a:"These are Spring Boot <strong>test slice</strong> annotations — each loads a subset of the application context for focused, faster tests.<br><br><strong>@SpringBootTest:</strong> loads the <em>full</em> application context. Boots everything — all beans, auto-configuration, embedded server (optional). Most comprehensive but slowest (5–30s startup).<br><pre>@SpringBootTest\n@AutoConfigureMockMvc\nclass FullIntegrationTest { ... }</pre><br><strong>@WebMvcTest(OrderController.class):</strong> loads only the web layer — controllers, filters, security, Jackson. No service or repository beans (you must mock them). Fast (~1–2s).<br><pre>@WebMvcTest(OrderController.class)\nclass OrderControllerTest {\n  @MockBean OrderService orderService;\n  @Autowired MockMvc mockMvc;\n}</pre><br><strong>@DataJpaTest:</strong> loads only JPA layer — entities, repositories, embedded DB (H2 by default, or real DB with Testcontainers). No web layer. Tests repository queries in isolation. Fast.<br><pre>@DataJpaTest\nclass OrderRepositoryTest {\n  @Autowired OrderRepository orderRepo;\n}</pre>"},

    {q:"What is Testcontainers and why is it better than H2 for integration tests?",d:"medium",
     a:"<strong>Testcontainers</strong> spins up real Docker containers (PostgreSQL, Redis, Kafka, MongoDB) during your tests and shuts them down after.<br><br><strong>Why better than H2 in-memory DB:</strong><ul><li><strong>Real dialect:</strong> H2 doesn't support PostgreSQL-specific features: <code>jsonb</code> columns, <code>uuid</code> type, <code>RETURNING</code> clause, partial indexes, CTEs with recursive queries</li><li><strong>Schema migration accuracy:</strong> your Flyway/Liquibase scripts run against the real engine</li><li><strong>Constraint behaviour:</strong> some FK and unique constraint behaviours differ between H2 and PostgreSQL</li></ul><br><strong>Setup in Spring Boot 3.1+ (built-in Testcontainers support):</strong><br><pre>@SpringBootTest\n@Testcontainers\nclass OrderRepositoryTest {\n  @Container\n  static PostgreSQLContainer&lt;?&gt; postgres =\n    new PostgreSQLContainer&lt;&gt;(\"postgres:16\");\n\n  @DynamicPropertySource\n  static void props(DynamicPropertyRegistry r) {\n    r.add(\"spring.datasource.url\", postgres::getJdbcUrl);\n    r.add(\"spring.datasource.username\", postgres::getUsername);\n    r.add(\"spring.datasource.password\", postgres::getPassword);\n  }\n}</pre><br>Reuse containers with <code>@Container</code> on a static field to start once per test class."},

    {q:"How do you test asynchronous code — Kafka consumers and @Async methods?",d:"hard",
     a:"<strong>Testing @Async methods with Awaitility:</strong><br><pre>@Autowired NotificationService svc;\n@Autowired NotificationRepository repo;\n\n@Test\nvoid shouldSendNotificationAsync() {\n  svc.sendAsync(\"user-1\", \"Order shipped\");\n\n  await()\n    .atMost(5, SECONDS)\n    .pollInterval(100, MILLISECONDS)\n    .until(() -&gt; repo.countByUserId(\"user-1\") == 1);\n}</pre><br><strong>Testing Kafka consumers with EmbeddedKafka:</strong><br><pre>@SpringBootTest\n@EmbeddedKafka(topics = \"orders\")\nclass OrderConsumerTest {\n  @Autowired KafkaTemplate&lt;String, String&gt; template;\n  CountDownLatch latch = new CountDownLatch(1);\n\n  @Test\n  void shouldProcessOrder() throws Exception {\n    template.send(\"orders\", \"{\\\"id\\\":\\\"1\\\"}\");\n    assertTrue(latch.await(10, SECONDS));\n    // assert side effects\n  }\n}</pre><br><strong>Never use Thread.sleep() for async tests</strong> — brittle (fails on slow CI, wastes time on fast machines). Awaitility is the standard solution."},

    {q:"What is WireMock and how do you use it for external API testing?",d:"medium",
     a:"<strong>WireMock</strong> is an HTTP server stub. Instead of calling a real external API (payment gateway, third-party service), your code calls a local WireMock server that returns pre-configured responses.<br><br><strong>Benefits:</strong><ul><li>Tests run offline — no real API keys needed</li><li>Test error conditions (timeouts, 500s, 429 rate limits) easily</li><li>Deterministic — no network flakiness</li><li>Fast — local HTTP calls</li></ul><br><strong>Spring Boot integration:</strong><br><pre>@SpringBootTest\n@AutoConfigureWireMock(port = 0)  // random port\nclass PaymentClientTest {\n\n  @Autowired PaymentClient paymentClient;\n\n  @Test\n  void shouldRetryOn500() {\n    stubFor(post(urlEqualTo(\"/charge\"))\n      .willReturn(serverError()));\n\n    stubFor(post(urlEqualTo(\"/charge\"))\n      .inScenario(\"retry\").whenScenarioStateIs(\"retry\")\n      .willReturn(okJson(\"{\\\"status\\\":\\\"success\\\"}\")));\n\n    PaymentResult result = paymentClient.charge(order);\n    assertEquals(\"success\", result.getStatus());\n  }\n}</pre>"},

    {q:"What is Test-Driven Development (TDD) and what are its benefits?",d:"medium",
     a:"<strong>TDD cycle: Red → Green → Refactor</strong><ol><li><strong>Red:</strong> write a failing test for the behaviour you want to implement. The code doesn't exist yet — the test fails.</li><li><strong>Green:</strong> write the minimum code necessary to make the test pass. Don't over-engineer.</li><li><strong>Refactor:</strong> clean up the code while keeping all tests green.</li></ol><br><strong>Benefits:</strong><ul><li><strong>Clarifies requirements first:</strong> you must think about what the code should <em>do</em> before thinking about how to do it</li><li><strong>Forces good design:</strong> untestable code (tight coupling, hidden dependencies) is immediately obvious — TDD naturally produces loosely coupled, injectable components</li><li><strong>Regression safety net:</strong> comprehensive test suite catches unintended side effects of future changes</li><li><strong>Living documentation:</strong> tests describe the expected behaviour in executable form</li></ul><br><strong>Practical note:</strong> strict TDD for every line is not always practical. Apply TDD selectively — for complex business logic, critical paths, and bug fixes (write a failing test that reproduces the bug before fixing it)."}
  ]
};

SEC_TESTING.qs.push(
  {q:"How do you test Spring Boot security — authenticated and unauthenticated requests?",d:"medium",
   a:"Spring Security Test provides annotations to set up the security context without going through the full auth flow.<br><br><strong>With MockMvc:</strong><br><pre>@WebMvcTest(OrderController.class)\n@Import(SecurityConfig.class)\nclass OrderControllerSecurityTest {\n\n  @Autowired MockMvc mockMvc;\n\n  @Test\n  void unauthenticated_returns401() throws Exception {\n    mockMvc.perform(get(\"/api/orders\"))\n      .andExpect(status().isUnauthorized());\n  }\n\n  @Test\n  @WithMockUser(roles = \"USER\")\n  void authenticated_returns200() throws Exception {\n    mockMvc.perform(get(\"/api/orders\"))\n      .andExpect(status().isOk());\n  }\n\n  @Test\n  @WithMockUser(roles = \"USER\") // not ADMIN\n  void insufficientRole_returns403() throws Exception {\n    mockMvc.perform(delete(\"/api/orders/123\"))\n      .andExpect(status().isForbidden());\n  }\n}</pre><br><code>@WithMockUser</code> — simple mock principal. <code>@WithUserDetails(\"alice\")</code> — loads real UserDetails from your UserDetailsService. <code>@WithSecurityContext</code> — fully custom security context."},

  {q:"What is mutation testing and how does it measure test quality?",d:"hard",
   a:"Code coverage tells you which lines were <em>executed</em> by tests — not whether your tests would actually detect a bug. A test that runs the code but never asserts anything gives 100% coverage but catches nothing.<br><br><strong>Mutation testing</strong> measures test effectiveness by automatically introducing small bugs (<em>mutations</em>) into your code, then checking if your tests fail:<ul><li>Change <code>a &gt; b</code> to <code>a &gt;= b</code></li><li>Change <code>return true</code> to <code>return false</code></li><li>Delete a method call</li></ul><br><strong>If your tests still pass</strong> when the mutation is introduced → mutation survived → your tests are not strong enough to catch that bug.<br><br><strong>Mutation score:</strong> <code>killed mutations / total mutations × 100%</code>. Above 80% is generally good.<br><br><strong>PIT Mutation Testing</strong> for Java:<br><pre>&lt;!-- pom.xml --&gt;\n&lt;plugin&gt;\n  &lt;groupId&gt;org.pitest&lt;/groupId&gt;\n  &lt;artifactId&gt;pitest-maven&lt;/artifactId&gt;\n&lt;/plugin&gt;\n\nmvn test-compile org.pitest:pitest-maven:mutationCoverage</pre>"}
);

SEC_TESTING.qs.push(
  {q:"What is consumer-driven contract testing with Pact?",d:"hard",
   a:"Contract testing verifies that a service provider (API) satisfies the contracts expected by all its consumers — without requiring both services to be running simultaneously.<br><br><strong>Consumer-driven:</strong> the consumer defines what it needs from the provider, not the other way around.<br><br><strong>Flow with Pact:</strong><ol><li>Consumer writes a Pact test that records interactions (request/expected response)</li><li>Pact generates a contract file (JSON)</li><li>Contract is published to a Pact Broker</li><li>Provider verifies it satisfies all consumer contracts on every build</li></ol><br><strong>Consumer test (order-service consuming user-service):</strong><br><pre>@ExtendWith(PactConsumerTestExt.class)\nclass OrderServicePactTest {\n  @Pact(consumer = \"order-service\", provider = \"user-service\")\n  RequestResponsePact getUserPact(PactDslWithProvider builder) {\n    return builder\n      .given(\"user 1 exists\")\n      .uponReceiving(\"get user by id\")\n      .path(\"/users/1\")\n      .method(\"GET\")\n      .willRespondWith()\n      .status(200)\n      .body(new PactDslJsonBody()\n        .stringType(\"name\", \"Alice\")\n        .stringType(\"email\"))\n      .toPact();\n  }\n}</pre><br><strong>Value:</strong> catches integration breaks before deployment, without E2E tests."},

  {q:"What are test data builders and why are they better than test fixtures?",d:"medium",
   a:"Test fixtures (static setup data) become brittle as your domain evolves — adding a required field to <code>Order</code> breaks every test that creates one.<br><br><strong>Test Data Builder pattern</strong> (using the Builder pattern for test objects):<br><pre>public class OrderTestDataBuilder {\n  private String id = UUID.randomUUID().toString();\n  private String customerId = \"default-customer\";\n  private List&lt;OrderItem&gt; items = List.of(defaultItem());\n  private Status status = Status.PENDING;\n  private Instant createdAt = Instant.now();\n\n  public OrderTestDataBuilder withStatus(Status status) {\n    this.status = status;\n    return this;\n  }\n\n  public OrderTestDataBuilder withCustomer(String customerId) {\n    this.customerId = customerId;\n    return this;\n  }\n\n  public Order build() {\n    return new Order(id, customerId, items, status, createdAt);\n  }\n}\n\n// In tests — only specify what's relevant:\nOrder order = new OrderTestDataBuilder()\n  .withStatus(Status.SHIPPED)\n  .build(); // all other fields have sensible defaults</pre><br><strong>Benefits:</strong> tests express intent clearly; adding required fields only breaks the builder (not 50 tests); each test sets up minimal state for its concern."},

  {q:"How do you load test a Spring Boot application?",d:"medium",
   a:"Load testing verifies your application handles expected traffic and identifies bottlenecks before they hit production.<br><br><strong>Gatling (Scala DSL, great for Java teams):</strong><br><pre>class OrderSimulation extends Simulation {\n  val httpConf = http.baseUrl(\"http://localhost:8080\")\n\n  val scenario = scenario(\"Order Flow\")\n    .exec(http(\"create order\")\n      .post(\"/api/orders\")\n      .body(StringBody(\"\"\"{ \"customerId\": \"cust-1\" }\"\"\"))\n      .check(status.is(201)))\n\n  setUp(\n    scenario.inject(\n      rampUsers(100).during(30.seconds), // ramp to 100 users over 30s\n      constantUsersPerSec(50).during(2.minutes) // hold 50 req/s for 2min\n    )\n  ).protocols(httpConf)\n   .assertions(\n     global.responseTime.p99.lt(500),  // p99 < 500ms\n     global.failedRequests.percent.lt(1) // < 1% errors\n   )\n}</pre><br><strong>k6</strong> (JavaScript) and <strong>JMeter</strong> are alternatives.<br><br><strong>What to measure:</strong> throughput (req/s), latency (p50/p95/p99), error rate, thread pool utilisation, GC pauses, DB connection pool exhaustion.<br><br><strong>Run load tests in a staging environment</strong> that mirrors production resources — CPU, memory, DB instance size."}
);

SEC_TESTING.qs.push(
  {q:"What is consumer-driven contract testing with Pact?",d:"hard",
   a:"Contract testing verifies that two services (consumer and provider) can communicate correctly — without deploying them together in a slow integration test.<br><br><strong>Problem it solves:</strong> Service A (consumer) calls Service B (provider). If B changes its API, A breaks — but you only find out in integration tests or production.<br><br><strong>Pact workflow:</strong><ol><li>Consumer writes a test defining what it expects from the provider → Pact generates a <em>contract</em> (JSON file)</li><li>Contract is published to Pact Broker</li><li>Provider tests run against the contract — verify it can satisfy all consumer expectations</li><li>CI/CD: can deploy only after both sides pass</li></ol><br><pre>// Consumer test (Spring Boot):\n@ExtendWith(PactConsumerTestExt.class)\nclass OrderClientPactTest {\n  @Pact(consumer = \"order-service\", provider = \"product-service\")\n  RequestResponsePact createPact(PactDslWithProvider builder) {\n    return builder\n      .given(\"product 123 exists\")\n      .uponReceiving(\"a request for product 123\")\n      .path(\"/products/123\").method(\"GET\")\n      .willRespondWith()\n      .status(200)\n      .body(newJsonBody(b -> {\n        b.stringType(\"name\", \"Laptop\");\n        b.decimalType(\"price\", 999.99);\n      }).build())\n      .toPact();\n  }\n}</pre>"},

  {q:"What is load testing and what are the key metrics?",d:"medium",
   a:"Load testing simulates real user traffic to measure system performance under expected and peak load — finding bottlenecks before production does.<br><br><strong>Types of load tests:</strong><ul><li><strong>Load test:</strong> gradually ramp up to expected peak load, hold, measure</li><li><strong>Stress test:</strong> push beyond capacity to find the breaking point</li><li><strong>Spike test:</strong> sudden large burst (flash sale, viral event)</li><li><strong>Soak test:</strong> sustained load for hours/days — finds memory leaks, connection exhaustion</li></ul><br><strong>Key metrics to measure:</strong><ul><li><strong>Throughput:</strong> requests per second (RPS) — how much can the system handle?</li><li><strong>Response time percentiles:</strong> p50, p95, p99 latency — 'p99 &lt; 500ms' is a common SLO</li><li><strong>Error rate:</strong> % of requests resulting in 4xx/5xx — should be &lt;0.1%</li><li><strong>Saturation:</strong> CPU %, memory %, connection pool usage — resource bottlenecks</li></ul><br><strong>Tools:</strong><ul><li><strong>Gatling:</strong> Scala DSL, high performance, great HTML reports</li><li><strong>k6:</strong> JavaScript, developer-friendly, CI/CD integration</li><li><strong>Apache JMeter:</strong> GUI-based, widely used, XML scenarios</li><li><strong>Locust:</strong> Python, distributed, easy to write</li></ul><br>Test against a production-like environment with production-like data."},

  {q:"What is the test data builder pattern?",d:"medium",
   a:"Test data builders create realistic, readable test objects without tedious constructor calls or partially-initialised objects.<br><br><strong>Without builder — hard to read, brittle:</strong><br><pre>Order order = new Order(\"ord-1\", \"cust-1\", null, null, 0, null,\n  \"PENDING\", LocalDate.now(), null, false, List.of());\n</pre><br><strong>With test data builder:</strong><br><pre>public class OrderBuilder {\n  private String id = \"ord-\" + UUID.randomUUID();\n  private String customerId = \"cust-default\";\n  private OrderStatus status = OrderStatus.PENDING;\n  private List&lt;OrderItem&gt; items = List.of(OrderItemBuilder.anItem().build());\n  private BigDecimal total = BigDecimal.TEN;\n\n  public static OrderBuilder anOrder() { return new OrderBuilder(); }\n\n  public OrderBuilder withStatus(OrderStatus s) { status = s; return this; }\n  public OrderBuilder withCustomer(String id) { customerId = id; return this; }\n  public OrderBuilder withItems(OrderItem... i) { items = List.of(i); return this; }\n\n  public Order build() { return new Order(id, customerId, status, items, total); }\n}\n\n// Clean, intention-revealing tests:\nOrder shippedOrder = anOrder().withStatus(SHIPPED).build();\nOrder emptyOrder = anOrder().withItems().build();</pre><br><strong>Benefits:</strong> sensible defaults (only set what matters for the test), self-documenting, easy to maintain when domain model changes."}
);

SEC_TESTING.qs.push(
  {q:"What is consumer-driven contract testing with Pact?",d:"hard",
   a:"Contract testing verifies that service integrations work without deploying both services simultaneously. The <em>consumer</em> defines the contract; the <em>provider</em> verifies it.<br><br><strong>Consumer side (defines expectations):</strong><br><pre>@ExtendWith(PactConsumerTestExt.class)\n@PactTestFor(providerName = \"order-service\")\nclass OrderClientContractTest {\n\n  @Pact(consumer = \"payment-service\")\n  RequestResponsePact orderPact(PactDslWithProvider builder) {\n    return builder.given(\"order 123 exists\")\n      .uponReceiving(\"get order 123\")\n      .path(\"/orders/123\").method(\"GET\")\n      .willRespondWith().status(200)\n      .body(new PactDslJsonBody()\n        .stringType(\"id\", \"123\")\n        .stringType(\"status\", \"PENDING\")\n        .decimalType(\"amount\", 99.99))\n      .toPact();\n  }\n\n  @Test @PactTestFor(pactMethod = \"orderPact\")\n  void shouldFetchOrder(MockServer server) {\n    OrderClient client = new OrderClient(server.getUrl());\n    assertThat(client.getOrder(\"123\").getStatus()).isEqualTo(\"PENDING\");\n  }\n}</pre>Pact file is published to Pact Broker.<br><br><strong>Provider side</strong> pulls the pact and verifies it against the real service. Fails the build if the contract is broken — catching breaking changes before production."},

  {q:"What are test data builders and why are they better than test fixtures?",d:"medium",
   a:"<strong>Test fixtures</strong> (hardcoded objects) become fragile when the domain model changes — adding a required field breaks every test that uses the fixture.<br><br><strong>Test Data Builder pattern:</strong> fluent builder with sensible defaults — each test only specifies what matters to it.<br><pre>public class OrderBuilder {\n  private String id = UUID.randomUUID().toString();\n  private String customerId = \"test-customer\";\n  private List&lt;OrderItem&gt; items = List.of(OrderItemBuilder.anItem().build());\n  private OrderStatus status = OrderStatus.PENDING;\n  private BigDecimal amount = new BigDecimal(\"99.99\");\n\n  public static OrderBuilder anOrder() { return new OrderBuilder(); }\n\n  public OrderBuilder withStatus(OrderStatus status) {\n    this.status = status; return this;\n  }\n  public OrderBuilder withAmount(BigDecimal amount) {\n    this.amount = amount; return this;\n  }\n  public Order build() {\n    return new Order(id, customerId, items, status, amount);\n  }\n}\n\n// Test only specifies what's relevant:\nOrder order = anOrder().withStatus(SHIPPED).build();\n// Adding new Order fields only requires updating the builder</pre><br>Tests become self-documenting — the test clearly communicates which fields matter for this specific scenario."},

  {q:"What is property-based testing and when should you use it?",d:"hard",
   a:"Property-based testing generates random inputs automatically and verifies that a property (invariant) holds for all of them — finding edge cases you wouldn't think to write manually.<br><br><strong>Example-based test (limited):</strong><br><pre>// Tests 3 specific cases\n@Test void sortTest() {\n  assertThat(sort(List.of(3,1,2))).isEqualTo(List.of(1,2,3));\n}</pre><br><strong>Property-based test with jqwik (Java):</strong><br><pre>@Property\nvoid sortedListIsAlwaysOrdered(@ForAll List&lt;@IntRange(min=0, max=1000) Integer&gt; list) {\n  List&lt;Integer&gt; sorted = sort(list);\n  // Property 1: same size\n  assertThat(sorted).hasSize(list.size());\n  // Property 2: ordered\n  for (int i = 0; i &lt; sorted.size() - 1; i++)\n    assertThat(sorted.get(i)).isLessThanOrEqualTo(sorted.get(i+1));\n  // Property 3: same elements\n  assertThat(sorted).containsExactlyInAnyOrderElementsOf(list);\n}\n// jqwik runs this 1000 times with random inputs</pre><br><strong>When to use:</strong> mathematical functions (sort, encode/decode, serialize/deserialize round-trip), parsers, business rules with well-defined invariants. Less useful for UI or integration tests."},

  {q:"How do you load test a Spring Boot application with Gatling?",d:"medium",
   a:"Gatling is a Scala-based load testing tool — simulations are written in code, version-controlled alongside the application.<br><br><strong>Example simulation:</strong><br><pre>class OrderSimulation extends Simulation {\n\n  val httpConf = http.baseUrl(\"http://localhost:8080\")\n    .acceptHeader(\"application/json\")\n\n  val createOrder = exec(\n    http(\"Create Order\")\n      .post(\"/orders\")\n      .body(StringBody(\"\"\"{\"customerId\":\"cust-1\",\"amount\":99.99}\"\"\")\n        .contentType(\"application/json\"))\n      .check(status.is(201))\n  )\n\n  val scn = scenario(\"Order Flow\")\n    .exec(createOrder)\n\n  setUp(\n    scn.inject(\n      rampUsers(100).during(30.seconds),  // ramp to 100 users over 30s\n      constantUsersPerSec(50).during(60.seconds) // hold 50 rps for 1 min\n    )\n  ).protocols(httpConf)\n   .assertions(\n     global.responseTime.percentile(95).lt(500), // p95 &lt; 500ms\n     global.failedRequests.percent.lt(1)          // &lt;1% errors\n   )\n}</pre><br>Run with <code>mvn gatling:test</code>. Gatling generates HTML reports with response time histograms and percentile charts."},

  {q:"What are test doubles — mock vs stub vs spy vs fake vs dummy?",d:"medium",
   a:"Test doubles replace real dependencies in unit tests. Each has a different purpose.<br><br><strong>Dummy:</strong> passed but never used — just fills a parameter.<br><pre>new OrderService(dummyLogger); // logger never called in this test</pre><br><strong>Stub:</strong> returns canned responses. No assertions. Just provides data.<br><pre>@Mock PaymentGateway gateway;\nwhen(gateway.charge(any())).thenReturn(new PaymentResult(SUCCESS));</pre><br><strong>Mock:</strong> stub + verification. Asserts that specific interactions happened.<br><pre>@Mock EmailService emailService;\n// ... test code ...\nverify(emailService).sendConfirmation(eq(\"user@example.com\"));</pre><br><strong>Spy:</strong> wraps a real object. Partial mocking — most methods use real implementation, some are stubbed.<br><pre>@Spy OrderService orderService = new OrderService(realRepo);\nwhen(orderService.calculateTax(any())).thenReturn(10.0); // stub one method</pre><br><strong>Fake:</strong> a working implementation with shortcuts (in-memory DB, fake server). Not a mock — it's real logic, just simplified.<br><pre>class FakeOrderRepository implements OrderRepository {\n  Map&lt;Long, Order&gt; orders = new HashMap&lt;&gt;();\n  public Order save(Order order) { orders.put(order.getId(), order); return order; }\n}</pre><br><strong>When to use:</strong> unit tests → mocks/stubs. Integration tests → fakes (Testcontainers, H2). Avoid over-mocking — it couples tests to implementation."},

  {q:"What is property-based testing and how does it differ from example-based?",d:"medium",
   a:"<strong>Example-based testing:</strong> you manually write specific input/output pairs. Limited coverage — only tests the cases you thought of.<br><pre>@Test void reverse() {\n  assertEquals(\"cba\", reverse(\"abc\"));\n  assertEquals(\"\", reverse(\"\"));\n}</pre><br><strong>Property-based testing:</strong> you define properties (invariants) that must hold for <em>all</em> inputs. The framework generates hundreds of random inputs automatically.<br><pre>@Property\nvoid reverseReverseIsIdentity(@ForAll String s) {\n  assertEquals(s, reverse(reverse(s)));\n}\n\n@Property\nvoid reverseLengthUnchanged(@ForAll String s) {\n  assertEquals(s.length(), reverse(s).length());\n}</pre><br>The framework (jqwik in Java, QuickCheck in Haskell) generates random strings and runs the test 1000 times. If a failure is found, it <strong>shrinks</strong> the input to the minimal failing case.<br><br><strong>Benefits:</strong><ul><li>Finds edge cases you didn't anticipate (empty strings, nulls, very large inputs)</li><li>Acts as executable specification — the properties document expected behavior</li><li>Refactoring safety — properties are less brittle than example tests</li></ul><br><strong>Use cases:</strong> parsers (parse + serialize = identity), codecs, data structures (sort, reverse), business rules with clear invariants."},

  {q:"How do you test a Spring Batch job?",d:"medium",
   a:"Spring Batch jobs process large datasets in chunks. Testing involves verifying the job completes successfully, data is transformed correctly, and failure scenarios are handled.<br><br><strong>Test job execution:</strong><br><pre>@SpringBootTest\n@TestExecutionListeners({DependencyInjectionTestExecutionListener.class,\n                         StepScopeTestExecutionListener.class})\npublic class OrderBatchJobTest {\n\n  @Autowired Job orderProcessingJob;\n  @Autowired JobLauncher jobLauncher;\n\n  @Test\n  void testJobExecution() throws Exception {\n    JobParameters params = new JobParametersBuilder()\n      .addString(\"inputFile\", \"classpath:orders.csv\")\n      .addLong(\"timestamp\", System.currentTimeMillis())\n      .toJobParameters();\n\n    JobExecution execution = jobLauncher.run(orderProcessingJob, params);\n\n    assertEquals(BatchStatus.COMPLETED, execution.getStatus());\n    assertEquals(100, execution.getStepExecutions().iterator().next().getWriteCount());\n  }\n}</pre><br><strong>Test ItemReader/ItemProcessor/ItemWriter independently:</strong><br><pre>@TestExecutionListeners({StepScopeTestExecutionListener.class})\npublic class OrderProcessorTest {\n\n  @Autowired ItemProcessor&lt;OrderCsv, Order&gt; processor;\n\n  @Test\n  void processorTransformsCorrectly() throws Exception {\n    OrderCsv input = new OrderCsv(\"123\", \"100.00\");\n    Order output = processor.process(input);\n    assertEquals(123L, output.getId());\n    assertEquals(new BigDecimal(\"100.00\"), output.getAmount());\n  }\n}</pre><br><strong>Use in-memory database:</strong> H2 or Testcontainers for integration tests."},

  {q:"How do you test @Scheduled methods in Spring Boot?",d:"medium",
   a:"<code>@Scheduled</code> methods run automatically at fixed intervals or cron schedules. Testing them requires either invoking them directly or using a test scheduler.<br><br><strong>Option 1 — invoke directly (unit test):</strong><br><pre>@SpringBootTest\npublic class ScheduledTaskTest {\n\n  @Autowired ReportGenerator reportGenerator;\n\n  @Test\n  void testScheduledReportGeneration() {\n    reportGenerator.generateDailyReport(); // call the scheduled method directly\n    // Verify side effects (file created, DB updated, etc.)\n  }\n}</pre><br><strong>Option 2 — use Awaitility to wait for scheduled execution:</strong><br><pre>@SpringBootTest\npublic class ScheduledTaskIntegrationTest {\n\n  @Autowired ReportService reportService;\n\n  @Test\n  void scheduledTaskExecutes() {\n    await().atMost(Duration.ofSeconds(10))\n           .untilAsserted(() -&gt; {\n             verify(reportService, atLeastOnce()).generate();\n           });\n  }\n}</pre><br><strong>Option 3 — disable scheduling in tests, manually trigger:</strong><br><pre>@TestConfiguration\nstatic class TestConfig {\n  @Bean\n  @Primary\n  public ThreadPoolTaskScheduler taskScheduler() {\n    ThreadPoolTaskScheduler scheduler = new ThreadPoolTaskScheduler();\n    scheduler.setPoolSize(0); // disable auto-scheduling\n    return scheduler;\n  }\n}</pre>Then manually call the scheduled method in tests.<br><br><strong>Best practice:</strong> extract business logic to a separate service method. Test that service method directly. The scheduled method becomes a thin wrapper."},

  {q:"What is BDD and how does Cucumber/Gherkin implement it?",d:"medium",
   a:"<strong>BDD (Behavior-Driven Development):</strong> tests written in plain English (Gherkin syntax) that describe <em>what</em> the system should do from a user's perspective — not <em>how</em> it works internally.<br><br><strong>Gherkin syntax:</strong><br><pre>Feature: Order placement\n  Scenario: User places a valid order\n    Given the user is logged in as \"alice@example.com\"\n    And the product \"Laptop\" is in stock\n    When the user places an order for \"Laptop\"\n    Then the order is confirmed\n    And the user receives a confirmation email</pre><br><strong>Step definitions (Java + Cucumber):</strong><br><pre>public class OrderSteps {\n\n  @Given(\"the user is logged in as {string}\")\n  public void userLoggedIn(String email) {\n    authService.login(email, \"password\");\n  }\n\n  @Given(\"the product {string} is in stock\")\n  public void productInStock(String productName) {\n    productRepo.save(new Product(productName, 10)); // 10 in stock\n  }\n\n  @When(\"the user places an order for {string}\")\n  public void placeOrder(String productName) {\n    orderService.createOrder(productName);\n  }\n\n  @Then(\"the order is confirmed\")\n  public void orderConfirmed() {\n    Order order = orderRepo.findLatest();\n    assertEquals(OrderStatus.CONFIRMED, order.getStatus());\n  }\n}</pre><br><strong>Benefits:</strong> non-technical stakeholders can read and validate scenarios. Tests serve as living documentation. Encourages thinking about behavior, not implementation.<br><br><strong>Drawback:</strong> extra layer of abstraction. Overhead of maintaining step definitions. Best for acceptance tests, not low-level unit tests."}
);