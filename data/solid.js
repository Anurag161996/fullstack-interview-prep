const SEC_SOLID = {
  id:"solid",title:"SOLID Principles",icon:"🔩",ib:"rgba(148,163,184,.12)",ic:"#94a3b8",
  grp:"arch",bar:"#94a3b8",tglow:"rgba(148,163,184,.06)",
  desc:"Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, and Dependency Inversion with examples.",
  tags:["SRP","OCP","LSP","ISP","DIP"],
  qs:[
    {q:"What is the Single Responsibility Principle?",d:"easy",id:"single-responsibility-principle",
     a:"A class should have only one reason to change — it should encapsulate one and only one responsibility.<br><br><strong>Violation:</strong> <code>UserService</code> that handles user registration, sends welcome emails, writes audit logs, validates phone numbers, and generates profile images.<br><br><strong>Fix:</strong> split into:<ul><li><code>UserRegistrationService</code> — core registration logic</li><li><code>WelcomeEmailService</code> — email delivery</li><li><code>AuditService</code> — audit logging</li><li><code>PhoneValidator</code> — validation logic</li></ul><br><strong>Benefits:</strong><ul><li>Each class is smaller and easier to understand</li><li>Changes to email logic don't risk breaking registration logic</li><li>Each class can be tested independently</li><li>Classes can be reused separately</li></ul><br><strong>Warning sign:</strong> a class with too many injected dependencies (>5) is often violating SRP."},

    {q:"What is the Open/Closed Principle?",d:"medium",id:"open-closed-principle",
     a:"Software entities should be <strong>open for extension but closed for modification</strong>. New behaviour should be addable without changing tested, working code.<br><br><strong>Violation:</strong><br><pre>// Every new discount type requires changing this class\nclass PriceCalculator {\n  double calculate(Order order) {\n    if (order.type == SEASONAL) return order.price * 0.9;\n    if (order.type == VIP) return order.price * 0.8;\n    // Adding loyalty discount requires editing this method\n  }\n}</pre><br><strong>Fix — Strategy pattern:</strong><br><pre>interface DiscountStrategy { double apply(double price); }\n@Component class SeasonalDiscount implements DiscountStrategy { ... }\n@Component class VipDiscount implements DiscountStrategy { ... }\n// Adding loyalty discount = new @Component class, zero changes to PriceCalculator</pre><br>Achieve OCP with: interfaces, abstract classes, Strategy pattern, Template Method pattern, Spring's <code>@Conditional</code>. New capabilities are <em>added</em> via new implementations, not by modifying existing code."},

    {q:"What is the Liskov Substitution Principle and what violates it?",d:"hard",id:"liskov-substitution-principle-violates",
     a:"Objects of a subtype must be usable wherever the parent type is expected, without breaking the correctness of the program.<br><br><strong>Classic violation — Square extends Rectangle:</strong><br><pre>Rectangle r = new Square(); // Square IS-A Rectangle? No.\nr.setWidth(5);\nr.setHeight(3);\nassert r.getArea() == 15; // FAILS in Square!\n// Square.setWidth() also sets height — breaks Rectangle's contract</pre><br><strong>Other common violations:</strong><ul><li>Subclass throws a new exception type not thrown by the parent</li><li>Subclass weakens a precondition (requires less) — actually OK per LSP</li><li>Subclass strengthens a postcondition (guarantees less) — violation</li><li>Subclass overrides a method to be a no-op</li></ul><br><strong>Fix:</strong> don't use inheritance to represent \"is kind of\" relationships when the subtype doesn't fully honour the parent's contract. Use composition instead. <code>Square</code> and <code>Rectangle</code> should both implement a <code>Shape</code> interface."},

    {q:"What are the Interface Segregation and Dependency Inversion Principles?",d:"medium",id:"interface-segregation-dependency-inversi",
     a:"<strong>Interface Segregation Principle (ISP):</strong> clients should not be forced to depend on interfaces they don't use. Split fat interfaces into smaller, specific ones.<br><br><strong>Violation:</strong><br><pre>// Every printer must implement scan/fax even if it can't\ninterface AllInOnePrinter { void print(); void scan(); void fax(); }</pre><br><strong>Fix:</strong><br><pre>interface Printer { void print(); }\ninterface Scanner { void scan(); }\ninterface Fax { void fax(); }\nclass SimplePrinter implements Printer { ... }         // only print\nclass OfficePrinter implements Printer, Scanner { ... } // print + scan</pre><br><br><strong>Dependency Inversion Principle (DIP):</strong> high-level modules should not depend on low-level modules — both should depend on abstractions.<br><br><strong>Violation:</strong> <code>OrderService</code> instantiates <code>new PostgresOrderRepository()</code> directly — tied to PostgreSQL.<br><br><strong>Fix:</strong><br><pre>// OrderService depends on the interface (abstraction)\nclass OrderService {\n  private final OrderRepository repo; // interface\n  OrderService(OrderRepository repo) { this.repo = repo; }\n}\n// Spring injects the concrete impl at runtime</pre>This is the foundation of Spring's entire dependency injection system."}
  ]
};

SEC_SOLID.qs.push(
  {q:"How do SOLID principles relate to testability and maintainability?",d:"medium",id:"solid-principles-relate-to",
     a:"Each SOLID principle directly improves a specific aspect of testability and maintainability:<br><br><strong>SRP → Smaller tests:</strong> a class with one responsibility has a small, focused test class. 200-line service = 200-line test. 2000-line god class = impossible test.<br><br><strong>OCP → No test rewrites on extension:</strong> adding a new strategy doesn't require modifying — or re-testing — existing strategies. Existing tests don't break.<br><br><strong>LSP → Substitutability in tests:</strong> you can replace real implementations with test doubles (mocks, stubs, fakes) because all implementations honour the same contract. If a subtype breaks the contract, mocking it produces misleading test results.<br><br><strong>ISP → Minimal mock setup:</strong> small, specific interfaces mean your test mocks only the methods the class under test actually uses — not 20 unrelated methods of a fat interface.<br><br><strong>DIP → Full controllability:</strong> dependencies are injected, so tests inject mocks/stubs instead of real implementations. Without DIP (hard-coded <code>new PostgresRepo()</code>), there's nothing to swap — you must test against a real DB."}
);

SEC_SOLID.qs.push(
  {q:"How do you apply SOLID principles when refactoring a God class?",d:"hard",id:"you-apply-solid-principles",
     a:"A God class does too much — it knows too much and controls too much. Common signs: 1000+ lines, 20+ methods, 10+ injected dependencies, long test class.<br><br><strong>Step-by-step refactoring example — UserService god class:</strong><br><pre>// BEFORE: UserService does everything\nclass UserService {\n  void register(User u)     // registration logic\n  void sendWelcomeEmail()   // email sending\n  void validatePhone()      // validation\n  void auditLog()           // audit logging\n  void generateAvatar()     // image generation\n  void updateLastLogin()    // session tracking\n}\n</pre><br><strong>Step 1 — SRP:</strong> identify responsibilities. Each bullet above is a separate responsibility. Extract each into its own class.<br><br><strong>Step 2 — DIP:</strong> have UserService depend on abstractions (<code>EmailSender</code>, <code>PhoneValidator</code>) not concrete implementations.<br><br><strong>Step 3 — ISP:</strong> if <code>UserRepository</code> has 20 methods but UserService only uses 3, extract a narrow <code>UserWriteRepository</code> interface.<br><br><strong>Step 4 — OCP:</strong> replace <code>if/else</code> chains (validation rules, notification channels) with Strategy collections.<br><br><strong>Result:</strong> 6 small, single-purpose classes, each testable in isolation, each with ≤5 dependencies."},

  {q:"What is composition over inheritance and why is it preferred?",d:"medium",id:"composition-over-inheritance-it",
     a:"<strong>Inheritance:</strong> \"IS-A\" relationship. Subclass inherits all behaviour from parent — including behaviour it may not want.<br><br><strong>Composition:</strong> \"HAS-A\" relationship. A class contains instances of other classes and delegates to them.<br><br><strong>Inheritance problems:</strong><ul><li>Fragile base class — changes to the parent break all children</li><li>Cannot change behaviour at runtime</li><li>Java's single inheritance limits flexibility</li><li>Deep hierarchies are hard to understand</li><li>Subclass may inherit methods that break LSP</li></ul><br><strong>Composition solution:</strong><br><pre>// Inheritance (fragile)\nclass EmailNotifier extends BaseNotifier {\n  void notify(Event e) { super.format(e); send(e); }\n}\n\n// Composition (flexible)\nclass OrderNotificationService {\n  private final MessageFormatter formatter;    // injected\n  private final NotificationChannel channel;  // injected — can swap\n  private final RateLimiter rateLimiter;      // injected\n\n  void notify(Order order) {\n    if (rateLimiter.allow()) {\n      channel.send(formatter.format(order));\n    }\n  }\n}</pre><br><strong>Spring enforces this naturally</strong> — every dependency is injected, every abstraction is an interface. DIP + composition over inheritance = the Spring Way."}
);

SEC_SOLID.qs.push(
  {q:"What is the difference between composition and inheritance, and when should you prefer each?",d:"medium",id:"composition-inheritance-should-you",
     a:"<strong>Inheritance (IS-A):</strong> a Dog IS-A Animal. Subclass inherits all parent behaviour. Tight coupling — changes to parent can break subclasses. Hierarchies become rigid over time.<br><br><strong>Composition (HAS-A):</strong> a Car HAS-A Engine. Delegate behaviour to composed objects. Loose coupling — change the Engine without affecting Car.<br><br><strong>Prefer composition when:</strong><ul><li>The relationship is \"has-a\" not truly \"is-a\"</li><li>You want to change behaviour at runtime (swap implementations)</li><li>You're mixing behaviours from multiple sources (multiple inheritance via composition)</li><li>The inheritance hierarchy is becoming deep or complex</li></ul><br><strong>Example — prefer composition:</strong><br><pre>// Inheritance (rigid):\nclass EmailOrderService extends EmailService, OrderService {} // can't in Java\n\n// Composition (flexible):\nclass OrderService {\n  private final EmailSender emailSender;     // compose\n  private final PaymentProcessor payment;   // compose\n  private final InventoryService inventory; // compose\n  // Mix and match implementations via constructor injection\n}</pre><br><strong>Rule:</strong> Use inheritance for genuine IS-A relationships where the subclass <em>is</em> a specialisation of the parent (LSP holds). Use composition for everything else. Spring's entire DI system encourages composition."},

  {q:"How do YAGNI and DRY relate to software design and when do they conflict with each other?",d:"medium",id:"yagni-dry-relate-to",
     a:"<strong>YAGNI</strong> (You Ain't Gonna Need It): don't implement functionality until it's actually needed. Don't over-engineer for hypothetical future requirements.<br><br><strong>DRY</strong> (Don't Repeat Yourself): every piece of knowledge should have a single, unambiguous representation. Avoid code duplication.<br><br><strong>When they conflict:</strong> creating an abstraction to avoid duplication (DRY) adds complexity that may not be needed (YAGNI).<br><pre>// Two similar methods — should we extract a common abstraction?\nvoid sendOrderEmail(Order order) { ... }\nvoid sendInvoiceEmail(Invoice invoice) { ... }\n\n// DRY says: yes, extract EmailSender\n// YAGNI says: wait until you have 3+ similar cases\n// (Rule of Three)</pre><br><strong>The Rule of Three:</strong> tolerate one duplication. Abstract on the second repetition only if you're sure the pattern is stable. Abstract on the third — by now the pattern is real.<br><br><strong>Wrong abstraction is worse than duplication:</strong> a premature abstraction that doesn't quite fit forces callers to work around it — adding more complexity than the duplication it was meant to eliminate (the WET trap: Wrong Every Time).<br><br><strong>Practical balance:</strong> apply DRY eagerly for <em>stable</em> knowledge (constants, utility methods). Apply YAGNI for <em>speculative</em> abstractions and features."},

  {q:"What is the Open/Closed Principle violation and how do you spot it?",d:"medium",id:"open-closed-principle-violation",
     a:"An OCP violation occurs when adding new behaviour requires modifying existing, tested code — introducing risk and regression.<br><br><strong>Warning signs in code:</strong><ul><li>Long <code>if/else</code> or <code>switch</code> chains on type/enum</li><li>Conditions like <code>if (type == \"PAYPAL\") ... else if (type == \"STRIPE\") ...</code></li><li>Every new feature requires editing the same central class</li><li>God classes that know about every variant</li></ul><br><strong>Classic violation:</strong><br><pre>// Every new shape forces changes to AreaCalculator:\nclass AreaCalculator {\n  double calculate(Object shape) {\n    if (shape instanceof Circle c) return Math.PI * c.r * c.r;\n    if (shape instanceof Rectangle r) return r.w * r.h;\n    // Adding Triangle requires editing this class\n  }\n}</pre><br><strong>OCP-compliant fix:</strong><br><pre>interface Shape { double area(); }\nrecord Circle(double r) implements Shape {\n  public double area() { return Math.PI * r * r; }\n}\nrecord Triangle(double b, double h) implements Shape {\n  public double area() { return 0.5 * b * h; }\n}\n// AreaCalculator just calls shape.area() — never needs to change</pre><br>Adding a new Shape = new class. Zero changes to calculator."}
);

SEC_SOLID.qs.push(
  {q:"How do SOLID principles apply in real Spring Boot code?",d:"medium",id:"solid-principles-apply-real",
     a:"<strong>SRP in Spring:</strong> split controllers (HTTP handling), services (business logic), and repositories (data access). A 500-line UserService that sends emails, calculates prices, and manages sessions violates SRP.<br><br><strong>OCP via Strategy in Spring:</strong><br><pre>// Open for extension (new @Component)\n// Closed for modification (no changes to NotificationService)\ninterface NotificationChannel { void send(String msg, String target); }\n@Component class EmailChannel implements NotificationChannel { ... }\n@Component class SmsChannel implements NotificationChannel { ... }\n\n@Service class NotificationService {\n  private final List&lt;NotificationChannel&gt; channels;\n  NotificationService(List&lt;NotificationChannel&gt; channels) {\n    this.channels = channels; // Spring injects all implementations\n  }\n}</pre><br><strong>LSP:</strong> all Spring <code>Repository</code> implementations (JpaRepository, MongoRepository) honour the same contract — code that works with CrudRepository works with any.<br><br><strong>ISP:</strong> define narrow interfaces per client: <code>OrderReader</code> (for read services), <code>OrderWriter</code> (for write services) — not a single fat <code>OrderService</code> interface with 30 methods.<br><br><strong>DIP:</strong> the entire Spring DI system. Your service depends on <code>OrderRepository</code> (abstraction), not <code>JpaOrderRepository</code> (concrete). Tests inject a mock."},

  {q:"When should you NOT follow SOLID principles?",d:"hard",id:"should-you-not-follow",
     a:"SOLID principles are guidelines, not laws. Blindly applying them can lead to over-engineering — premature abstraction is as harmful as none.<br><br><strong>When SRP becomes harmful:</strong> splitting a 50-line class into 10 micro-classes adds more navigation overhead than it saves. Simple CRUD controllers don't need a separate validator, mapper, assembler, and transformer.<br><br><strong>When OCP becomes harmful:</strong> abstracting for extension before you know the extension points. 'You Ain't Gonna Need It' (YAGNI) — don't build a plugin architecture for a feature that will never be extended.<br><br><strong>When DIP becomes harmful:</strong> injecting interfaces everywhere in a small app adds boilerplate without real benefit. If you have only one implementation and no tests for that class, the interface is noise.<br><br><strong>Practical rules:</strong><ul><li>Apply SRP when a class has multiple reasons to change <em>in practice</em>, not in theory</li><li>Apply OCP when you've actually needed to extend the class twice — Rule of Three</li><li>Apply DIP when you need to test or swap implementations — if you won't, save the interface</li></ul><br><strong>Heuristic:</strong> SOLID for libraries and shared code (many consumers, long lifetime); simpler for application code (single consumer, shorter lifetime)."}
);