const SEC_HIBERNATE = {
  id:"hibernate",title:"Hibernate & JPA",icon:"🗄",ib:"rgba(251,146,60,.12)",ic:"#fb923c",
  grp:"spring",bar:"#fb923c",tglow:"rgba(251,146,60,.07)",
  desc:"ORM, entity lifecycle, N+1, caching, locking, JPQL, and best practices.",
  tags:["ORM","N+1","Lazy/Eager","Caching","Locking","JPQL"],
  qs:[
    {q:"What is JPA and how does Hibernate relate to it?",d:"easy",
     a:"<strong>JPA</strong> (Jakarta Persistence API) is a specification — it defines standard annotations (<code>@Entity</code>, <code>@Id</code>, <code>@OneToMany</code>) and interfaces (<code>EntityManager</code>, <code>Query</code>). It describes <em>what</em> an ORM must do, not how.<br><br><strong>Hibernate</strong> is the most popular JPA implementation. It provides the actual SQL generation, dirty checking, caching, and lazy loading logic.<br><br><strong>Spring Data JPA</strong> adds a repository abstraction on top — <code>JpaRepository&lt;Entity, ID&gt;</code> gives you CRUD + pagination + sorting with zero SQL. It delegates to Hibernate under the hood.<br><br>You code against JPA interfaces → Spring Data JPA wraps them → Hibernate executes SQL."},

    {q:"Explain the JPA entity lifecycle states.",d:"medium",
     a:"<strong>Transient:</strong> Created with <code>new</code> — not associated with any persistence context, no DB row, not tracked.<br><br><strong>Managed (Persistent):</strong> Attached to an open <code>EntityManager</code>/<code>Session</code>. Any field change is detected automatically (dirty checking) and flushed to DB on commit. Returned by <code>find()</code>, <code>persist()</code>, or JPQL queries.<br><br><strong>Detached:</strong> Was managed, but the session was closed or <code>detach()</code> was called. Changes are NOT tracked. Common in Spring MVC when an entity is modified in the controller after the service transaction ended.<br><br><strong>Removed:</strong> <code>remove()</code> was called. Scheduled for DELETE on flush. Still in memory until flush.<br><br><strong>Transitions:</strong><ul><li>Transient → Managed: <code>persist()</code></li><li>Detached → Managed copy: <code>merge()</code></li><li>Managed → Detached: <code>detach()</code> or session close</li><li>Managed → Removed: <code>remove()</code></li></ul>"},

    {q:"What is the N+1 select problem and how do you fix it?",d:"hard",
     a:"<strong>N+1 problem:</strong> loading N parent entities triggers N additional queries to load each lazy association — 1 query for orders + N queries for items.<br><br><strong>Fix 1 — JOIN FETCH in JPQL:</strong><br><pre>@Query(\"SELECT o FROM Order o JOIN FETCH o.items WHERE o.status = :s\")\nList&lt;Order&gt; findWithItems(@Param(\"s\") Status s);</pre><br><strong>Fix 2 — @EntityGraph on repository method:</strong><br><pre>@EntityGraph(attributePaths = {\"items\", \"customer\"})\nList&lt;Order&gt; findByStatus(Status status);</pre><br><strong>Fix 3 — @BatchSize (Hibernate-specific):</strong><br><pre>@OneToMany @BatchSize(size = 20)\nprivate List&lt;Item&gt; items; // loads children in batches of 20</pre><br><strong>Fix 4 — DTO projection:</strong> only SELECT the fields you need — bypass the association entirely.<br><br><strong>Detection:</strong> enable SQL logging (<code>spring.jpa.show-sql=true</code>) and use Hibernate Statistics or p6spy to count queries per request. <strong>Never</strong> use blanket <code>FetchType.EAGER</code> — it creates N+1 at a different layer."},

    {q:"What is optimistic vs pessimistic locking in Hibernate?",d:"hard",
     a:"<strong>Optimistic locking:</strong> assumes conflicts are rare — no DB lock held. Uses a <code>@Version</code> field:<br><pre>@Entity class Product {\n  @Version int version;\n  ...\n}</pre>On UPDATE, Hibernate appends <code>WHERE version = :expected</code>. If 0 rows updated (another transaction changed it), throws <code>OptimisticLockException</code>. The caller retries.<br><br><strong>Pessimistic locking:</strong> acquires a DB row lock immediately — prevents concurrent modification.<br><pre>@Lock(LockModeType.PESSIMISTIC_WRITE)\nOptional&lt;Product&gt; findById(Long id);\n// Issues: SELECT ... FOR UPDATE</pre><br><strong>When to use each:</strong><ul><li>Optimistic: low-contention reads (most web applications) — high concurrency, no held locks</li><li>Pessimistic: high-contention writes (e.g., inventory deduction, seat reservation) — guarantees no lost updates</li></ul>"},

    {q:"What is the first-level and second-level cache in Hibernate?",d:"hard",
     a:"<strong>First-level cache (L1):</strong> built into every <code>Session</code>/<code>EntityManager</code> — always enabled. Acts as an identity map: within one session, loading the same entity ID twice returns the <em>same Java object</em> without hitting the DB again. Cleared on session close.<br><br><strong>Second-level cache (L2):</strong> shared across sessions (configurable). Opt-in per entity:<br><pre>@Entity @Cacheable\n@Cache(usage = CacheConcurrencyStrategy.READ_WRITE)\npublic class Country { ... }</pre>Configure a provider in <code>application.properties</code>:<br><code>spring.jpa.properties.hibernate.cache.use_second_level_cache=true</code><br>Common providers: EHCache (in-memory), Redisson (Redis-backed for distributed L2).<br><br><strong>Query cache:</strong> caches JPQL result sets (just primary keys). Invalidated on any modification to the queried table. Use only for truly static reference data — invalidation storms hurt performance.<br><br><strong>L2 cache concurrency strategies:</strong> READ_ONLY (immutable data), READ_WRITE (most common), NONSTRICT_READ_WRITE, TRANSACTIONAL."},

    {q:"What causes LazyInitializationException and how do you fix it?",d:"medium",
     a:"<strong>Cause:</strong> accessing a lazy-loaded association <em>after</em> the Hibernate session is closed.<br><br>Common pattern: entity loaded in <code>@Service</code> → transaction/session ends → controller tries to access <code>order.getItems()</code> → Hibernate has no open session to load items → exception.<br><br><strong>Fix 1 (preferred) — DTO projection:</strong> never pass entities to the web layer. Map to DTOs in the service while the session is open.<br><br><strong>Fix 2 — JOIN FETCH:</strong> load the association eagerly in the query when you know you'll need it.<br><br><strong>Fix 3 — @Transactional scope:</strong> extend the transaction to cover the lazy access. Use sparingly — can degrade performance by holding transactions too long.<br><br><strong>Anti-fix — OSIV:</strong> Spring Boot enables Open Session in View by default (<code>spring.jpa.open-in-view=true</code>). It keeps the session open for the full HTTP request. This silently fires lazy queries in the controller/view layer — <strong>disable it</strong> (<code>spring.jpa.open-in-view=false</code>) and fix the root cause properly."},

    {q:"What is the OSIV anti-pattern and why should you disable it?",d:"hard",
     a:"<strong>OSIV (Open Session In View)</strong> is a Spring Boot default that binds a Hibernate session to the entire HTTP request lifecycle — from filter processing to response writing.<br><br><strong>Why it seems helpful:</strong> lazy associations work in the controller and even in serialization (Jackson serialises the entity and fires lazy queries transparently).<br><br><strong>Why it's harmful at scale:</strong><ul><li>Holds a DB connection (from HikariCP) for the <em>entire</em> request duration including controller logic, serialization, and network time</li><li>Under load (e.g., 100 concurrent requests × 200ms each) you exhaust the connection pool even with simple queries</li><li>Lazy queries fire invisibly in the presentation layer — hard to detect, hard to optimise</li><li>Transaction boundaries become blurry</li></ul><br><strong>Fix:</strong> Set <code>spring.jpa.open-in-view=false</code>. Use DTO projections in your service layer. Load exactly what you need while the transaction is open."},

    {q:"What are the differences between persist(), merge(), save(), and saveOrUpdate()?",d:"hard",
     a:"<strong>JPA standard:</strong><br><code>persist(entity)</code>: transitions Transient → Managed. Throws <code>EntityExistsException</code> if the entity already has an ID and is detached.<br><br><code>merge(entity)</code>: copies the state of a detached (or even new) entity into a <em>new</em> managed instance. Always use the returned value — the argument remains detached.<br><pre>Product saved = em.merge(detachedProduct); // use saved, not detachedProduct</pre><br><strong>Hibernate-specific:</strong><br><code>save(entity)</code>: similar to persist but returns the generated ID immediately (requires a DB round-trip for IDENTITY strategy).<br><br><code>saveOrUpdate(entity)</code>: either saves (new) or reattaches (detached) — can trigger unintended updates. Avoid it.<br><br><strong>Spring Data JPA <code>repository.save(entity)</code>:</strong> calls <code>persist</code> if new (no ID), <code>merge</code> if existing. Cleanest option — just use this."},

    {q:"What are JPQL vs Criteria API vs Native Query?",d:"medium",
     a:"<strong>JPQL</strong> (Jakarta Persistence Query Language): SQL-like but operates on entities and fields, not tables and columns. Portable across providers.<br><pre>@Query(\"SELECT u FROM User u WHERE u.email = :email AND u.active = true\")\nOptional&lt;User&gt; findActiveByEmail(@Param(\"email\") String email);</pre>Pros: readable. Cons: string-based — typos caught at runtime only.<br><br><strong>Criteria API:</strong> programmatic, type-safe query construction using metamodel:<br><pre>CriteriaBuilder cb = em.getCriteriaBuilder();\nCriteriaQuery&lt;User&gt; q = cb.createQuery(User.class);\nRoot&lt;User&gt; u = q.from(User.class);\nq.where(cb.equal(u.get(User_.email), email));\nem.createQuery(q).getResultList();</pre>Pros: refactor-safe, composable for dynamic queries. Cons: verbose. Spring Data <strong>Specifications</strong> make this cleaner.<br><br><strong>Native Query:</strong> raw SQL — use for DB-specific features (window functions, CTEs, <code>RETURNING</code>). <code>@Query(nativeQuery=true)</code>. Loses portability."},

    {q:"What is dirty checking and how do you optimise read-only operations?",d:"hard",
     a:"<strong>Dirty checking:</strong> at flush time (commit or explicit <code>flush()</code>), Hibernate compares the current state of all managed entities against their snapshot (taken at load time). If differences are found, it auto-generates UPDATE statements — no explicit save call needed.<br><br><strong>How snapshots work:</strong> Hibernate stores a deep copy of every loaded entity in the persistence context. On flush, it iterates all entities and field-by-field compares. For large transactions with many entities, this loop is significant overhead.<br><br><strong>Optimisation for read-only operations:</strong><br><pre>// Spring Data — hints\n@QueryHints(@QueryHint(name = HINT_FETCH_SIZE, value = \"50\"))\n@Transactional(readOnly = true)\nList&lt;User&gt; findAllActiveUsers();\n\n// Or at service level\n@Transactional(readOnly = true)\npublic List&lt;UserDto&gt; getAllUsers() { ... }</pre><br>With <code>readOnly=true</code>: Hibernate skips snapshot storage and dirty checking entirely → significant memory and CPU savings for large result sets. Also hints the DB driver to optimise (e.g., JDBC <code>setReadOnly(true)</code>)."},

    {q:"What are entity inheritance strategies in JPA?",d:"hard",
     a:"JPA supports four inheritance mapping strategies:<br><br><strong>SINGLE_TABLE:</strong> all subclasses in one table with a discriminator column. Fastest queries (no join). Subclass-specific columns are nullable for other types. Best for shallow hierarchies with few subclass-specific fields.<br><br><strong>TABLE_PER_CLASS:</strong> each concrete class has its own complete table. No JOIN for single-type queries. Polymorphic queries use UNION ALL — slow and complex. Generally avoid.<br><br><strong>JOINED:</strong> parent class has its own table; subclass tables hold only subclass-specific columns, joined by primary key. Normalised schema. Polymorphic queries require JOIN per level. Best for complex, deeply nested hierarchies with many fields per subclass.<br><br><strong>MAPPED_SUPERCLASS:</strong> parent is not a JPA entity — just provides mapped fields to subclasses via inheritance. No polymorphic queries across subclasses. Use when the parent is just a base class (e.g., <code>BaseEntity</code> with id/createdAt/updatedAt).<br><br><strong>Recommendation:</strong> use SINGLE_TABLE for performance; JOINED for schema cleanliness. Avoid TABLE_PER_CLASS."},

    {q:"How do you implement pagination and sorting with Spring Data JPA?",d:"medium",
     a:"Spring Data provides first-class pagination support via <code>Pageable</code> and <code>Page&lt;T&gt;</code>.<br><br><strong>Controller:</strong><br><pre>@GetMapping(\"/products\")\nPage&lt;ProductDto&gt; list(\n  @RequestParam(defaultValue = \"0\") int page,\n  @RequestParam(defaultValue = \"20\") int size,\n  @RequestParam(defaultValue = \"name\") String sort\n) {\n  Pageable pageable = PageRequest.of(page, size, Sort.by(sort));\n  return productService.list(pageable);\n}</pre><br><strong>Repository:</strong><br><pre>Page&lt;Product&gt; findByActiveTrue(Pageable pageable);\n// or with custom query:\n@Query(\"SELECT p FROM Product p WHERE p.category = :cat\")\nPage&lt;Product&gt; findByCategory(@Param(\"cat\") String cat, Pageable pageable);</pre><br><strong>Response includes:</strong> <code>content</code>, <code>totalElements</code>, <code>totalPages</code>, <code>number</code>, <code>size</code>, <code>first</code>/<code>last</code> booleans.<br><br><strong>Slice&lt;T&gt;</strong>: like Page but doesn't count total — use for cursor-like infinite scroll where total count is expensive."},

    {q:"What is the @GeneratedValue strategy and which works best for microservices?",d:"medium",
     a:"<strong>IDENTITY:</strong> DB auto-increment (<code>SERIAL</code>, <code>AUTO_INCREMENT</code>). Hibernate must INSERT and then SELECT LAST_INSERT_ID() to get the ID — prevents batch insert optimization.<br><br><strong>SEQUENCE:</strong> DB sequence with pre-allocation. Hibernate fetches IDs in blocks (<code>allocationSize=50</code> default) — enables true batch inserts. Best for PostgreSQL.<br><pre>@GeneratedValue(strategy = SEQUENCE, generator = \"order_seq\")\n@SequenceGenerator(name=\"order_seq\", sequenceName=\"order_id_seq\", allocationSize=50)</pre><br><strong>TABLE:</strong> simulates sequence with a DB table — poor performance, avoid.<br><br><strong>AUTO:</strong> provider picks (usually SEQUENCE for PostgreSQL).<br><br><strong>For microservices / distributed systems:</strong> use application-generated <strong>UUIDs</strong> — no DB round-trip, globally unique, safe to generate in any service instance:<br><pre>@Id @GeneratedValue(generator = \"UUID\")\n@GenericGenerator(name = \"UUID\", strategy = \"org.hibernate.id.UUIDGenerator\")\nprivate UUID id;</pre>Or use Snowflake IDs (Twitter-style time-ordered 64-bit IDs) for sortability."}
  ]
};

SEC_HIBERNATE.qs.push(
  {q:"What is the difference between CascadeType.REMOVE and orphanRemoval?",d:"hard",
   a:"<strong>CascadeType.REMOVE:</strong> when you call <code>entityManager.remove(parent)</code>, the DELETE cascades to all children. But if you remove a child from the parent's collection, the child is NOT deleted — it becomes an orphan.<br><pre>order.getItems().remove(item); // item still in DB!</pre><br><strong>orphanRemoval=true:</strong> when a child entity is removed from the parent's collection (dereferenced), it is automatically deleted from the DB.<br><pre>@OneToMany(mappedBy=\"order\",\n           cascade = CascadeType.ALL,\n           orphanRemoval = true)\nprivate List&lt;OrderItem&gt; items;\n\n// This now deletes the item from DB:\norder.getItems().remove(item);</pre><br><strong>When to use:</strong> <code>orphanRemoval=true</code> for fully owned child entities (OrderItem belongs only to Order). Do NOT use if the child can be referenced from multiple parents.<br><br><strong>Common pitfall:</strong> calling <code>order.setItems(new ArrayList&lt;&gt;())</code> with orphanRemoval will delete ALL existing items — always clear the collection instead: <code>order.getItems().clear()</code>."},

  {q:"How do you implement audit fields (createdAt, updatedAt, createdBy) in JPA?",d:"medium",
   a:"<strong>Option 1 — @MappedSuperclass with @EntityListeners (Spring Data JPA):</strong><br><pre>@MappedSuperclass\n@EntityListeners(AuditingEntityListener.class)\npublic abstract class BaseEntity {\n\n  @CreatedDate\n  @Column(updatable = false)\n  private Instant createdAt;\n\n  @LastModifiedDate\n  private Instant updatedAt;\n\n  @CreatedBy\n  @Column(updatable = false)\n  private String createdBy;\n\n  @LastModifiedBy\n  private String updatedBy;\n}</pre><br>Enable: <code>@EnableJpaAuditing</code> on a <code>@Configuration</code> class.<br><br>Provide the current user via an <code>AuditorAware&lt;String&gt;</code> bean:<br><pre>@Bean\nAuditorAware&lt;String&gt; auditorProvider() {\n  return () -&gt; Optional.ofNullable(\n    SecurityContextHolder.getContext()\n      .getAuthentication().getName());\n}</pre><br><strong>All your entities just extend BaseEntity</strong> — zero boilerplate per entity."},

  {q:"What is the difference between @OneToOne and @Embedded?",d:"medium",
   a:"<strong>@OneToOne:</strong> maps a relationship between two separate entities — each has its own table, primary key, and lifecycle. The relationship is established via a foreign key.<br><pre>@Entity class User {\n  @OneToOne(cascade = CascadeType.ALL)\n  @JoinColumn(name = \"address_id\")\n  private Address address; // Address has its own table\n}</pre><br><strong>@Embedded / @Embeddable:</strong> embeds a value object directly into the owner's table — no separate table, no separate primary key. The embedded object is a pure value with no identity.<br><pre>@Embeddable class Address {\n  private String street;\n  private String city;\n  private String zip;\n}\n\n@Entity class User {\n  @Embedded\n  private Address address; // columns go into USER table\n}</pre><br><strong>When to use each:</strong><ul><li>@Embedded: address, money, coordinate — value objects with no independent identity or lifecycle</li><li>@OneToOne: when the related object needs its own ID, can be shared, or has independent lifecycle</li></ul>"}
);

SEC_HIBERNATE.qs.push(
  {q:"How do you implement soft delete in JPA/Hibernate?",d:"medium",
   a:"Soft delete marks records as deleted (with a flag or timestamp) instead of physically removing them — data is preserved for auditing, recovery, or compliance.<br><br><strong>Implementation with @SQLRestriction (Hibernate 6) / @Where (Hibernate 5):</strong><br><pre>@Entity\n@SQLRestriction(\"deleted_at IS NULL\") // auto-added to all queries\npublic class User {\n  @Id Long id;\n  String name;\n\n  @Column(name = \"deleted_at\")\n  private Instant deletedAt;\n\n  public void softDelete() {\n    this.deletedAt = Instant.now();\n  }\n}\n\n// Repository — override delete to soft delete\n@Repository\npublic interface UserRepository extends JpaRepository&lt;User, Long&gt; {\n  @Modifying\n  @Query(\"UPDATE User u SET u.deletedAt = :now WHERE u.id = :id\")\n  void softDelete(@Param(\"id\") Long id, @Param(\"now\") Instant now);\n}</pre><br><strong>@SQLRestriction</strong> adds <code>AND deleted_at IS NULL</code> to every query automatically — deleted records are invisible.<br><br><strong>Include deleted (admin view):</strong> use a native query or a separate entity without the restriction: <code>@Entity @Table(name=\"users\")</code> without the filter."},

  {q:"What is @Formula in Hibernate and when do you use it?",d:"hard",
   a:"<code>@Formula</code> maps a field to a SQL expression rather than a column. The expression is embedded in SELECT statements — it's a virtual, read-only, computed field.<br><br><pre>@Entity\npublic class OrderEntity {\n\n  @Id Long id;\n\n  @Formula(\"(SELECT SUM(i.price * i.quantity) FROM order_items i WHERE i.order_id = id)\")\n  private BigDecimal totalAmount;\n\n  @Formula(\"DATEDIFF(NOW(), created_at)\")\n  private int daysSinceCreated;\n\n  @Formula(\"(SELECT COUNT(*) FROM order_items i WHERE i.order_id = id)\")\n  private int itemCount;\n}\n</pre><br><strong>Important rules:</strong><ul><li>The SQL is database-specific — not portable across DB engines</li><li>Read-only — Hibernate never generates INSERT/UPDATE for @Formula fields</li><li>Lazily computed — only included in SELECT when the field is accessed</li><li>Can reference correlated subqueries, functions, joins</li></ul><br><strong>Alternative:</strong> DTO projections with <code>@Query</code> are more flexible and portable for complex computed values."},

  {q:"What is the difference between @EntityGraph with FETCH and LOAD?",d:"hard",
   a:"<code>@EntityGraph</code> lets you override the default fetch strategy for a specific query — loading associations eagerly that are normally lazy.<br><br><strong>Two types:</strong><br><pre>// Fetch graph: ONLY the specified paths are fetched eagerly.\n// Everything else uses LAZY (even if mapped as EAGER on the entity)\n@EntityGraph(attributePaths = {\"items\"}, type = EntityGraph.EntityGraphType.FETCH)\nList&lt;Order&gt; findByStatus(Status status);\n\n// Load graph: specified paths are EAGER.\n// Everything else uses its MAPPED default (EAGER stays EAGER)\n@EntityGraph(attributePaths = {\"items\"}, type = EntityGraph.EntityGraphType.LOAD)\nList&lt;Order&gt; findByCustomerId(Long customerId);</pre><br><strong>Named entity graphs (defined on entity):</strong><br><pre>@Entity\n@NamedEntityGraph(\n  name = \"Order.withItems\",\n  attributeNodes = @NamedAttributeNode(value = \"items\",\n    subgraph = \"items.product\"),\n  subgraphs = @NamedSubgraph(name = \"items.product\",\n    attributeNodes = @NamedAttributeNode(\"product\"))\n)\npublic class Order { ... }\n\n@EntityGraph(\"Order.withItems\")\nOptional&lt;Order&gt; findById(Long id);</pre><br><strong>Use FETCH graph</strong> (default) — it's explicit and predictable. LOAD is trickier since EAGER mappings on the entity can cause N+1 unexpectedly."},

  {q:"How do you perform bulk INSERT and UPDATE efficiently with Hibernate?",d:"hard",
   a:"Hibernate's default behaviour — one INSERT per entity — is inefficient for thousands of records. Bulk operations require specific configuration.<br><br><strong>Batch inserts:</strong><br><pre># application.properties\nspring.jpa.properties.hibernate.jdbc.batch_size=50\nspring.jpa.properties.hibernate.order_inserts=true\nspring.jpa.properties.hibernate.order_updates=true</pre><br>Also set <code>GenerationType.SEQUENCE</code> with allocationSize — IDENTITY strategy disables batching.<br><br><strong>Flush and clear periodically</strong> to avoid OOM:<br><pre>@Transactional\nvoid batchInsert(List&lt;Product&gt; products) {\n  for (int i = 0; i &lt; products.size(); i++) {\n    em.persist(products.get(i));\n    if (i % 50 == 0) { // match batch_size\n      em.flush();\n      em.clear(); // detach processed entities\n    }\n  }\n}</pre><br><strong>JPQL bulk update/delete (most efficient):</strong><br><pre>@Modifying @Query(\"UPDATE Product p SET p.price = p.price * 1.1 WHERE p.category = :cat\")\nint increasePrices(@Param(\"cat\") String cat);\n// Single UPDATE statement — no entity loading at all</pre><br>After <code>@Modifying</code> queries, the persistence context is stale — add <code>clearAutomatically = true</code>."},

  {q:"What is Hibernate Envers and how do you use it for audit history?",d:"medium",
   a:"Hibernate Envers automatically audits all changes to <code>@Audited</code> entities — storing a full history of every version in separate audit tables.<br><br><strong>Setup:</strong><br><pre>&lt;!-- pom.xml --&gt;\n&lt;dependency&gt;\n  &lt;groupId&gt;org.hibernate.orm&lt;/groupId&gt;\n  &lt;artifactId&gt;hibernate-envers&lt;/artifactId&gt;\n&lt;/dependency&gt;</pre><br><pre>@Entity\n@Audited // audit ALL changes to this entity\npublic class Product {\n  @Id Long id;\n  @NotAudited String internalNote; // exclude specific fields\n  String name;\n  BigDecimal price;\n}</pre><br>Envers creates: <code>product_AUD</code> table (all columns + <code>REV</code>, <code>REVTYPE</code>) and <code>REVINFO</code> table (revision metadata).<br><br><strong>Querying history:</strong><br><pre>AuditReader reader = AuditReaderFactory.get(em);\n\n// Get product as it was at revision 5\nProduct v5 = reader.find(Product.class, productId, 5);\n\n// Get all revisions for a product\nList&lt;Number&gt; revs = reader.getRevisions(Product.class, productId);\n\n// Query: all products changed in last 24h\nAuditQuery q = reader.createQuery()\n  .forRevisionsOfEntity(Product.class, false, true)\n  .add(AuditEntity.revisionProperty(\"timestamp\")\n       .gt(yesterday.toEpochMilli()))\n  .getResultList();</pre>"},

  {q:"What is a projection interface in Spring Data JPA and when should you use it?",d:"medium",
   a:"Projections let you fetch only specific fields from an entity instead of the full object — reducing over-fetching, improving query performance, and giving the web layer a purpose-built DTO.<br><br><strong>Interface projection (closed projection):</strong><br><pre>// Define interface — Spring generates a proxy\npublic interface OrderSummary {\n  Long getId();\n  String getStatus();\n  Instant getCreatedAt();\n  // Nested projection:\n  CustomerInfo getCustomer();\n\n  interface CustomerInfo {\n    String getName();\n    String getEmail();\n  }\n}\n\n// Repository\nList&lt;OrderSummary&gt; findByStatus(Status status);\n// Spring generates: SELECT o.id, o.status, o.created_at,\n//                          c.name, c.email FROM orders o JOIN customers c...</pre><br><strong>Open projection (with SpEL):</strong><br><pre>interface OrderView {\n  @Value(\"#{target.firstName + ' ' + target.lastName}\")\n  String getFullName();\n}</pre><br><strong>DTO projection (class-based):</strong><br><pre>record OrderDto(Long id, String status) {}\n@Query(\"SELECT new com.app.OrderDto(o.id, o.status) FROM Order o\")\nList&lt;OrderDto&gt; findSummaries();</pre><br>Use closed interface projections for simple read views; DTO projections when you need constructor logic or non-entity sources."},

  {q:"How do you handle optimistic locking conflicts gracefully in Spring Boot?",d:"hard",
   a:"When two users update the same record simultaneously, the second update throws <code>ObjectOptimisticLockingFailureException</code> (Spring wrapper for Hibernate's <code>StaleObjectStateException</code>).<br><br><strong>Entity setup:</strong><br><pre>@Entity\npublic class Product {\n  @Id Long id;\n  String name;\n  BigDecimal price;\n  @Version int version; // auto-managed by Hibernate\n}</pre><br><strong>Handle the conflict:</strong><br><pre>@Service\npublic class ProductService {\n  @Retryable(\n    retryFor = ObjectOptimisticLockingFailureException.class,\n    maxAttempts = 3,\n    backoff = @Backoff(delay = 100, multiplier = 2)\n  )\n  @Transactional\n  public Product updatePrice(Long id, BigDecimal newPrice) {\n    Product p = repo.findById(id).orElseThrow();\n    p.setPrice(newPrice);\n    return repo.save(p);\n  }\n\n  @Recover\n  public Product recoverFromConflict(ObjectOptimisticLockingFailureException e, Long id, BigDecimal newPrice) {\n    throw new PriceUpdateConflictException(\"Please reload and try again\");\n  }\n}</pre><br><strong>API response:</strong> return <code>409 Conflict</code> with a clear message — let the client reload and retry with fresh data."},

  {q:"What is the N+1 problem with @ManyToOne and how is it different from @OneToMany?",d:"hard",
   a:"The N+1 problem has two distinct forms depending on the relationship direction:<br><br><strong>@OneToMany N+1 (classic form):</strong><br><pre>// Load 100 orders → then 100 separate queries for each order's items\nList&lt;Order&gt; orders = orderRepo.findAll(); // 1 query\norders.forEach(o -&gt; o.getItems().size()); // +100 queries = 101 total</pre><br><strong>@ManyToOne N+1 (less obvious, often missed):</strong><br><pre>// EAGER is the DEFAULT for @ManyToOne!\n// Even with findAll(), Hibernate issues a query per unique customer\n@ManyToOne // FetchType.EAGER by default!\nprivate Customer customer;\n\nList&lt;Order&gt; orders = orderRepo.findAll();\n// Hibernate issues extra SELECTs to load each unique Customer\n// 100 orders with 50 unique customers = 51 queries</pre><br><strong>Key insight:</strong> @ManyToOne defaults to EAGER — change it to LAZY and use JOIN FETCH when needed:<br><pre>@ManyToOne(fetch = FetchType.LAZY)\nprivate Customer customer;\n\n// Explicit fetch when needed:\n@Query(\"SELECT o FROM Order o JOIN FETCH o.customer WHERE o.id = :id\")\nOptional&lt;Order&gt; findWithCustomer(@Param(\"id\") Long id);</pre><br>Always override @ManyToOne and @OneToOne to LAZY as a best practice."},
);

SEC_HIBERNATE.qs.push(
  {q:"How do you implement soft delete in JPA/Hibernate?",d:"medium",
   a:"Soft delete marks records as deleted (sets a flag) instead of physically removing them — preserving history and allowing recovery.<br><br><strong>Step 1 — add deleted flag to entity:</strong><br><pre>@Entity\n@SQLDelete(sql = \"UPDATE orders SET deleted = true WHERE id = ?\")\n@FilterDef(name = \"deletedFilter\", parameters =\n  @ParamDef(name = \"isDeleted\", type = Boolean.class))\n@Filter(name = \"deletedFilter\", condition = \"deleted = :isDeleted\")\npublic class Order {\n  @Column(nullable = false)\n  private boolean deleted = false;\n}</pre><br><strong>Step 2 — enable filter in every request (filter/interceptor):</strong><br><pre>@Component\npublic class SoftDeleteFilter {\n  @Autowired EntityManager em;\n\n  @PostConstruct\n  public void enableFilter() {\n    Session session = em.unwrap(Session.class);\n    session.enableFilter(\"deletedFilter\")\n           .setParameter(\"isDeleted\", false);\n  }\n}</pre><br><strong>Alternative — @Where (simpler, always applied):</strong><br><pre>@Entity\n@SQLDelete(sql = \"UPDATE orders SET deleted = true WHERE id = ?\")\n@Where(clause = \"deleted = false\")\npublic class Order { ... }</pre><br><strong>Pitfall:</strong> foreign keys still point to soft-deleted records — handle in application logic. Consider adding a <code>deleted_at</code> timestamp for audit purposes."},

  {q:"What is Hibernate Envers and how do you use it for entity auditing?",d:"hard",
   a:"Hibernate Envers automatically tracks every change to an entity across all transactions — creating a full audit history.<br><br><strong>Setup:</strong><br><pre>&lt;dependency&gt;\n  &lt;groupId&gt;org.hibernate.orm&lt;/groupId&gt;\n  &lt;artifactId&gt;hibernate-envers&lt;/artifactId&gt;\n&lt;/dependency&gt;</pre><br><strong>Annotate entities to audit:</strong><br><pre>@Entity\n@Audited\npublic class Product {\n  @Id Long id;\n  String name;\n  BigDecimal price;\n\n  @NotAudited // exclude this field from history\n  String internalNote;\n}</pre><br>Envers creates a <code>product_aud</code> table with columns: id, rev (revision ID), revtype (0=INSERT, 1=UPDATE, 2=DELETE), and all audited fields.<br><br><strong>Query history:</strong><br><pre>AuditReader reader = AuditReaderFactory.get(entityManager);\n\n// Get entity at specific revision\nProduct past = reader.find(Product.class, productId, revisionNumber);\n\n// Get all revisions for an entity\nList&lt;Number&gt; revisions = reader.getRevisions(Product.class, productId);\n\n// Query: all products changed after a date\nAuditQuery query = reader.createQuery()\n  .forEntitiesAtRevision(Product.class, rev)\n  .add(AuditEntity.property(\"price\").gt(100))\n  .getResultList();</pre>"},

  {q:"What are the different types of JPA projections?",d:"medium",
   a:"Projections let you fetch only the columns you need instead of full entities — reducing data transfer and preventing accidental lazy loading.<br><br><strong>1. Interface projection:</strong><br><pre>interface UserSummary {\n  String getName();\n  String getEmail();\n  // Nested: Address getAddress();\n}\n\n// Repository:\nList&lt;UserSummary&gt; findByActiveTrue();\n// Spring generates a proxy implementing UserSummary</pre><br><strong>2. DTO projection (class-based):</strong><br><pre>public record UserDto(String name, String email) {}\n\n// JPQL constructor expression:\n@Query(\"SELECT new com.app.UserDto(u.name, u.email) FROM User u WHERE u.active = true\")\nList&lt;UserDto&gt; findActiveUserDtos();\n\n// Spring Data also supports:\nList&lt;UserDto&gt; findByActiveTrue(Class&lt;UserDto&gt; type);</pre><br><strong>3. Dynamic projection (choose at call time):</strong><br><pre>&lt;T&gt; List&lt;T&gt; findByActive(boolean active, Class&lt;T&gt; type);\n// Call:\nrepo.findByActive(true, UserSummary.class);\nrepo.findByActive(true, UserDto.class);</pre><br><strong>Performance tip:</strong> interface projections backed by a query only SELECT the mapped columns. DTO projections are even faster — no proxy overhead."},

  {q:"How do you perform bulk updates and deletes efficiently in JPA?",d:"hard",
   a:"Loading entities one by one and calling <code>save()</code> or <code>remove()</code> is extremely inefficient for bulk operations — N SELECT + N UPDATE queries.<br><br><strong>JPQL bulk update:</strong><br><pre>@Modifying\n@Query(\"UPDATE Product p SET p.price = p.price * 1.1 WHERE p.category = :cat\")\nint increasePricesByCategory(@Param(\"cat\") String cat);\n// Single UPDATE statement, no entities loaded into memory</pre><br><strong>JPQL bulk delete:</strong><br><pre>@Modifying\n@Query(\"DELETE FROM Order o WHERE o.status = 'CANCELLED' AND o.createdAt &lt; :cutoff\")\nint deleteOldCancelledOrders(@Param(\"cutoff\") Instant cutoff);</pre><br><strong>@Modifying(clearAutomatically = true):</strong> after a bulk update, the persistence context (L1 cache) may hold stale entities. Set <code>clearAutomatically = true</code> to evict the cache automatically after the update.<br><br><strong>Native SQL for complex cases:</strong><br><pre>@Modifying\n@Query(value = \"UPDATE products SET status = 'ARCHIVED' WHERE last_sold_at &lt; NOW() - INTERVAL '2 years'\",\n       nativeQuery = true)\nint archiveOldProducts();</pre><br><strong>Batch inserts:</strong> enable JDBC batching: <code>spring.jpa.properties.hibernate.jdbc.batch_size=50</code> + use <code>SEQUENCE</code> ID strategy (not IDENTITY)."},

  {q:"What is the N+1 problem with @ManyToOne and how is it different from @OneToMany?",d:"hard",
   a:"N+1 manifests differently depending on the relationship type:<br><br><strong>@OneToMany N+1 (lazy collection):</strong> load N orders, then N queries to load each order's items.<br><pre>List&lt;Order&gt; orders = orderRepo.findAll(); // 1 query\norders.forEach(o -> o.getItems().size()); // N queries!</pre><br><strong>@ManyToOne N+1 (EAGER by default!):</strong> <code>@ManyToOne</code> defaults to <code>FetchType.EAGER</code> — every query that loads OrderItem <em>automatically</em> also loads the associated Order. If you load 100 items from different orders, that's 100 extra Order queries — or a JOIN you didn't ask for.<br><pre>// Even this simple query loads EVERY Order for each item:\nList&lt;OrderItem&gt; items = itemRepo.findByProductId(id);</pre><br><strong>Fix for @ManyToOne:</strong><br><pre>// Override default to LAZY\n@ManyToOne(fetch = FetchType.LAZY)\n@JoinColumn(name = \"order_id\")\nprivate Order order;\n\n// Then JOIN FETCH when you need the order:\n@Query(\"SELECT i FROM OrderItem i JOIN FETCH i.order WHERE i.productId = :id\")\nList&lt;OrderItem&gt; findWithOrder(@Param(\"id\") Long id);</pre><br><strong>Rule:</strong> set ALL relationships to LAZY. Fetch eagerly only via JOIN FETCH when you explicitly know you need the association."},

  {q:"How does Hibernate's batch fetching work and when should you use it?",d:"hard",
   a:"Batch fetching is Hibernate's built-in N+1 mitigation — instead of 1 query per association, it groups multiple IDs into a single IN clause.<br><br><strong>Configure globally:</strong><br><pre>spring.jpa.properties.hibernate.default_batch_fetch_size=20\n# Or in hibernate.cfg.xml:\nhibernate.default_batch_fetch_size=20</pre><br><strong>Per-association:</strong><br><pre>@OneToMany(mappedBy = \"order\")\n@BatchSize(size = 20)\nList&lt;OrderItem&gt; items;</pre><br><strong>How it works:</strong> when you access <code>order1.getItems()</code>, Hibernate doesn't just load items for order1. It looks at the other orders in the persistence context and loads items for up to 20 orders in one query:<br><pre>-- Instead of N queries:\nSELECT * FROM order_items WHERE order_id = 1;\nSELECT * FROM order_items WHERE order_id = 2;\n...\n-- Hibernate generates:\nSELECT * FROM order_items WHERE order_id IN (1,2,3,...,20);</pre><br><strong>When to use:</strong> great for cases where JOIN FETCH isn't practical (e.g., multiple bag collections, or dynamic lazy loading in a large object graph). Not as efficient as JOIN FETCH but much better than pure N+1."}
);

SEC_HIBERNATE.qs.push(
  {q:"What is @Formula in Hibernate and when do you use it?",d:"medium",
   a:"<code>@Formula</code> maps a read-only virtual field computed by a SQL expression — the formula is embedded in every SELECT for that entity.<br><pre>@Entity\npublic class Order {\n  @Id private Long id;\n\n  @Formula(\"(SELECT SUM(i.price * i.quantity) FROM order_item i WHERE i.order_id = id)\")\n  private BigDecimal totalAmount;\n\n  @Formula(\"(SELECT COUNT(*) FROM order_item i WHERE i.order_id = id)\")\n  private int itemCount;\n}</pre><br><strong>Important notes:</strong><ul><li>The SQL is executed as a subquery in every SELECT — can be expensive on large datasets</li><li>Read-only — Hibernate ignores this field on INSERT/UPDATE</li><li>Use native SQL (table/column names, not entity/field names)</li><li>Good for simple derived fields; for complex cases prefer DB views or DTO projections</li></ul><br><strong>Alternatives:</strong> <code>@ColumnTransformer</code> for read/write SQL transformations; <code>@Generated</code> for DB-generated columns; a DB view mapped as a read-only entity."},

  {q:"What is @AttributeConverter and how do you use it for custom type mapping?",d:"medium",
   a:"<code>@AttributeConverter</code> maps a Java type to a DB column type when there's no built-in mapping — e.g., enums to custom strings, value objects to a single column.<br><br><strong>Example — persist a Money value object as a BigDecimal column:</strong><br><pre>@Converter(autoApply = true)\npublic class MoneyConverter implements AttributeConverter&lt;Money, BigDecimal&gt; {\n\n  @Override\n  public BigDecimal convertToDatabaseColumn(Money money) {\n    return money == null ? null : money.getAmount();\n  }\n\n  @Override\n  public Money convertToEntityAttribute(BigDecimal value) {\n    return value == null ? null : Money.of(value);\n  }\n}\n\n@Entity\npublic class Order {\n  private Money totalPrice; // auto-converted to/from BigDecimal\n}</pre><br><code>autoApply = true</code> applies the converter to all fields of that type across all entities. Without it, annotate each field with <code>@Convert(converter = MoneyConverter.class)</code>.<br><br><strong>Common uses:</strong> custom enum-to-DB mappings (instead of ordinal), JSON serialization of a POJO into a TEXT column, encrypted fields."},

  {q:"How do you perform bulk updates efficiently in Hibernate?",d:"hard",
   a:"Loading entities into memory for a bulk update is extremely inefficient (1000 entities = 1000 SELECT + 1000 UPDATE). Use JPQL or native bulk UPDATE instead.<br><br><strong>JPQL bulk update:</strong><br><pre>@Modifying\n@Query(\"UPDATE Order o SET o.status = :newStatus WHERE o.status = :oldStatus AND o.createdAt &lt; :cutoff\")\nint bulkUpdateStatus(@Param(\"newStatus\") Status newStatus,\n                     @Param(\"oldStatus\") Status oldStatus,\n                     @Param(\"cutoff\") Instant cutoff);</pre><br><strong>Important gotcha:</strong> JPQL bulk updates bypass the first-level cache — after running, the in-memory state is stale. Clear the EntityManager:<br><pre>@Modifying(clearAutomatically = true, flushAutomatically = true)\n@Query(...)</pre><br><strong>Native SQL for complex updates:</strong><br><pre>@Modifying\n@Query(value = \"UPDATE orders SET status = 'ARCHIVED' WHERE created_at &lt; NOW() - INTERVAL '1 year'\", nativeQuery = true)\nint archiveOldOrders();</pre><br><strong>Batch inserts:</strong> enable JDBC batching via <code>spring.jpa.properties.hibernate.jdbc.batch_size=50</code> and use <code>saveAll()</code> — Hibernate groups INSERTs into batch statements."},

  {q:"What is soft delete and how do you implement it with Hibernate?",d:"medium",
   a:"Soft delete marks records as deleted (e.g., <code>deleted_at = NOW()</code>) instead of physically removing them — preserves data for audit, recovery, and referential integrity.<br><br><strong>Implementation with @SQLRestriction (Hibernate 6) / @Where (Hibernate 5):</strong><br><pre>@Entity\n@SQLRestriction(\"deleted_at IS NULL\")  // auto-appended to every query\npublic class User {\n  @Id private Long id;\n  private String name;\n\n  @Column(name = \"deleted_at\")\n  private Instant deletedAt;\n\n  public void softDelete() {\n    this.deletedAt = Instant.now();\n  }\n}\n\n// Override the DELETE operation\n@SQLDelete(sql = \"UPDATE users SET deleted_at = NOW() WHERE id = ?\")\n@Entity\npublic class User { ... }</pre><br>Now <code>entityManager.remove(user)</code> executes the UPDATE instead of DELETE. All queries automatically filter <code>WHERE deleted_at IS NULL</code>.<br><br><strong>Pitfall:</strong> unique constraints on columns like <code>email</code> break because soft-deleted rows still exist. Fix: make the unique constraint conditional, or append a timestamp to soft-deleted values."},

  {q:"What are DTO projections vs interface projections in Spring Data JPA?",d:"medium",
   a:"Both avoid loading full entity graphs when you only need a subset of fields — essential for performance.<br><br><strong>Interface projection (Spring Data):</strong><br><pre>interface UserSummary {\n  String getName();\n  String getEmail();\n  // Nested\n  AddressView getAddress();\n  interface AddressView { String getCity(); }\n}\n\nList&lt;UserSummary&gt; findByActiveTrue(); // Spring generates proxy</pre>Spring generates a proxy at runtime — convenient but has overhead for nested projections (may still issue joins).<br><br><strong>DTO projection (constructor expression):</strong><br><pre>public record UserDto(String name, String email) {}\n\n@Query(\"SELECT new com.app.dto.UserDto(u.name, u.email) FROM User u WHERE u.active = true\")\nList&lt;UserDto&gt; findActiveSummaries();</pre>Generates a single SQL SELECT with only the needed columns — most efficient.<br><br><strong>Dynamic projections:</strong><br><pre>&lt;T&gt; List&lt;T&gt; findByActiveTrue(Class&lt;T&gt; type);\n// call: repo.findByActiveTrue(UserSummary.class)</pre><br><strong>When to use:</strong> DTO projections for performance-critical read queries; interface projections for convenience in less-critical paths."},

  {q:"What is @AttributeConverter and how do you map custom types?",d:"medium",
   a:"<code>@AttributeConverter</code> lets you define custom mappings between a Java type and a database column — useful for enums, value objects, JSON, encryption.<br><br><strong>Example — store a Money value object as a BIGINT (cents):</strong><br><pre>@Converter(autoApply = true)\npublic class MoneyConverter implements AttributeConverter&lt;Money, Long&gt; {\n\n  @Override\n  public Long convertToDatabaseColumn(Money money) {\n    return money == null ? null : money.getCents();\n  }\n\n  @Override\n  public Money convertToEntityAttribute(Long cents) {\n    return cents == null ? null : new Money(cents);\n  }\n}</pre><br>With <code>autoApply = true</code>, all <code>Money</code> fields are automatically converted. Without it, annotate each field:<br><pre>@Convert(converter = MoneyConverter.class)\nprivate Money price;</pre><br><strong>JSON column example:</strong><br><pre>@Converter\npublic class JsonConverter implements AttributeConverter&lt;Map&lt;String, Object&gt;, String&gt; {\n  @Override\n  public String convertToDatabaseColumn(Map&lt;String, Object&gt; map) {\n    return objectMapper.writeValueAsString(map);\n  }\n  @Override\n  public Map&lt;String, Object&gt; convertToEntityAttribute(String json) {\n    return objectMapper.readValue(json, Map.class);\n  }\n}</pre>"},

  {q:"How do you map a database view to a read-only JPA entity?",d:"medium",
   a:"Database views are query results that behave like tables — perfect for read-only projections in JPA.<br><br><strong>Step 1 — create the view:</strong><br><pre>CREATE VIEW order_summary AS\nSELECT\n  o.id,\n  o.order_number,\n  c.name AS customer_name,\n  SUM(i.price * i.quantity) AS total\nFROM orders o\nJOIN customers c ON o.customer_id = c.id\nJOIN order_items i ON o.id = i.order_id\nGROUP BY o.id, c.name;</pre><br><strong>Step 2 — map to an immutable entity:</strong><br><pre>@Entity\n@Immutable  // Hibernate will not track changes or issue UPDATEs\n@Table(name = \"order_summary\")\npublic class OrderSummary {\n  @Id private Long id;\n  private String orderNumber;\n  private String customerName;\n  private BigDecimal total;\n\n  // Getters only — no setters\n}</pre><br><strong>Repository:</strong><br><pre>public interface OrderSummaryRepository extends JpaRepository&lt;OrderSummary, Long&gt; {\n  List&lt;OrderSummary&gt; findByCustomerNameContaining(String name);\n}</pre><br><strong>Benefits:</strong> denormalised reads without N+1 queries, complex aggregates pre-computed by the DB, no risk of accidental writes."},

  {q:"What is the difference between JOINED and SINGLE_TABLE inheritance performance-wise?",d:"hard",
   a:"JPA supports three inheritance strategies. <code>JOINED</code> and <code>SINGLE_TABLE</code> are the most common — each has different performance trade-offs.<br><br><strong>SINGLE_TABLE (default):</strong> one table for the entire hierarchy with a discriminator column.<br><pre>@Entity\n@Inheritance(strategy = InheritanceType.SINGLE_TABLE)\n@DiscriminatorColumn(name = \"type\")\npublic abstract class Payment { ... }\n\n@Entity @DiscriminatorValue(\"CARD\")\npublic class CardPayment extends Payment { ... }\n\n@Entity @DiscriminatorValue(\"BANK\")\npublic class BankTransferPayment extends Payment { ... }</pre><br><strong>Pros:</strong> fastest reads (single table scan), no joins, simplest schema.<br><strong>Cons:</strong> nullable subclass columns (can't have NOT NULL), table can get wide, poor data integrity.<br><br><strong>JOINED:</strong> one table per class, joined at query time.<br><pre>@Entity\n@Inheritance(strategy = InheritanceType.JOINED)\npublic abstract class Payment { ... } // payments table (id, amount, date)\n\n@Entity\npublic class CardPayment extends Payment { ... } // card_payments table (id FK, card_number)\n\n@Entity\npublic class BankTransferPayment extends Payment { ... } // bank_payments table (id FK, account)</pre><br><strong>Pros:</strong> normalised schema, enforces NOT NULL constraints, cleaner data model.<br><strong>Cons:</strong> every query LEFT JOINs all subclass tables — slower reads, more complex SQL.<br><br><strong>Rule:</strong> use SINGLE_TABLE for performance-critical reads and shallow hierarchies; use JOINED for clean schema and deep hierarchies."},

  {q:"How does @Version work with UUID primary keys?",d:"hard",
   a:"<code>@Version</code> enables optimistic locking — Hibernate includes the version field in the WHERE clause of UPDATE/DELETE to detect concurrent modifications.<br><br><strong>Standard usage with auto-increment ID:</strong><br><pre>@Entity\npublic class Order {\n  @Id @GeneratedValue(strategy = GenerationType.IDENTITY)\n  private Long id;\n\n  @Version\n  private Long version; // increments on each update\n}</pre><br><strong>UUID primary key version field:</strong><br>When using UUID as the primary key, you still need a separate <code>@Version</code> field (typically Long or Integer):<br><pre>@Entity\npublic class Order {\n  @Id\n  private UUID id; // assigned in code, not auto-generated by DB\n\n  @Version\n  private Long version; // Hibernate manages this\n\n  @PrePersist\n  public void generateId() {\n    if (id == null) id = UUID.randomUUID();\n  }\n}</pre><br><strong>Why not use UUID as the version?</strong> <code>@Version</code> must be a numeric or timestamp type that Hibernate can increment. UUIDs don't increment naturally.<br><br><strong>What happens on conflict:</strong><br><pre>// Thread 1 and Thread 2 both read version=5\norder.setStatus(SHIPPED); // Thread 1 updates\nem.flush(); // UPDATE ... WHERE id=? AND version=5, sets version=6\n\n// Thread 2 tries to update with stale version=5\n// UPDATE affects 0 rows → OptimisticLockException</pre>"},

  {q:"What is Hibernate's StatelessSession and when should you use it?",d:"hard",
   a:"<code>StatelessSession</code> is a lightweight Hibernate session with no first-level cache, no dirty checking, no automatic cascade — direct SQL execution with minimal overhead.<br><br><strong>When to use:</strong><ul><li>Batch processing — insert/update thousands of rows without memory buildup</li><li>Read-only bulk queries where you don't need entity lifecycle management</li><li>Performance-critical operations where you manually control SQL</li></ul><br><strong>Example — batch insert:</strong><br><pre>StatelessSession session = sessionFactory.openStatelessSession();\nTransaction tx = session.beginTransaction();\n\nfor (Order order : largeOrderList) {\n  session.insert(order); // bypasses cache, executes INSERT immediately\n}\n\ntx.commit();\nsession.close();</pre><br><strong>Key differences from Session:</strong><br><table><tr><th>Session</th><th>StatelessSession</th></tr><tr><td>First-level cache (identity map)</td><td>No cache</td></tr><tr><td>Dirty checking on flush</td><td>No dirty checking</td></tr><tr><td>Lazy loading</td><td>No lazy loading — everything EAGER</td></tr><tr><td>Cascade operations</td><td>No cascade</td></tr><tr><td>Interceptors / event listeners</td><td>No lifecycle callbacks</td></tr></table><br><strong>Pitfall:</strong> you must manually manage associations and call <code>insert()</code>/<code>update()</code> explicitly — Hibernate won't track changes."}

);