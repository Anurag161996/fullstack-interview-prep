const SEC_SQL = {
  id:"sql",title:"SQL & Databases",icon:"🏛",ib:"rgba(163,117,255,.12)",ic:"#a378ff",
  grp:"data",bar:"#a378ff",tglow:"rgba(163,117,255,.07)",
  desc:"Indexes, ACID, MVCC, joins, query plans, transactions, normalization, and sharding.",
  tags:["Indexes","ACID","MVCC","EXPLAIN","Isolation","Joins"],
  qs:[
    {q:"How does a B-tree index work and what is the leftmost prefix rule?",d:"medium", id:1,
     a:"A B-tree index is a balanced tree where every internal node stores sorted key ranges guiding the search, and leaf nodes store key-pointer pairs pointing to actual rows. Lookup is O(log n) — traverse from root to leaf.<br><br><strong>Composite index leftmost prefix rule:</strong> an index on <code>(a, b, c)</code> can efficiently serve:<ul><li><code>WHERE a = ?</code></li><li><code>WHERE a = ? AND b = ?</code></li><li><code>WHERE a = ? AND b = ? AND c = ?</code></li></ul>But NOT <code>WHERE b = ?</code> alone (no leading column — full scan).<br><br><strong>Column order strategy:</strong> put equality-filter columns first (highest selectivity), range filter column last. Example for <code>WHERE status = 'active' AND created_at &gt; '2024-01-01'</code>: index on <code>(status, created_at)</code>.<br><br><strong>Covering index:</strong> includes all columns referenced in a query — avoids heap lookup entirely (Index Only Scan in PostgreSQL)."},

    {q:"What are ACID properties and how does PostgreSQL enforce each?",d:"medium", id:2,
     a:"<strong>Atomicity:</strong> all operations in a transaction succeed or all are rolled back. Enforced by the Write-Ahead Log (WAL) — changes are written to WAL before data pages. On crash, incomplete transactions are rolled back during recovery.<br><br><strong>Consistency:</strong> the database moves from one valid state to another. Enforced by constraints (PRIMARY KEY, FOREIGN KEY, UNIQUE, CHECK, NOT NULL), triggers, and application-level validation.<br><br><strong>Isolation:</strong> concurrent transactions don't interfere with each other. PostgreSQL uses MVCC — each transaction sees a consistent snapshot, not the latest dirty data. No read locks needed; readers and writers don't block each other.<br><br><strong>Durability:</strong> committed data survives crashes. WAL records are fsync'd to disk before the commit acknowledgement is sent to the client. Even if the server crashes immediately after, the WAL allows recovery of committed transactions."},

    {q:"What is MVCC and how does PostgreSQL implement it?",d:"hard", id:3,
     a:"MVCC (Multi-Version Concurrency Control) keeps multiple row versions instead of overwriting in place. Each row version has hidden system columns:<ul><li><code>xmin</code>: transaction ID that created this version</li><li><code>xmax</code>: transaction ID that deleted/updated this version (0 if still current)</li></ul><br><strong>Snapshot visibility rule:</strong> a transaction with snapshot S sees a row version if <code>xmin</code> is committed and ≤ S's snapshot, and <code>xmax</code> is null or not committed or > S's snapshot.<br><br><strong>Benefits:</strong> readers never block writers; writers never block readers — no shared read locks needed. High concurrency for read-heavy workloads.<br><br><strong>Cost:</strong> dead row versions accumulate when rows are updated/deleted. <strong>VACUUM</strong> reclaims them. Monitor with <code>SELECT n_dead_tup FROM pg_stat_user_tables</code>. Autovacuum handles this automatically but may lag on high-update tables."},

    {q:"How do you read a PostgreSQL EXPLAIN ANALYZE output?",d:"hard", id:4,
     a:"<code>EXPLAIN ANALYZE</code> runs the query and shows actual execution stats:<br><br><strong>Key nodes and what they signal:</strong><ul><li><code>Seq Scan</code> on large table → missing index (check <code>rows</code> count)</li><li><code>Index Scan</code> → good; <code>Index Only Scan</code> → great (covering index)</li><li><code>Bitmap Heap Scan</code> → good for range queries returning many rows</li><li><code>Hash Join</code> → efficient for large unsorted sets</li><li><code>Nested Loop</code> → efficient when inner side is small; dangerous if inner loops many times</li></ul><br><strong>Key metrics to look at:</strong><ul><li><code>actual rows</code> vs <code>rows</code> estimate — big mismatch? Run <code>ANALYZE tablename</code> to update statistics</li><li><code>actual time</code> — high value? That node is the bottleneck</li><li><code>Buffers: hit=X read=Y</code> — <code>read</code> means disk I/O (cache miss)</li><li><code>Sort</code> without an index → add index on ORDER BY column</li></ul>"},

    {q:"What are database transaction isolation levels?",d:"hard", id:5,
     a:"Isolation levels control what dirty or stale data a transaction can see from concurrent transactions.<br><br><strong>READ UNCOMMITTED:</strong> can see uncommitted changes (dirty reads). Effectively unsupported in PostgreSQL (behaves as READ COMMITTED).<br><br><strong>READ COMMITTED</strong> (PostgreSQL default): each SQL statement sees a fresh snapshot of committed data. Non-repeatable reads possible (same row read twice gives different values if another transaction commits between them).<br><br><strong>REPEATABLE READ:</strong> transaction sees a snapshot from its start — same query returns same results throughout. Prevents non-repeatable reads. PostgreSQL's SSI implementation also prevents most phantom reads.<br><br><strong>SERIALIZABLE:</strong> full isolation. Implemented via SSI (Serializable Snapshot Isolation) in PostgreSQL — detects read-write conflicts and aborts one of the conflicting transactions. Most correct, some serialization failures possible (retry needed). Use for financial transactions.<br><br><strong>Rule:</strong> use the minimum isolation level that meets your correctness requirements."},

    {q:"What are the types of SQL joins?",d:"easy", id:6,
     a:"<strong>INNER JOIN:</strong> returns only rows where the join condition matches in both tables. Most common.<br><br><strong>LEFT (OUTER) JOIN:</strong> all rows from the left table + matching rows from the right. Non-matching right rows → NULL. Use when you want all records from the left even if no match exists.<br><br><strong>RIGHT (OUTER) JOIN:</strong> all rows from the right + matching from left. Rarely used — just swap tables and use LEFT JOIN instead.<br><br><strong>FULL OUTER JOIN:</strong> all rows from both tables. Non-matching rows on either side → NULL.<br><br><strong>CROSS JOIN:</strong> Cartesian product — every left row × every right row. O(n×m). Use with caution on large tables.<br><br><strong>SELF JOIN:</strong> a table joined with itself — useful for hierarchical data (employee/manager relationship) or finding duplicate rows.<br><br><pre>-- Find employees and their managers\nSELECT e.name, m.name AS manager\nFROM employees e\nLEFT JOIN employees m ON e.manager_id = m.id;</pre>"},

    {q:"What is normalization and when do you deliberately denormalize?",d:"medium", id:7,
     a:"<strong>Normalization</strong> eliminates data redundancy and update anomalies through normal forms:<ul><li><strong>1NF:</strong> atomic values, no repeating groups</li><li><strong>2NF:</strong> no partial dependencies on a composite key</li><li><strong>3NF:</strong> no transitive dependencies (non-key column depends only on the primary key)</li></ul>Benefits: single source of truth, cleaner updates, smaller storage.<br><br><strong>Denormalize when:</strong><ul><li>Read performance is critical and the data is mostly read (reporting, search)</li><li>Pre-computed aggregate columns (daily totals, counts) avoid expensive GROUP BY at query time</li><li>CQRS read models — dedicated read tables denormalized for specific query patterns</li><li>Search indexes (Elasticsearch) — entire document stored denormalized for fast retrieval</li></ul><br><strong>Trade-off:</strong> faster reads, risk of inconsistency, more complex writes. Apply denormalization selectively after profiling, not as a default."},

    {q:"What is connection pooling and how do you configure HikariCP in Spring Boot?",d:"medium", id:8,
     a:"Establishing a DB connection is expensive (~50–100ms for TCP handshake + TLS + authentication). <strong>Connection pooling</strong> maintains a set of pre-established connections that are checked out and returned per request, amortising the overhead.<br><br><strong>HikariCP</strong> is the default pool in Spring Boot (included in <code>spring-boot-starter-data-jpa</code>). Key properties:<br><pre>spring.datasource.hikari.maximum-pool-size=10\nspring.datasource.hikari.minimum-idle=5\nspring.datasource.hikari.connection-timeout=30000  # ms\nspring.datasource.hikari.idle-timeout=600000\nspring.datasource.hikari.max-lifetime=1800000</pre><br><strong>Pool sizing formula:</strong> <code>(CPU cores × 2) + number of disk spindles</code>. For most apps, 10–20 connections is optimal. More connections ≠ more performance — excessive connections cause DB-side context switching overhead.<br><br><strong>Monitor:</strong> expose HikariCP metrics via Micrometer/Actuator: <code>hikaricp.connections.active</code>, <code>hikaricp.connections.pending</code>."},

    {q:"What are window functions in SQL and when do you use them?",d:"hard", id:9,
     a:"Window functions compute a value across a set of related rows <em>without collapsing them</em> (unlike GROUP BY). Syntax: <code>function() OVER (PARTITION BY ... ORDER BY ... ROWS BETWEEN ...)</code><br><br><strong>Common functions:</strong><ul><li><code>ROW_NUMBER()</code> — unique sequential rank (no ties)</li><li><code>RANK()</code> — rank with gaps for ties</li><li><code>DENSE_RANK()</code> — rank without gaps</li><li><code>LAG(col, n)</code> / <code>LEAD(col, n)</code> — access previous/next row's value</li><li><code>SUM(amount) OVER (PARTITION BY user_id ORDER BY date)</code> — running total per user</li><li><code>NTILE(4)</code> — divide rows into N equal buckets (quartiles)</li></ul><br><strong>Classic use cases:</strong><ul><li>Top-N per group: <code>WHERE rn = 1</code> on a <code>ROW_NUMBER() OVER (PARTITION BY category ORDER BY price DESC)</code></li><li>Running totals / moving averages</li><li>Finding consecutive events or gaps in sequences</li></ul>"},

    {q:"What is the difference between DELETE, TRUNCATE, and DROP?",d:"easy", id:10,
     a:"<strong>DELETE:</strong> DML statement. Removes rows matching a WHERE clause (all rows if no WHERE). Each row deletion is logged individually. Fires triggers. Can be rolled back in a transaction. Respects foreign key constraints. Slow on large tables.<br><br><strong>TRUNCATE:</strong> DDL statement. Removes ALL rows from a table instantly by deallocating data pages. Minimal logging (just the deallocation). Much faster than DELETE for large tables. Resets identity/serial columns. In PostgreSQL, TRUNCATE is transactional and can be rolled back — unlike Oracle/MySQL. Does NOT fire row-level triggers (may fire statement-level).<br><br><strong>DROP TABLE:</strong> DDL statement. Removes the entire table structure, all data, all indexes, all constraints, all triggers. Permanent. Cannot select from it anymore. In PostgreSQL, transactional — can be rolled back.<br><br><strong>Summary:</strong> DELETE = remove rows conditionally; TRUNCATE = clear table fast; DROP = delete the table itself."},

    {q:"What is database sharding and what problems does it introduce?",d:"hard", id:11,
     a:"<strong>Sharding</strong> horizontally partitions data across multiple database instances. Each shard holds a subset of rows, determined by a shard key.<br><br><strong>Example:</strong> <code>shard_id = user_id % 4</code> → 4 shards, each holding 25% of users.<br><br><strong>Benefits:</strong><ul><li>Scales write throughput (each shard handles independent writes)</li><li>Scales storage (data distributed across multiple disks)</li><li>Each shard is smaller → faster queries on indexed data</li></ul><br><strong>Problems introduced:</strong><ul><li><strong>Cross-shard JOINs:</strong> a join across shards requires fetching from multiple DBs and joining in application code</li><li><strong>Resharding:</strong> changing the shard count requires moving data — operationally complex</li><li><strong>Hot shards:</strong> if the shard key is skewed (e.g., viral user has all traffic), one shard overloads</li><li><strong>No cross-shard ACID transactions:</strong> use Sagas for multi-shard operations</li></ul><br><strong>Try first:</strong> read replicas (for read scaling), vertical scaling, table partitioning (single DB, multiple physical files). Only shard when you've exhausted all other options."},

    {q:"What are stored procedures and when should you use them?",d:"medium", id:12,
     a:"A stored procedure is pre-compiled SQL code stored in the database server, executed by name.<br><br><strong>Advantages:</strong><ul><li>Reduced network round-trips (batch logic runs server-side)</li><li>Pre-compiled execution plan (slightly faster first execution)</li><li>Centralised data validation logic</li></ul><br><strong>Disadvantages:</strong><ul><li>Logic outside the application — harder to version control, test, debug</li><li>Vendor lock-in (T-SQL vs PL/pgSQL vs PL/SQL)</li><li>Difficult to test in CI/CD pipelines</li><li>IDE support / refactoring is worse than application code</li></ul><br><strong>When to use:</strong><ul><li>Complex batch operations that would require many round-trips</li><li>Database-level auditing or row-level security</li><li>Legacy systems where stored procs are already the standard</li></ul><br><strong>Modern preference:</strong> keep logic in the application layer for maintainability, testability, and deployability. Use stored procedures sparingly and only for genuine database-tier operations."}
  ]
};

SEC_SQL.qs.push(
  {q:"What is the difference between HAVING and WHERE?",d:"easy", id:13,
     a:"<strong>WHERE</strong> filters rows <em>before</em> grouping — it operates on individual rows. Cannot reference aggregate functions.<br><br><strong>HAVING</strong> filters groups <em>after</em> <code>GROUP BY</code> — it operates on the aggregated result. Can reference aggregate functions.<br><br><pre>-- Find categories with more than 10 products priced over $50\nSELECT category, COUNT(*) AS cnt, AVG(price) AS avg_price\nFROM products\nWHERE price &gt; 50              -- filter rows first\nGROUP BY category\nHAVING COUNT(*) &gt; 10;         -- filter groups after aggregation</pre><br><strong>Execution order:</strong> FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT.<br><br><strong>Tip:</strong> push as much filtering into WHERE as possible — it reduces the number of rows that need to be grouped, which is faster."},

  {q:"What are CTEs (Common Table Expressions) and how do recursive CTEs work?",d:"hard", id:14,
     a:"A <strong>CTE</strong> (WITH clause) is a named temporary result set scoped to a single query. Improves readability and allows referencing the same subquery multiple times.<br><pre>WITH monthly_revenue AS (\n  SELECT DATE_TRUNC('month', created_at) AS month,\n         SUM(amount) AS revenue\n  FROM orders\n  GROUP BY 1\n)\nSELECT month, revenue,\n       LAG(revenue) OVER (ORDER BY month) AS prev_revenue\nFROM monthly_revenue;</pre><br><strong>Recursive CTE:</strong> references itself to traverse hierarchical data (org charts, trees):<br><pre>WITH RECURSIVE org_tree AS (\n  -- Anchor: start with CEO\n  SELECT id, name, manager_id, 0 AS depth\n  FROM employees WHERE manager_id IS NULL\n\n  UNION ALL\n\n  -- Recursive: find direct reports\n  SELECT e.id, e.name, e.manager_id, ot.depth + 1\n  FROM employees e\n  JOIN org_tree ot ON e.manager_id = ot.id\n)\nSELECT * FROM org_tree ORDER BY depth, name;</pre><br>PostgreSQL limits recursion depth with <code>SET max_recursion_depth</code> to prevent infinite loops."},

  {q:"What are partial indexes and covering indexes in PostgreSQL?",d:"hard", id:15,
     a:"<strong>Partial index:</strong> an index built on a subset of rows (those satisfying a WHERE predicate). Much smaller than a full index — faster to scan and maintain.<br><pre>-- Only index active users (90% are inactive)\nCREATE INDEX idx_users_email_active\nON users (email)\nWHERE active = true;\n\n-- Query MUST include the same predicate to use the index\nSELECT * FROM users WHERE email = ? AND active = true; -- uses index\nSELECT * FROM users WHERE email = ?;                   -- cannot use it</pre><br><strong>Covering index (Index Only Scan):</strong> index includes all columns referenced in the query — avoids heap lookup entirely.<br><pre>-- Query: SELECT name, status FROM orders WHERE user_id = ?\nCREATE INDEX idx_orders_user_covering\nON orders (user_id) INCLUDE (name, status);\n-- PostgreSQL fetches everything from the index leaf — no heap access</pre><br>Covering indexes are extremely powerful for hot read paths — can reduce query cost by 10–100× for large tables."}
);

SEC_SQL.qs.push(
  {q:"What are materialized views and when do you use them?",d:"hard", id:16,
     a:"A <strong>materialized view</strong> stores the result of a query physically on disk — unlike a regular view which is just a stored SQL statement re-executed on every access.<br><br><strong>Create in PostgreSQL:</strong><br><pre>CREATE MATERIALIZED VIEW monthly_sales_summary AS\nSELECT\n  DATE_TRUNC('month', order_date) AS month,\n  product_id,\n  SUM(quantity) AS total_qty,\n  SUM(amount) AS total_revenue,\n  COUNT(*) AS order_count\nFROM orders\nGROUP BY 1, 2;\n\nCREATE INDEX ON monthly_sales_summary(month, product_id);</pre><br><strong>Refresh:</strong><br><pre>REFRESH MATERIALIZED VIEW monthly_sales_summary;\n-- Non-blocking (requires unique index):\nREFRESH MATERIALIZED VIEW CONCURRENTLY monthly_sales_summary;</pre><br><strong>Use when:</strong><ul><li>Expensive aggregation queries needed frequently (dashboards, reports)</li><li>The underlying data changes slowly (hourly/nightly refresh is acceptable)</li><li>You can tolerate slight staleness</li></ul><br><strong>Trade-off:</strong> queries are fast (read pre-computed data), but data is stale until next refresh and refresh itself can be expensive. For real-time needs, use a CQRS read model instead."},

  {q:"What is an UPSERT and how do you write one in PostgreSQL?",d:"medium", id:17,
     a:"An UPSERT inserts a row if it doesn't exist, or updates it if it does — atomically, without a separate SELECT first (which would create a race condition).<br><br><strong>PostgreSQL INSERT ... ON CONFLICT:</strong><br><pre>-- Do nothing on conflict (idempotent insert)\nINSERT INTO user_sessions (user_id, token, created_at)\nVALUES (:userId, :token, NOW())\nON CONFLICT (user_id) DO NOTHING;\n\n-- Update on conflict (true upsert)\nINSERT INTO product_inventory (product_id, quantity, updated_at)\nVALUES (:id, :qty, NOW())\nON CONFLICT (product_id) DO UPDATE SET\n  quantity = EXCLUDED.quantity,\n  updated_at = EXCLUDED.updated_at;\n\n-- Conditional update on conflict\nON CONFLICT (product_id) DO UPDATE SET\n  quantity = product_inventory.quantity + EXCLUDED.quantity\n  WHERE product_inventory.updated_at &lt; EXCLUDED.updated_at;</pre><br><code>EXCLUDED</code> refers to the row that would have been inserted.<br><br><strong>Spring Data JPA:</strong><br><pre>@Modifying\n@Query(value = \"INSERT INTO product_inventory ... ON CONFLICT ...\", nativeQuery = true)\nvoid upsertInventory(@Param(\"id\") Long id, @Param(\"qty\") int qty);</pre>"},

  {q:"How does PostgreSQL JSONB differ from JSON and when do you use it?",d:"medium", id:18,
     a:"PostgreSQL has two JSON types:<br><br><strong>json:</strong> stores JSON as plain text — preserves whitespace and key order. Parses on every access. Essentially a validated text column.<br><br><strong>jsonb:</strong> stores JSON in a parsed binary format — no whitespace, no duplicate keys, keys sorted alphabetically. Faster to query; supports indexing.<br><br><strong>Why always choose jsonb:</strong><br><pre>-- GIN index on all jsonb keys/values\nCREATE INDEX idx_metadata ON products USING gin(metadata);\n\n-- Query any key\nSELECT * FROM products WHERE metadata @&gt; '{\"colour\": \"red\"}';\n\n-- Access specific path\nSELECT metadata -&gt;&gt; 'brand' FROM products WHERE id = 1;\n\n-- Check key exists\nSELECT * FROM products WHERE metadata ? 'warranty';\n\n-- Update a single key (no rewrite)\nUPDATE products SET metadata = metadata || '{\"sale\": true}'::jsonb\nWHERE category = 'electronics';</pre><br><strong>When to use JSONB:</strong> semi-structured or schema-less attributes (product metadata, user preferences, dynamic form fields) where the structure varies per row. Not a replacement for normalized relational data — don't store what belongs in columns."},

  {q:"What is table partitioning in PostgreSQL and what are the strategies?",d:"hard", id:19,
     a:"Table partitioning splits a large logical table into smaller physical pieces (partitions) — improving query performance, simplifying data lifecycle management, and enabling faster deletes.<br><br><strong>Range partitioning (most common for time-series):</strong><br><pre>CREATE TABLE orders (\n  id BIGINT,\n  order_date DATE,\n  amount DECIMAL\n) PARTITION BY RANGE (order_date);\n\nCREATE TABLE orders_2024_q1 PARTITION OF orders\n  FOR VALUES FROM ('2024-01-01') TO ('2024-04-01');\nCREATE TABLE orders_2024_q2 PARTITION OF orders\n  FOR VALUES FROM ('2024-04-01') TO ('2024-07-01');</pre><br>Queries with <code>WHERE order_date BETWEEN '2024-01-01' AND '2024-03-31'</code> only scan the relevant partition (<strong>partition pruning</strong>).<br><br><strong>List partitioning:</strong> partition by a discrete set of values (e.g., region: 'US', 'EU', 'APAC').<br><br><strong>Hash partitioning:</strong> distribute rows evenly across N partitions via hash — useful for parallel processing, not for range queries.<br><br><strong>Benefits:</strong> DROP old partition = instant delete of millions of rows. Each partition has its own indexes. Maintenance operations (VACUUM, ANALYZE) per partition."},

  {q:"What are database transactions and how do savepoints work?",d:"medium", id:20,
     a:"A transaction is a unit of work that is either fully committed or fully rolled back. In Spring, <code>@Transactional</code> manages this automatically.<br><br><strong>Savepoints</strong> allow partial rollback within a transaction — roll back to a checkpoint without aborting the entire transaction:<br><br><pre>@Transactional\npublic void processOrder(Order order) {\n  orderRepo.save(order);\n  \n  // Create a savepoint before risky operation\n  Object savepoint = TransactionAspectSupport\n    .currentTransactionStatus().createSavepoint();\n  \n  try {\n    // Risky operation: send to external system\n    externalSystem.notifyOrder(order);\n  } catch (ExternalSystemException e) {\n    // Roll back only to the savepoint — order save is preserved\n    TransactionAspectSupport\n      .currentTransactionStatus().rollbackToSavepoint(savepoint);\n    // Log and continue without the notification\n    log.warn(\"External notification failed, continuing\", e);\n  }\n  \n  auditRepo.save(new AuditEvent(order.getId()));\n}</pre><br><strong>JPA NESTED propagation</strong> uses savepoints automatically — inner @Transactional(NESTED) rolls back to its savepoint without affecting the outer transaction."},

  {q:"What is the difference between optimistic and pessimistic concurrency in SQL?",d:"hard", id:21,
     a:"These are database-level strategies for handling concurrent modifications to the same rows:<br><br><strong>Pessimistic concurrency — lock first:</strong><br><pre>-- PostgreSQL: lock the row for update\nSELECT * FROM accounts\nWHERE id = 123\nFOR UPDATE; -- other transactions block here\n\n-- Skip locked rows (non-blocking, for job queues)\nSELECT * FROM jobs\nWHERE status = 'PENDING'\nLIMIT 10\nFOR UPDATE SKIP LOCKED;</pre><br>Row held exclusively until transaction commits. Zero risk of lost updates. Performance impact on high-concurrency tables.<br><br><strong>Optimistic concurrency — check after:</strong><br><pre>-- Use a version column\nUPDATE accounts\nSET balance = :newBalance, version = version + 1\nWHERE id = :id AND version = :expectedVersion;\n\n-- If rows_affected = 0, another transaction changed it first</pre><br><strong>FOR UPDATE SKIP LOCKED</strong> is extremely useful for distributed job queues — multiple workers pick up different tasks without blocking each other:<br><pre>// Spring Data JPA:\n@Lock(LockModeType.PESSIMISTIC_WRITE)\n@QueryHints(@QueryHint(name = \"javax.persistence.lock.timeout\", value = \"-2\")) // SKIP_LOCKED\nList&lt;Job&gt; findTop10ByStatus(Status status);</pre>"},

  {q:"What is the difference between INNER JOIN, LEFT JOIN, and EXISTS vs IN?",d:"medium", id:22,
     a:"<strong>JOIN vs EXISTS/IN for existence checks:</strong><br><br><strong>IN subquery:</strong> evaluates the subquery once, then checks each row against the result set. Can be slow if subquery returns many rows.<br><pre>SELECT * FROM orders\nWHERE customer_id IN (SELECT id FROM customers WHERE country = 'IN');</pre><br><strong>EXISTS:</strong> short-circuits as soon as the subquery finds one match — more efficient for large datasets.<br><pre>SELECT * FROM orders o\nWHERE EXISTS (\n  SELECT 1 FROM customers c\n  WHERE c.id = o.customer_id AND c.country = 'IN'\n);</pre><br><strong>JOIN equivalent:</strong><br><pre>SELECT DISTINCT o.* FROM orders o\nJOIN customers c ON c.id = o.customer_id\nWHERE c.country = 'IN';</pre><br><strong>Performance rules of thumb:</strong><ul><li><code>EXISTS</code> > <code>IN</code> when the subquery is large</li><li><code>IN</code> ≈ <code>EXISTS</code> when the subquery is small (modern optimisers often rewrite one to the other)</li><li>JOIN is often fastest when you also need columns from the joined table</li><li>NOT EXISTS often outperforms NOT IN (NOT IN returns NULL for null values — semantic trap)</li></ul>"}
);

SEC_SQL.qs.push(
  {q:"What is an UPSERT and how does it work in PostgreSQL?",d:"medium", id:23,
     a:"An UPSERT inserts a new row if it doesn't exist, or updates the existing row if it does — atomically, in a single statement. Avoids race conditions between SELECT + INSERT/UPDATE.<br><br><strong>PostgreSQL syntax (INSERT ... ON CONFLICT):</strong><br><pre>-- Insert or update if email already exists\nINSERT INTO users (email, name, login_count)\nVALUES ('alice@example.com', 'Alice', 1)\nON CONFLICT (email)         -- conflict target\nDO UPDATE SET\n  name = EXCLUDED.name,     -- EXCLUDED = the row we tried to insert\n  login_count = users.login_count + 1,\n  updated_at = NOW();</pre><br><strong>Insert or ignore (do nothing on conflict):</strong><br><pre>INSERT INTO event_deduplication (event_id)\nVALUES ('evt-123')\nON CONFLICT (event_id) DO NOTHING;</pre><br><strong>Use cases:</strong><ul><li>User last-seen timestamp update</li><li>Idempotent event processing deduplication</li><li>Maintaining running counters</li><li>Syncing data from external sources</li></ul><br><strong>JPA/Spring Data:</strong> use <code>@Query</code> with native SQL, or <code>save()</code> with merge semantics (SELECT + INSERT/UPDATE — not atomic)."},

  {q:"What are materialized views and when should you use them?",d:"hard", id:24,
     a:"A materialized view is like a regular view but its query result is physically stored on disk. Unlike a regular view (which re-runs the query on every access), a materialized view is pre-computed and refreshed on demand.<br><br><strong>Create:</strong><br><pre>CREATE MATERIALIZED VIEW monthly_sales_summary AS\nSELECT\n  DATE_TRUNC('month', created_at) AS month,\n  category,\n  SUM(amount) AS total,\n  COUNT(*) AS order_count\nFROM orders\nJOIN products USING (product_id)\nGROUP BY 1, 2;\n\nCREATE INDEX ON monthly_sales_summary(month, category);</pre><br><strong>Refresh:</strong><br><pre>-- Full refresh (blocks reads by default)\nREFRESH MATERIALIZED VIEW monthly_sales_summary;\n\n-- Concurrent refresh (non-blocking, requires unique index)\nREFRESH MATERIALIZED VIEW CONCURRENTLY monthly_sales_summary;</pre><br><strong>When to use:</strong><ul><li>Expensive aggregate queries run frequently (dashboards, reports)</li><li>Read-heavy, write-light data</li><li>Acceptable to show slightly stale data (refresh every 15min)</li></ul><br><strong>When NOT to use:</strong> real-time data requirements, frequently changing source data, or when data must always be perfectly up-to-date."},

  {q:"What is table partitioning in PostgreSQL and how do you use it?",d:"hard", id:25,
     a:"Partitioning divides a large table into smaller physical pieces (partitions) based on a key. Queries that filter on the partition key only scan relevant partitions — dramatic performance improvement for time-series data.<br><br><strong>Range partitioning (by date):</strong><br><pre>CREATE TABLE events (\n  id BIGSERIAL,\n  user_id BIGINT,\n  event_type TEXT,\n  created_at TIMESTAMPTZ NOT NULL\n) PARTITION BY RANGE (created_at);\n\n-- Create partitions per month:\nCREATE TABLE events_2024_01 PARTITION OF events\n  FOR VALUES FROM ('2024-01-01') TO ('2024-02-01');\n\nCREATE TABLE events_2024_02 PARTITION OF events\n  FOR VALUES FROM ('2024-02-01') TO ('2024-03-01');</pre><br><strong>Partition pruning:</strong><br><pre>-- Only scans events_2024_01 partition:\nSELECT * FROM events\nWHERE created_at BETWEEN '2024-01-01' AND '2024-01-31';</pre><br><strong>Hash partitioning</strong> for even data distribution:<br><pre>PARTITION BY HASH (user_id);\n-- Partition 0: users where MOD(user_id, 4) = 0</pre><br><strong>Benefits:</strong> faster queries on partition key, easier archival (DROP TABLE on old partition), parallel query across partitions, smaller indexes per partition."},

  {q:"What is the difference between optimistic locking at the DB level vs application level?",d:"hard", id:26,
     a:"<strong>Database-level optimistic locking (version column):</strong><br><pre>-- Hibernate adds this automatically with @Version:\nUPDATE orders\nSET status = 'SHIPPED', version = 6\nWHERE id = 123 AND version = 5;  -- fails if someone else updated\n\n-- 0 rows affected → OptimisticLockException → retry</pre><br><strong>Database-level CAS with RETURNING:</strong><br><pre>UPDATE account\nSET balance = balance - 100\nWHERE id = 42 AND balance >= 100\nRETURNING balance;\n-- If no rows returned → balance was insufficient or row changed</pre><br><strong>Application-level optimistic check:</strong><br><pre>// Redis CAS via WATCH/MULTI/EXEC:\nredis.watch(\"inventory:item:1\");\nint qty = Integer.parseInt(redis.get(\"inventory:item:1\"));\nif (qty >= requested) {\n  Transaction tx = redis.multi();\n  tx.set(\"inventory:item:1\", String.valueOf(qty - requested));\n  List&lt;Object&gt; result = tx.exec(); // null if WATCH key changed\n  if (result == null) throw new RetryableException();\n}</pre><br><strong>Key difference:</strong> DB-level is atomic at the DB layer — safe across multiple app instances. Application-level (outside a transaction) requires careful retry logic and may not be safe under all failure modes."},

);

SEC_SQL.qs.push(
  {q:"What are UPSERT patterns in PostgreSQL and how do you implement them?",d:"medium", id:27,
     a:"UPSERT (INSERT … ON CONFLICT) atomically inserts a row or updates it if a conflict occurs on a unique key — avoiding the classic check-then-insert race condition.<br><br><strong>Basic upsert:</strong><br><pre>INSERT INTO products (id, name, price, updated_at)\nVALUES (1, 'Widget', 9.99, NOW())\nON CONFLICT (id) DO UPDATE SET\n  name       = EXCLUDED.name,\n  price      = EXCLUDED.price,\n  updated_at = EXCLUDED.updated_at;</pre><code>EXCLUDED</code> refers to the row that was <em>attempted</em> to be inserted.<br><br><strong>Insert-only if not exists:</strong><br><pre>INSERT INTO users (email, created_at)\nVALUES ('alice@example.com', NOW())\nON CONFLICT (email) DO NOTHING;</pre><br><strong>Conditional upsert:</strong><br><pre>ON CONFLICT (id) DO UPDATE SET\n  price = EXCLUDED.price\nWHERE products.price &lt;&gt; EXCLUDED.price; -- only if price changed</pre><br><strong>In Spring Data JPA:</strong><br><pre>@Modifying\n@Query(value = \"INSERT INTO products ... ON CONFLICT ... DO UPDATE ...\",\n       nativeQuery = true)\nvoid upsertProduct(...);</pre>"},

  {q:"What are materialized views and when do you use them?",d:"hard", id:28,
     a:"A <strong>materialized view</strong> stores the result of a query physically on disk — unlike a regular view which re-executes the query every time it's accessed.<br><br><strong>Create:</strong><br><pre>CREATE MATERIALIZED VIEW monthly_sales_summary AS\nSELECT\n  DATE_TRUNC('month', created_at) AS month,\n  product_id,\n  SUM(quantity) AS total_qty,\n  SUM(amount) AS total_revenue\nFROM orders\nWHERE status = 'COMPLETED'\nGROUP BY 1, 2\nWITH DATA; -- populate immediately\n\nCREATE UNIQUE INDEX ON monthly_sales_summary (month, product_id);</pre><br><strong>Refresh:</strong><br><pre>REFRESH MATERIALIZED VIEW CONCURRENTLY monthly_sales_summary;\n-- CONCURRENTLY: doesn't lock the view during refresh\n-- requires a unique index</pre><br><strong>When to use:</strong><ul><li>Dashboard/reporting queries that are slow (&gt;1s) and don't need real-time data</li><li>Complex aggregations over millions of rows</li><li>Replacing application-level caching that's hard to invalidate</li></ul><br><strong>Trade-off:</strong> stale data between refreshes. Schedule <code>REFRESH</code> via pg_cron or a Spring <code>@Scheduled</code> task. Check staleness with <code>pg_stat_user_tables.last_vacuum</code>."},

  {q:"What is table partitioning in PostgreSQL and what are the strategies?",d:"hard", id:29,
     a:"Partitioning splits a large table into smaller physical pieces while appearing as a single table to queries. Reduces index size and enables partition pruning — the query planner skips irrelevant partitions entirely.<br><br><strong>Range partitioning (most common — by date):</strong><br><pre>CREATE TABLE orders (\n  id BIGINT, created_at TIMESTAMP, amount DECIMAL\n) PARTITION BY RANGE (created_at);\n\nCREATE TABLE orders_2024_q1 PARTITION OF orders\n  FOR VALUES FROM ('2024-01-01') TO ('2024-04-01');\n\nCREATE TABLE orders_2024_q2 PARTITION OF orders\n  FOR VALUES FROM ('2024-04-01') TO ('2024-07-01');\n\n-- Query planner only scans relevant partition:\nSELECT * FROM orders WHERE created_at &gt; '2024-03-01'; -- only hits q1</pre><br><strong>List partitioning:</strong> by discrete values (region, country, status).<br><br><strong>Hash partitioning:</strong> distributes rows evenly — good for load balancing without time-series access patterns.<br><br><strong>Benefits:</strong> old partitions can be DETACHED and archived (near-instant). Indexes per partition are much smaller. VACUUM runs on individual partitions.<br><br><strong>Downsides:</strong> cross-partition queries are slower; foreign keys into partitioned tables are complex."},

  {q:"What is the difference between optimistic locking in DB vs application level?",d:"hard", id:30,
     a:"<strong>Application-level optimistic locking</strong> (Hibernate @Version): adds a <code>version</code> column, incremented on every update. The UPDATE includes <code>WHERE version = expected</code> — if 0 rows updated, another transaction beat us.<br><pre>UPDATE orders SET status='SHIPPED', version=6\nWHERE id=1 AND version=5;\n-- If 0 rows affected → OptimisticLockException</pre><br><strong>Database-level optimistic locking</strong> (compare-and-swap with timestamps or checksums):<br><pre>UPDATE orders SET status='SHIPPED', updated_at=NOW()\nWHERE id=1 AND updated_at='2024-01-15 10:30:00';\n-- Check rows affected in application code</pre><br><strong>Key differences:</strong><ul><li>Hibernate's version is more reliable — timestamps have second/millisecond granularity and can collide</li><li>DB-level works across different tech stacks sharing the same table</li><li>Hibernate version auto-handles retry logic via <code>@Retryable</code></li></ul><br><strong>When to prefer pessimistic locking:</strong> high contention (inventory reservations, seat booking) where most updates would collide — optimistic locking causes too many retries."},

  {q:"What is a database sequence and how does it differ from auto-increment?",d:"medium", id:31,
     a:"<strong>Auto-increment (SERIAL / IDENTITY):</strong> generates the next ID automatically on INSERT. Simple but has a critical limitation — the ID is generated at the DB level only, so the application doesn't know the ID until after the INSERT.<br><br><strong>Sequence:</strong> a separate DB object that generates unique integers. You call <code>NEXTVAL</code> explicitly to get the next value before or during insert.<br><pre>-- Create\nCREATE SEQUENCE order_id_seq START 1 INCREMENT 50 CACHE 100;\n\n-- Get next value\nSELECT NEXTVAL('order_id_seq'); -- returns 1, then 51, then 101...\n\n-- Use in insert\nINSERT INTO orders (id, ...) VALUES (NEXTVAL('order_id_seq'), ...);</pre><br><strong>Advantages of sequences over auto-increment:</strong><ul><li>Pre-fetch IDs in blocks (Hibernate allocationSize=50) — avoids a round-trip per insert, enables batch inserts</li><li>Can span multiple tables — single sequence for order and order_archive</li><li>Gaps are normal and expected (cached values are lost on restart)</li></ul><br>In Spring JPA: <code>@GeneratedValue(strategy=SEQUENCE)</code> uses sequences and benefits from pre-allocation."},

  {q:"What are database transactions savepoints and when do you use them?",d:"hard", id:32,
     a:"A <strong>savepoint</strong> is a named checkpoint within a transaction. You can roll back to a savepoint without rolling back the entire transaction — partial rollback.<br><br><strong>SQL syntax:</strong><br><pre>BEGIN;\n  INSERT INTO orders (customer_id) VALUES (1); -- succeeds\n\n  SAVEPOINT before_payment;\n\n  INSERT INTO payments (order_id, amount) VALUES (1, 100); -- might fail\n\n  -- If payment fails:\n  ROLLBACK TO SAVEPOINT before_payment; -- only undoes payment\n  -- Order INSERT is preserved!\n\n  -- Mark payment as pending instead:\n  UPDATE orders SET payment_status = 'PENDING' WHERE id = 1;\n\nCOMMIT; -- commits order + pending status</pre><br><strong>JPA / Spring:</strong> used internally by NESTED transaction propagation — Spring creates a JDBC savepoint when entering a nested transaction. If the nested transaction rolls back, only the savepoint is rolled back.<br><br><strong>Practical use:</strong> fault-tolerant batch processing — if one item fails, rollback to savepoint and continue with the next item without losing the whole batch."},

  {q:"What is VACUUM in PostgreSQL and why is it needed for MVCC?",d:"hard", id:33,
     a:"PostgreSQL uses MVCC (Multi-Version Concurrency Control) — when you UPDATE or DELETE a row, the old version isn't immediately removed. It's marked as dead but still takes up disk space. <code>VACUUM</code> reclaims that space.<br><br><strong>Why dead rows exist:</strong> concurrent transactions might still need to see the old version (snapshot isolation). PostgreSQL keeps all versions until no transaction can see them.<br><br><strong>VACUUM:</strong> marks dead rows as reusable space. Does NOT return space to the OS — just makes it available for future INSERTs.<br><pre>VACUUM orders; -- reclaim space in the orders table</pre><br><strong>VACUUM FULL:</strong> rewrites the entire table, returning space to the OS — but locks the table for the duration (avoid in production).<br><pre>VACUUM FULL orders; -- exclusive lock, very slow</pre><br><strong>Autovacuum (enabled by default):</strong> background process that automatically VACUUMs tables when they accumulate enough dead rows.<br><pre>autovacuum = on\nautovacuum_vacuum_threshold = 50\nautovacuum_vacuum_scale_factor = 0.2 -- vacuum when 20% of rows are dead</pre><br><strong>When to tune:</strong> high-write tables (updates/deletes) can bloat quickly — lower scale_factor or increase vacuum frequency. Monitor with <code>pg_stat_user_tables</code>."},

  {q:"How do you detect and kill long-running queries in PostgreSQL?",d:"medium", id:34,
     a:"Long-running queries can block other operations, consume resources, and indicate bugs (missing indexes, Cartesian joins).<br><br><strong>Find long-running queries:</strong><br><pre>SELECT\n  pid,\n  now() - query_start AS duration,\n  state,\n  query\nFROM pg_stat_activity\nWHERE state != 'idle'\n  AND now() - query_start > interval '5 minutes'\nORDER BY duration DESC;</pre><br><strong>Kill a specific query:</strong><br><pre>SELECT pg_cancel_backend(12345); -- graceful cancel (SIGINT)\nSELECT pg_terminate_backend(12345); -- forceful kill (SIGTERM)</pre><br><strong>Cancel all queries from a specific user:</strong><br><pre>SELECT pg_cancel_backend(pid)\nFROM pg_stat_activity\nWHERE usename = 'reporting_user' AND state = 'active';</pre><br><strong>Set statement timeout per session:</strong><br><pre>SET statement_timeout = '30s'; -- auto-cancel queries after 30s</pre><br><strong>Set timeout globally:</strong><br><pre>-- postgresql.conf\nstatement_timeout = 60000 -- 60 seconds in milliseconds</pre><br><strong>Lock conflicts:</strong> use <code>pg_locks</code> to see what's blocking:<br><pre>SELECT * FROM pg_locks WHERE NOT granted;</pre>"},

  {q:"What is the difference between BETWEEN and >=/<= on index performance?",d:"medium", id:35,
     a:"<code>BETWEEN</code> and the equivalent <code>&gt;= AND &lt;=</code> are semantically identical and produce the same query plan — the planner treats them as the same.<br><br><pre>-- These two are identical:\nSELECT * FROM orders WHERE created_at BETWEEN '2024-01-01' AND '2024-01-31';\nSELECT * FROM orders WHERE created_at >= '2024-01-01' AND created_at <= '2024-01-31';</pre><br><strong>Index usage:</strong> both use an index on <code>created_at</code> if it exists. The planner chooses an index scan or seq scan based on selectivity.<br><br><strong>Gotcha — BETWEEN is inclusive on both ends:</strong><br><pre>created_at BETWEEN '2024-01-01' AND '2024-01-31'\n-- Includes rows where created_at = '2024-01-31 00:00:00'\n-- Does NOT include '2024-01-31 23:59:59'</pre><br>For date ranges, prefer explicit bounds:<br><pre>created_at >= '2024-01-01' AND created_at < '2024-02-01' -- clearer</pre><br><strong>Performance tip:</strong> the <code>created_at < '2024-02-01'</code> form is slightly better than <code>&lt;= '2024-01-31'</code> because it avoids time zone / timestamp precision issues and is easier to reason about (exclusive upper bound)."},

  {q:"What are LATERAL joins in PostgreSQL and when are they needed?",d:"hard", id:36,
     a:"<code>LATERAL</code> allows a subquery in the FROM clause to reference columns from preceding tables in the same FROM list — like a correlated subquery but in the FROM clause.<br><br><strong>Problem without LATERAL:</strong> you can't reference earlier tables in a subquery in FROM.<br><pre>-- This FAILS:\nSELECT c.name, top_orders.*\nFROM customers c,\n     (SELECT * FROM orders o WHERE o.customer_id = c.id LIMIT 3) top_orders;\n-- ERROR: c.id not available in subquery</pre><br><strong>LATERAL solves it:</strong><br><pre>SELECT c.name, top_orders.*\nFROM customers c,\nLATERAL (\n  SELECT * FROM orders o\n  WHERE o.customer_id = c.id\n  ORDER BY created_at DESC\n  LIMIT 3\n) top_orders;</pre>For each customer row, the LATERAL subquery is evaluated with <code>c.id</code> available.<br><br><strong>Equivalent with LEFT JOIN LATERAL:</strong><br><pre>SELECT c.name, o.*\nFROM customers c\nLEFT JOIN LATERAL (\n  SELECT * FROM orders WHERE customer_id = c.id LIMIT 3\n) o ON true;</pre><br><strong>Use cases:</strong> top-N per group, complex aggregations per row, calling a function per row that needs row-specific parameters."},

  {q:"How do you do schema migrations with Flyway vs Liquibase?",d:"medium", id:37,
     a:"Both are schema migration tools that version-control your database — apply incremental SQL or Java-based changes, track applied migrations, and prevent drift.<br><br><strong>Flyway (simpler, SQL-first):</strong><br><pre>src/main/resources/db/migration/\n  V1__create_users_table.sql\n  V2__add_email_column.sql\n  V3__create_orders_table.sql</pre>Naming: <code>V{version}__{description}.sql</code>. Flyway applies them in version order, records each in <code>flyway_schema_history</code> table.<br><br><strong>Spring Boot config:</strong><br><pre>spring.flyway.enabled=true\nspring.flyway.locations=classpath:db/migration\nspring.flyway.baseline-on-migrate=true</pre><br><strong>Liquibase (more features, XML/YAML/JSON/SQL):</strong><br><pre>&lt;changeSet id=\"1\" author=\"alice\"&gt;\n  &lt;createTable tableName=\"users\"&gt;\n    &lt;column name=\"id\" type=\"bigint\" autoIncrement=\"true\"&gt;\n      &lt;constraints primaryKey=\"true\"/&gt;\n    &lt;/column&gt;\n    &lt;column name=\"email\" type=\"varchar(255)\"/&gt;\n  &lt;/createTable&gt;\n&lt;/changeSet&gt;</pre><br><strong>Advantages:</strong><ul><li><strong>Flyway:</strong> dead simple, SQL-based, minimal config, great for small teams</li><li><strong>Liquibase:</strong> DB-agnostic (same changelog for Postgres/MySQL), rollback support, preconditions, better for complex multi-DB systems</li></ul><br><strong>Both:</strong> fail-fast on startup if migration fails — prevent deploying with schema drift."},

  {q:"What is the difference between a database sequence vs SERIAL/BIGSERIAL?",d:"medium", id:38,
     a:"<code>SERIAL</code> is PostgreSQL shorthand that auto-creates a sequence and sets the column default to <code>NEXTVAL</code>. A standalone <code>SEQUENCE</code> gives you more control.<br><br><strong>SERIAL (convenience macro):</strong><br><pre>CREATE TABLE orders (\n  id SERIAL PRIMARY KEY,  -- auto-creates orders_id_seq\n  amount DECIMAL\n);</pre>Behind the scenes, PostgreSQL creates:<br><pre>CREATE SEQUENCE orders_id_seq;\nCREATE TABLE orders (\n  id INTEGER NOT NULL DEFAULT NEXTVAL('orders_id_seq'),\n  amount DECIMAL\n);\nALTER SEQUENCE orders_id_seq OWNED BY orders.id;</pre><br><strong>Standalone SEQUENCE (explicit control):</strong><br><pre>CREATE SEQUENCE shared_id_seq START 1000 INCREMENT 50 CACHE 100;\n\nCREATE TABLE orders (\n  id BIGINT DEFAULT NEXTVAL('shared_id_seq') PRIMARY KEY,\n  amount DECIMAL\n);\n\nCREATE TABLE invoices (\n  id BIGINT DEFAULT NEXTVAL('shared_id_seq') PRIMARY KEY,\n  order_id BIGINT\n);</pre>Now orders and invoices share a sequence — globally unique IDs across tables.<br><br><strong>When to use explicit SEQUENCE:</strong><ul><li>Share a sequence across multiple tables</li><li>Pre-fetch IDs in blocks (Hibernate allocationSize)</li><li>Need custom START or INCREMENT</li></ul><br><strong>BIGSERIAL vs SERIAL:</strong> SERIAL is INT (max 2 billion), BIGSERIAL is BIGINT (max 9 quintillion) — always use BIGSERIAL in production."}
);