const SEC_MAVEN = {
  id:"maven",title:"Maven & Build Tools",icon:"🔧",ib:"rgba(248,113,113,.1)",ic:"#f87171",
  grp:"java",bar:"#f87171",tglow:"rgba(248,113,113,.06)",
  desc:"POM, lifecycle phases, dependency scopes, BOM, plugins, and Maven vs Gradle.",
  tags:["POM","Lifecycle","BOM","Scopes","Plugins","Gradle"],
  qs:[
    {q:"What are the Maven build lifecycle phases in order?",d:"medium", id:1,
     a:"Maven has three built-in lifecycles. The <strong>default</strong> lifecycle (most important):<br><br><code>validate</code> → <code>initialize</code> → <code>generate-sources</code> → <code>compile</code> → <code>test-compile</code> → <code>test</code> → <code>package</code> → <code>verify</code> → <code>install</code> → <code>deploy</code><br><br>Running a phase executes <em>all prior phases</em> first.<br><br><strong>Common commands:</strong><ul><li><code>mvn compile</code> — compile source only</li><li><code>mvn test</code> — compile + run tests</li><li><code>mvn package</code> — compile + test + create JAR/WAR</li><li><code>mvn install</code> — package + copy to ~/.m2 local repo</li><li><code>mvn deploy</code> — install + push to remote repository (Nexus, Artifactory)</li><li><code>mvn clean</code> — delete target/ directory (separate lifecycle)</li></ul><br><strong>Skip tests:</strong> <code>mvn package -DskipTests</code> (skip execution) or <code>-Dmaven.test.skip=true</code> (skip compilation too)."},

    {q:"What is a BOM (Bill of Materials) and why does Spring Boot use one?",d:"medium", id:2,
     a:"A BOM is a special Maven POM with a <code>&lt;dependencyManagement&gt;</code> section that declares a curated, version-compatible set of dependencies — without being a parent POM.<br><br><strong>Import a BOM:</strong><br><pre>&lt;dependencyManagement&gt;\n  &lt;dependencies&gt;\n    &lt;dependency&gt;\n      &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;\n      &lt;artifactId&gt;spring-boot-dependencies&lt;/artifactId&gt;\n      &lt;version&gt;3.2.0&lt;/version&gt;\n      &lt;type&gt;pom&lt;/type&gt;\n      &lt;scope&gt;import&lt;/scope&gt;\n    &lt;/dependency&gt;\n  &lt;/dependencies&gt;\n&lt;/dependencyManagement&gt;</pre><br><strong>Result:</strong> you can declare Spring Boot dependencies without specifying versions — the BOM provides them:<br><pre>&lt;dependency&gt;\n  &lt;groupId&gt;com.fasterxml.jackson.core&lt;/groupId&gt;\n  &lt;artifactId&gt;jackson-databind&lt;/artifactId&gt;\n  &lt;!-- no version needed -- Spring BOM provides it --&gt;\n&lt;/dependency&gt;</pre><br><strong>Why it matters:</strong> Spring Boot manages ~300 dependency versions that are all tested together. Without the BOM, you'd get version conflicts between Jackson, Hibernate, Tomcat, etc."},

    {q:"What is the difference between Maven and Gradle?",d:"medium", id:3,
     a:"<strong>Maven:</strong><ul><li>XML configuration (pom.xml) — verbose but consistent</li><li>Rigid, opinionated lifecycle — predictable, convention over configuration</li><li>Large ecosystem — every library publishes Maven coordinates</li><li>Slower for large multi-module projects</li></ul><br><strong>Gradle:</strong><ul><li>Groovy or Kotlin DSL (build.gradle / build.gradle.kts) — concise, type-safe (Kotlin)</li><li>Flexible — build tasks are a DAG, not a fixed lifecycle</li><li>Incremental builds — only rebuild what changed</li><li>Build cache — skip tasks whose inputs/outputs haven't changed (even across machines)</li><li>Parallel task execution — significantly faster for large projects</li><li>Gradle daemon stays warm between builds</li></ul><br><strong>Performance comparison:</strong> for a 50-module project, Gradle can be 5–10× faster than Maven due to incrementalism and caching.<br><br><strong>When to choose:</strong> Maven for simple/standard Java projects and when team familiarity matters. Gradle for Android (required), large multi-module monorepos, and when build performance is critical."}
  ]
};

SEC_MAVEN.qs.push(
  {q:"What is the Maven dependency scope and what does each mean?",d:"medium", id:4,
     a:"Dependency scopes control when a dependency is available on the classpath:<br><br><strong>compile</strong> (default): available in all phases — compile, test, and at runtime. Included in the packaged JAR's classpath.<br><br><strong>provided</strong>: available at compile and test time, but NOT included in the package — the runtime environment provides it (e.g., Servlet API in a WAR deployed to Tomcat; Tomcat provides the API, so you don't bundle it).<br><br><strong>runtime</strong>: not needed for compilation but needed at runtime (e.g., JDBC driver — you compile against the java.sql API, the driver is loaded at runtime).<br><br><strong>test</strong>: only available for test compilation and execution — never included in the packaged artifact. JUnit, Mockito, Testcontainers.<br><br><strong>system</strong>: like provided but you specify the JAR path explicitly — avoid it.<br><br><strong>import</strong>: only used in <code>&lt;dependencyManagement&gt;</code> to import a BOM.<br><br><strong>Practical tip:</strong> always use <code>test</code> scope for testing libraries — accidentally including JUnit in your production JAR wastes space and can cause classpath conflicts."}
);

SEC_MAVEN.qs.push(
  {q:"How do you structure a Maven multi-module project?",d:"medium", id:5,
     a:"Multi-module projects split a large codebase into logical, independently-buildable modules while sharing a common parent POM.<br><br><strong>Structure:</strong><br><pre>my-app/                    ← parent POM\n├── pom.xml\n├── my-app-api/            ← shared DTOs and interfaces\n│   └── pom.xml\n├── my-app-core/           ← business logic (depends on api)\n│   └── pom.xml\n├── my-app-persistence/    ← DB layer (depends on core)\n│   └── pom.xml\n└── my-app-web/            ← Spring Boot app (depends on all)\n    └── pom.xml</pre><br><strong>Parent pom.xml:</strong><br><pre>&lt;packaging&gt;pom&lt;/packaging&gt;\n&lt;modules&gt;\n  &lt;module&gt;my-app-api&lt;/module&gt;\n  &lt;module&gt;my-app-core&lt;/module&gt;\n  &lt;module&gt;my-app-persistence&lt;/module&gt;\n  &lt;module&gt;my-app-web&lt;/module&gt;\n&lt;/modules&gt;\n\n&lt;dependencyManagement&gt;\n  &lt;!-- Spring Boot BOM + shared version management --&gt;\n&lt;/dependencyManagement&gt;</pre><br><strong>Benefits:</strong> build only changed modules (<code>mvn -pl my-app-core -am install</code>), enforce layered architecture (persistence can't import web), separate release cycles for shared APIs."},

  {q:"How do you resolve Maven dependency conflicts?",d:"medium", id:6,
     a:"When two dependencies transitively bring in different versions of the same library, Maven uses <strong>nearest wins</strong> — the version closest to your project in the dependency tree is used. This can silently pull in the wrong version.<br><br><strong>Diagnose conflicts:</strong><br><pre>mvn dependency:tree -Dincludes=com.fasterxml.jackson.core:jackson-databind\n# Shows full dependency tree for jackson-databind\n\nmvn dependency:analyze\n# Reports unused declared and used undeclared dependencies</pre><br><strong>Force a specific version (explicit declaration wins):</strong><br><pre>&lt;dependencies&gt;\n  &lt;dependency&gt;\n    &lt;groupId&gt;com.fasterxml.jackson.core&lt;/groupId&gt;\n    &lt;artifactId&gt;jackson-databind&lt;/artifactId&gt;\n    &lt;version&gt;2.16.1&lt;/version&gt; &lt;!-- overrides transitive --&gt;\n  &lt;/dependency&gt;\n&lt;/dependencies&gt;</pre><br><strong>Exclude transitive dependency:</strong><br><pre>&lt;dependency&gt;\n  &lt;groupId&gt;some.library&lt;/groupId&gt;\n  &lt;artifactId&gt;some-lib&lt;/artifactId&gt;\n  &lt;exclusions&gt;\n    &lt;exclusion&gt;\n      &lt;groupId&gt;com.fasterxml.jackson.core&lt;/groupId&gt;\n      &lt;artifactId&gt;jackson-databind&lt;/artifactId&gt;\n    &lt;/exclusion&gt;\n  &lt;/exclusions&gt;\n&lt;/dependency&gt;</pre><br>The Spring Boot BOM avoids most conflicts by pre-aligning all Spring library versions."},

  {q:"What is the Maven Wrapper (mvnw) and why should you use it?",d:"easy", id:7,
     a:"The Maven Wrapper bundles a specific Maven version with your project — anyone checking out the code runs the exact same Maven version without installing it globally.<br><br><strong>Generate it:</strong><br><pre>mvn wrapper:wrapper -Dmaven=3.9.6\n# Creates:\n# mvnw         (Unix shell script)\n# mvnw.cmd     (Windows batch script)\n# .mvn/wrapper/maven-wrapper.properties  (version config)</pre><br><strong>Use it:</strong><br><pre>./mvnw clean install    # Unix\nmvnw.cmd clean install  # Windows</pre><br><strong>Why it matters:</strong><ul><li><strong>Reproducibility:</strong> CI/CD and all developers use the same Maven version — no \"works on my machine\" due to different Maven versions</li><li><strong>No global install needed:</strong> new team members just clone and run</li><li><strong>Version pinning:</strong> upgrade Maven version in one place (<code>maven-wrapper.properties</code>), everyone gets it on next pull</li></ul><br>Commit the <code>mvnw</code>, <code>mvnw.cmd</code>, and <code>.mvn/</code> directory to Git. The wrapper JAR can be distributed or downloaded at runtime."}
);

SEC_MAVEN.qs.push(
  {q:"How do you structure a Maven multi-module project?",d:"medium", id:8,
     a:"Multi-module projects group related modules under a single build — shared versioning, single <code>mvn build</code>, and cross-module dependency management.<br><br><strong>Structure:</strong><br><pre>my-app/                    ← parent POM\n├── pom.xml                ← packaging=pom, declares modules\n├── my-app-api/            ← shared DTOs, interfaces\n│   └── pom.xml\n├── my-app-domain/         ← business logic (depends on api)\n│   └── pom.xml\n├── my-app-infrastructure/ ← DB, messaging (depends on domain)\n│   └── pom.xml\n└── my-app-web/            ← controllers, Spring Boot app\n    └── pom.xml</pre><br><strong>Parent POM:</strong><br><pre>&lt;groupId&gt;com.myapp&lt;/groupId&gt;\n&lt;artifactId&gt;my-app-parent&lt;/artifactId&gt;\n&lt;version&gt;1.0.0-SNAPSHOT&lt;/version&gt;\n&lt;packaging&gt;pom&lt;/packaging&gt;\n\n&lt;modules&gt;\n  &lt;module&gt;my-app-api&lt;/module&gt;\n  &lt;module&gt;my-app-domain&lt;/module&gt;\n  &lt;module&gt;my-app-web&lt;/module&gt;\n&lt;/modules&gt;\n\n&lt;dependencyManagement&gt;\n  &lt;!-- Import Spring BOM + manage internal module versions --&gt;\n&lt;/dependencyManagement&gt;</pre><br><strong>Build from root:</strong> <code>mvn clean install</code> builds all modules in dependency order. <code>mvn -pl my-app-web -am</code> builds only the web module and its dependencies."},

  {q:"How do you resolve Maven dependency conflicts?",d:"hard", id:9,
     a:"When two transitive dependencies pull in different versions of the same library, Maven uses nearest-wins (shortest path from root) — which may not be the version you want.<br><br><strong>Detect conflicts:</strong><br><pre>mvn dependency:tree -Dverbose | grep ':omitted'\n# Shows which versions were omitted and why\n\nmvn dependency:analyze\n# Shows unused declared + used undeclared dependencies</pre><br><strong>Force a specific version:</strong><br><pre>&lt;!-- Declare explicitly in your POM — always wins over transitive --&gt;\n&lt;dependency&gt;\n  &lt;groupId&gt;com.fasterxml.jackson.core&lt;/groupId&gt;\n  &lt;artifactId&gt;jackson-databind&lt;/artifactId&gt;\n  &lt;version&gt;2.16.0&lt;/version&gt; &lt;!-- locks version --&gt;\n&lt;/dependency&gt;</pre><br><strong>Or use dependencyManagement (preferred in multi-module):</strong><br><pre>&lt;dependencyManagement&gt;\n  &lt;dependencies&gt;\n    &lt;dependency&gt;\n      &lt;groupId&gt;com.fasterxml.jackson.core&lt;/groupId&gt;\n      &lt;artifactId&gt;jackson-databind&lt;/artifactId&gt;\n      &lt;version&gt;2.16.0&lt;/version&gt;\n    &lt;/dependency&gt;\n  &lt;/dependencies&gt;\n&lt;/dependencyManagement&gt;</pre><br><strong>Exclude transitive dependency:</strong><br><pre>&lt;dependency&gt;\n  &lt;groupId&gt;some.library&lt;/groupId&gt;\n  &lt;artifactId&gt;lib&lt;/artifactId&gt;\n  &lt;exclusions&gt;\n    &lt;exclusion&gt;\n      &lt;groupId&gt;org.slf4j&lt;/groupId&gt;\n      &lt;artifactId&gt;*&lt;/artifactId&gt;\n    &lt;/exclusion&gt;\n  &lt;/exclusions&gt;\n&lt;/dependency&gt;</pre>"}
);

SEC_MAVEN.qs.push(
  {q:"What is a Maven multi-module project and how do you structure one?",d:"medium", id:10,
     a:"A multi-module project has a parent POM that coordinates multiple sub-modules — each a separate Maven project that can be built together or independently.<br><br><strong>Parent POM structure:</strong><br><pre>&lt;!-- pom.xml (parent) --&gt;\n&lt;groupId&gt;com.myapp&lt;/groupId&gt;\n&lt;artifactId&gt;myapp-parent&lt;/artifactId&gt;\n&lt;packaging&gt;pom&lt;/packaging&gt;\n\n&lt;modules&gt;\n  &lt;module&gt;myapp-api&lt;/module&gt;       &lt;!-- shared DTOs --&gt;\n  &lt;module&gt;myapp-domain&lt;/module&gt;    &lt;!-- business logic --&gt;\n  &lt;module&gt;myapp-data&lt;/module&gt;      &lt;!-- repositories --&gt;\n  &lt;module&gt;myapp-web&lt;/module&gt;       &lt;!-- controllers, Spring Boot main --&gt;\n&lt;/modules&gt;\n\n&lt;!-- Centralise versions for all modules --&gt;\n&lt;dependencyManagement&gt; ... &lt;/dependencyManagement&gt;\n&lt;build&gt;&lt;pluginManagement&gt; ... &lt;/pluginManagement&gt;&lt;/build&gt;</pre><br><strong>Child module references parent:</strong><br><pre>&lt;parent&gt;\n  &lt;groupId&gt;com.myapp&lt;/groupId&gt;\n  &lt;artifactId&gt;myapp-parent&lt;/artifactId&gt;\n  &lt;version&gt;1.0-SNAPSHOT&lt;/version&gt;\n&lt;/parent&gt;</pre><br><strong>Build all modules:</strong> <code>mvn install</code> from parent directory — Maven builds in dependency order.<br><br><strong>Benefits:</strong> shared dependency versions, build only changed modules (<code>-pl myapp-web -am</code>), clear architectural boundaries enforced by module dependencies."},

  {q:"How do you resolve Maven dependency conflicts?",d:"medium", id:11,
     a:"<strong>Dependency mediation:</strong> when two dependencies require different versions of the same transitive dependency, Maven uses the <em>nearest definition wins</em> rule — the version closest to your project in the dependency tree wins.<br><br><strong>Diagnose conflicts:</strong><br><pre>mvn dependency:tree           # show full dependency tree\nmvn dependency:tree -Dincludes=com.fasterxml.jackson.core  # filter specific artifact\nmvn dependency:analyze        # find unused declared and used undeclared deps</pre><br><strong>Resolve by explicit declaration:</strong><br><pre>&lt;!-- Force a specific version, wins over all transitive --&gt;\n&lt;dependency&gt;\n  &lt;groupId&gt;com.fasterxml.jackson.core&lt;/groupId&gt;\n  &lt;artifactId&gt;jackson-databind&lt;/artifactId&gt;\n  &lt;version&gt;2.16.1&lt;/version&gt;\n&lt;/dependency&gt;</pre><br><strong>Exclude a specific transitive dependency:</strong><br><pre>&lt;dependency&gt;\n  &lt;groupId&gt;com.library&lt;/groupId&gt;\n  &lt;artifactId&gt;library&lt;/artifactId&gt;\n  &lt;exclusions&gt;\n    &lt;exclusion&gt;\n      &lt;groupId&gt;org.old&lt;/groupId&gt;\n      &lt;artifactId&gt;old-lib&lt;/artifactId&gt;\n    &lt;/exclusion&gt;\n  &lt;/exclusions&gt;\n&lt;/dependency&gt;</pre><br>Spring Boot BOM avoids most conflicts by pre-testing compatible version sets."}
);