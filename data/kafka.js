const SEC_KAFKA = {
  id:"kafka",title:"Apache Kafka",icon:"📨",ib:"rgba(251,191,36,.12)",ic:"#fbbf24",
  grp:"infra",bar:"#fbbf24",tglow:"rgba(251,191,36,.07)",
  desc:"Topics, partitions, consumer groups, delivery semantics, rebalancing, consumer lag, and producer tuning.",
  tags:["Partitions","Consumer groups","Exactly-once","Consumer lag","Rebalancing","Producer config"],
  qs:[
    {q:"What is Kafka and how does it differ from traditional message queues?",d:"medium",
     a:"Kafka is a distributed event streaming platform. Key differences from traditional queues (RabbitMQ, SQS):<br><br><strong>Message retention:</strong> messages are NOT deleted on consumption — retained for a configurable period (hours, days, weeks). Multiple consumer groups can independently replay the same events from any offset.<br><br><strong>Pull model:</strong> consumers poll at their own pace. Traditional queues push to consumers. Pull enables consumers to process at different speeds without overwhelming them.<br><br><strong>Ordering:</strong> strictly ordered within a partition; no global ordering across partitions.<br><br><strong>Throughput:</strong> millions of events/second via sequential disk writes, batching, and compression.<br><br><strong>Use Kafka for:</strong> event sourcing, audit logs, stream processing, fan-out to multiple systems, replay/reprocessing of historical data.<br><br><strong>Use traditional queues for:</strong> simple task queuing, complex routing (RabbitMQ exchanges), per-message acknowledgement, low-throughput RPC-style messaging."},

    {q:"What is a topic, partition, and offset?",d:"medium",
     a:"<strong>Topic:</strong> a named, append-only stream of records — conceptually like a database table for events. Producers write to topics; consumers read from topics.<br><br><strong>Partition:</strong> a topic is split into N ordered, immutable partitions. Each partition is an independent, sequentially-written log. Parallelism is bounded by the number of partitions — max one consumer per partition in a group.<br><br><strong>Offset:</strong> a monotonically increasing integer index within a partition. Uniquely identifies a record's position. Consumers commit their offset to mark progress.<br><br><strong>Partition key:</strong> determines which partition a message goes to via <code>hash(key) % numPartitions</code>. Same key → same partition → ordered delivery per key. Example: all events for a given <code>orderId</code> go to the same partition → processed in order.<br><br><strong>Replica:</strong> each partition has one leader and N-1 followers (replicas) on different brokers. Only the leader handles reads/writes; followers replicate. If the leader fails, a follower becomes the new leader."},

    {q:"What are consumer groups and how does partition assignment work?",d:"medium",
     a:"A <strong>consumer group</strong> is a logical subscriber to a topic. Within a group, each partition is assigned to exactly one consumer — enabling parallel processing without duplicate consumption.<br><br><strong>Scaling rules:</strong><ul><li>2 partitions, 1 consumer → consumer reads both</li><li>2 partitions, 2 consumers → one partition each (optimal)</li><li>2 partitions, 3 consumers → one consumer is idle (no partition to assign)</li></ul><br><strong>Multiple groups:</strong> each group maintains its own offset and independently consumes the full topic. Group A and Group B both read all events — fan-out at no extra cost.<br><br><strong>Assignment strategies:</strong><ul><li><code>RangeAssignor</code> (default): contiguous partition ranges per consumer</li><li><code>RoundRobinAssignor</code>: distribute evenly across consumers</li><li><code>StickyAssignor</code>: like RoundRobin but minimises partition movement on rebalance</li><li><code>CooperativeStickyAssignor</code>: incremental rebalance — best for production</li></ul>"},

    {q:"What are the three delivery semantics and how do you achieve exactly-once?",d:"hard",
     a:"<strong>At-most-once:</strong> commit offset before processing. Message lost if consumer crashes after commit but before processing. Fastest — use for metrics, logging where some loss is acceptable.<br><br><strong>At-least-once:</strong> process message, then commit offset. If consumer crashes after processing but before commit, message is reprocessed. Duplicates possible. <em>Default behaviour.</em> Make your consumer idempotent to handle duplicates safely.<br><br><strong>Exactly-once (EOS):</strong> requires three components:<ol><li><strong>Idempotent producer:</strong> <code>enable.idempotence=true</code> — broker deduplicates retries using sequence numbers per partition</li><li><strong>Kafka Transactions:</strong> <code>transactional.id=my-producer-1</code> — atomically write to multiple partitions</li><li><strong>Consumer with read_committed:</strong> <code>isolation.level=read_committed</code> — only sees committed transactional messages</li></ol><br>Exactly-once within Kafka-to-Kafka pipelines. For Kafka-to-DB: use the outbox pattern or idempotent DB writes (INSERT ... ON CONFLICT DO NOTHING with a deduplication key)."},

    {q:"What is consumer rebalancing and how do you minimise disruption?",d:"hard",
     a:"<strong>Rebalancing</strong> is triggered when the consumer group composition changes — a consumer joins, leaves, or is considered dead. Partitions are reassigned across the group.<br><br><strong>Eager rebalance (default RANGE/ROUND_ROBIN):</strong> all consumers stop processing, revoke all partitions, new assignment is computed, consumers resume. Stop-the-world pause proportional to the number of partitions.<br><br><strong>Cooperative incremental rebalance</strong> (<code>CooperativeStickyAssignor</code>): only partitions that need to move are revoked and reassigned. Consumers not involved keep processing — minimal disruption.<br><br><strong>Configuration to minimise false rebalances:</strong><ul><li><code>session.timeout.ms</code>: max time between heartbeats before consumer is considered dead (default 45s)</li><li><code>heartbeat.interval.ms</code>: heartbeat frequency — set to ⅓ of session.timeout</li><li><code>max.poll.interval.ms</code>: max time between polls — must exceed your processing time</li></ul><br><strong>ConsumerRebalanceListener:</strong> implement <code>onPartitionsRevoked()</code> to commit offsets before losing partitions."},

    {q:"What is consumer lag and how do you handle it?",d:"medium",
     a:"<strong>Consumer lag</strong> = latest offset in a partition − consumer's last committed offset.<br><br>Lag of 0 = consumer is keeping up. High lag = consumers are falling behind producers.<br><br><strong>Monitor lag:</strong><br><pre>kafka-consumer-groups.sh \\\n  --bootstrap-server kafka:9092 \\\n  --describe --group my-group</pre>Or expose via JMX metrics → Prometheus/Grafana/Datadog. Set an alert when lag exceeds your SLO threshold (e.g., >10,000 messages or >5 minutes of lag).<br><br><strong>Causes and fixes:</strong><ul><li>Slow processing → optimise consumer logic, batch DB writes</li><li>Not enough consumers → add consumer instances (up to partition count)</li><li>Not enough partitions → increase partition count (requires rebalance, check ordering implications)</li><li>Downstream bottleneck → use async processing inside consumer</li></ul><br><strong>Catch-up strategy:</strong> temporarily scale consumer instances horizontally. Once lag is cleared, scale back down."},

    {q:"What are important Kafka producer configurations for throughput and durability?",d:"hard",
     a:"<strong>Durability (acks):</strong><ul><li><code>acks=0</code> — fire and forget. No durability. Maximum throughput.</li><li><code>acks=1</code> — leader acknowledges. Lost if leader crashes before replication.</li><li><code>acks=all</code> — leader + all in-sync replicas acknowledge. Most durable. Use with <code>min.insync.replicas=2</code>.</li></ul><br><strong>Throughput:</strong><ul><li><code>batch.size=65536</code> (64KB) — larger batches = fewer requests</li><li><code>linger.ms=10</code> — wait up to 10ms to fill a batch (trade latency for throughput)</li><li><code>compression.type=lz4</code> (or zstd for best ratio) — reduces network I/O and disk usage</li><li><code>buffer.memory=67108864</code> (64MB) — total producer buffer</li></ul><br><strong>Reliability:</strong><ul><li><code>enable.idempotence=true</code> — deduplicates retried messages (required for exactly-once)</li><li><code>retries=Integer.MAX_VALUE</code> with <code>max.in.flight.requests.per.connection=5</code> (when idempotent)</li><li><code>delivery.timeout.ms=120000</code> — total time to deliver a batch</li></ul>"},

    {q:"How do you handle poison pill messages in Kafka?",d:"hard",
     a:"A <strong>poison pill</strong> is a message that causes the consumer to throw an exception every time it's processed, blocking all subsequent messages in the partition forever (Kafka preserves order — you can't skip ahead).<br><br><strong>Handling strategies:</strong><br><br><strong>1. Dead Letter Topic (DLT) — recommended:</strong><br><pre>@Bean\nDeadLetterPublishingRecoverer recoverer(KafkaTemplate&lt;?,?&gt; t) {\n  return new DeadLetterPublishingRecoverer(t,\n    (r, e) -&gt; new TopicPartition(r.topic() + \".DLT\", r.partition()));\n}\n@Bean\nDefaultErrorHandler handler(DeadLetterPublishingRecoverer r) {\n  return new DefaultErrorHandler(r, new FixedBackOff(1000L, 3));\n}</pre><br>After 3 retries: publish message + error details to <code>orders.DLT</code>. Monitor DLT for manual reprocessing or analysis.<br><br><strong>2. Retry topics with exponential delay:</strong> publish to <code>topic.retry.1</code> (1s delay) → <code>topic.retry.2</code> (10s) → <code>topic.retry.3</code> (60s) → DLT. Spring Kafka supports this via <code>RetryTopicConfiguration</code>."}
  ]
};

SEC_KAFKA.qs.push(
  {q:"What is log compaction in Kafka and when do you use it?",d:"hard",
   a:"<strong>Log compaction</strong> is an alternative to time-based retention. Instead of deleting messages older than N days, Kafka retains the <em>most recent value for each key</em> indefinitely — older duplicates are removed.<br><br><strong>Result:</strong> a compacted topic contains the full current state for every key — it's a key-value store where the log is the source of truth.<br><br><strong>Use cases:</strong><ul><li>User profile snapshots — always have the latest profile, not history</li><li>Database Change Data Capture (CDC) — latest row state per primary key</li><li>Kafka Streams KTable materialization — rebuild state from compacted topic after restart</li></ul><br><strong>How it works:</strong> a background compaction thread periodically scans log segments. For any key with multiple entries, all but the latest are deleted. A <em>tombstone</em> (value=null) marks a key as deleted.<br><br><strong>Configure:</strong><br><pre>log.cleanup.policy=compact\nlog.min.cleanable.dirty.ratio=0.5\nlog.segment.bytes=1073741824</pre>"},

  {q:"What is the difference between Kafka and traditional database replication?",d:"medium",
   a:"<strong>Database replication</strong> (PostgreSQL streaming replication, MySQL binlog replication):<ul><li>Replicates the internal data format (WAL/binlog) — tightly coupled to the DB engine</li><li>Replica is a full copy of the primary — same schema, same query capability</li><li>Primarily for HA and read scaling</li><li>Consumers must be a DB replica (same engine)</li></ul><br><strong>Kafka (event streaming):</strong><ul><li>Services publish domain events in a business-meaningful format (JSON, Avro)</li><li>Any consumer can subscribe — services in other languages, analytics tools, data warehouses</li><li>Decoupled: producer and consumer evolve independently</li><li>Can fan-out to many heterogeneous consumers simultaneously</li><li>Supports replay, time-travel, and audit log use cases</li></ul><br><strong>Debezium (CDC):</strong> bridges both worlds — reads the DB WAL and publishes each change as a Kafka event. Downstream consumers get reliable, ordered change events without the producer making any code changes."}
);

SEC_KAFKA.qs.push(
  {q:"What is Schema Registry and why is it important with Kafka?",d:"hard",
   a:"A Schema Registry (Confluent Schema Registry) is a centralised store for Avro/Protobuf/JSON schemas used by Kafka producers and consumers.<br><br><strong>Problem without it:</strong> producers and consumers must agree on the message format out-of-band. If a producer changes the schema (adds/removes fields), consumers that don't know about the change will fail.<br><br><strong>How it works:</strong><ol><li>Producer registers the schema; receives a schema ID</li><li>Message sent as: <code>[magic byte][4-byte schema ID][avro bytes]</code></li><li>Consumer reads schema ID, fetches schema from registry, deserialises</li></ol><br><strong>Spring Boot setup:</strong><br><pre>spring:\n  kafka:\n    producer:\n      value-serializer: io.confluent.kafka.serializers.KafkaAvroSerializer\n    consumer:\n      value-deserializer: io.confluent.kafka.serializers.KafkaAvroDeserializer\n    properties:\n      schema.registry.url: http://schema-registry:8081</pre><br><strong>Compatibility modes:</strong><ul><li><code>BACKWARD</code>: new schema can read old data (add optional fields, remove fields)</li><li><code>FORWARD</code>: old schema can read new data</li><li><code>FULL</code>: both directions</li></ul>Schema Registry enforces these rules on every registration — preventing breaking changes from being deployed."},

  {q:"What is Kafka Connect and how does it simplify data integration?",d:"medium",
   a:"Kafka Connect is a framework for scalably and reliably streaming data between Kafka and other systems — without writing custom producer/consumer code.<br><br><strong>Two connector types:</strong><ul><li><strong>Source connectors:</strong> pull data FROM a system INTO Kafka (JDBC source, Debezium CDC, S3, REST API)</li><li><strong>Sink connectors:</strong> push data FROM Kafka TO a system (Elasticsearch sink, S3 sink, JDBC sink, MongoDB)</li></ul><br><strong>Example — Debezium PostgreSQL CDC source connector config:</strong><br><pre>{\n  \"name\": \"postgres-cdc\",\n  \"config\": {\n    \"connector.class\": \"io.debezium.connector.postgresql.PostgresConnector\",\n    \"database.hostname\": \"postgres\",\n    \"database.port\": \"5432\",\n    \"database.user\": \"replicator\",\n    \"database.dbname\": \"mydb\",\n    \"table.include.list\": \"public.orders,public.products\",\n    \"topic.prefix\": \"mydb\"\n  }\n}</pre><br>This streams every INSERT/UPDATE/DELETE from the orders and products tables to Kafka topics — no code changes needed in your application.<br><br><strong>Benefits:</strong> declarative configuration, distributed execution, automatic restart on failure, exactly-once semantics with transactional sinks."},

  {q:"How do you monitor Kafka consumer health in production?",d:"medium",
   a:"<strong>Key metrics to monitor:</strong><br><br><strong>1. Consumer lag</strong> (most important):<br><pre>kafka-consumer-groups.sh --bootstrap-server kafka:9092 \\\n  --describe --group my-group\n\n# GROUP        TOPIC    PARTITION  CURRENT-OFFSET  LOG-END-OFFSET  LAG\n# order-group  orders   0          1000            1050            50</pre><br>Alert when lag > your SLO threshold (e.g., 5 minutes of lag or 10,000 messages).<br><br><strong>2. Prometheus/JMX metrics via micrometer-kafka:</strong><br><pre>management.metrics.enable.kafka=true\n\n# Key metrics:\n# kafka.consumer.records-lag-max     ← worst partition lag\n# kafka.consumer.fetch-rate          ← records/sec consumed\n# kafka.consumer.commit-rate         ← offset commits/sec\n# kafka.consumer.rebalance-total     ← rebalance frequency</pre><br><strong>3. Burrow</strong> (LinkedIn's Kafka consumer lag monitor): evaluates consumer health with trend analysis — not just point-in-time lag but whether the consumer is making progress.<br><br><strong>Alerting thresholds:</strong><ul><li>Lag &gt; N messages → warning</li><li>Lag increasing over 5min → critical (consumer falling further behind)</li><li>Consumer group not committed in 10min → dead consumer alert</li></ul>"}
);

SEC_KAFKA.qs.push(
  {q:"What is Confluent Schema Registry and how does it work with Avro?",d:"hard",
   a:"Schema Registry is a central repository for Avro, JSON Schema, and Protobuf schemas. It enforces schema compatibility before messages are published — preventing breaking changes from reaching consumers.<br><br><strong>How it works:</strong><ol><li>Producer registers its Avro schema with Schema Registry — gets a schema ID (integer)</li><li>Serialised message = magic byte (0) + 4-byte schema ID + Avro bytes</li><li>Consumer reads the schema ID, fetches schema from registry, deserialises</li></ol><br><strong>Spring Boot + Kafka + Avro:</strong><br><pre># application.yml\nspring.kafka.producer.value-serializer: io.confluent.kafka.serializers.KafkaAvroSerializer\nspring.kafka.consumer.value-deserializer: io.confluent.kafka.serializers.KafkaAvroDeserializer\nspring.kafka.properties.schema.registry.url: http://schema-registry:8081</pre><br><strong>Compatibility modes:</strong><ul><li><code>BACKWARD</code>: new schema can read old messages (consumers upgrade first)</li><li><code>FORWARD</code>: old consumers can read new messages (producers upgrade first)</li><li><code>FULL</code>: both backward and forward compatible</li><li><code>NONE</code>: no compatibility check</li></ul><br><strong>Safe evolution rules:</strong> add optional fields with defaults, don't remove fields, don't change field types. Schema Registry enforces this automatically on publish."},

  {q:"What is Kafka Connect and when would you use it?",d:"medium",
   a:"Kafka Connect is a framework for streaming data between Kafka and external systems (databases, file systems, cloud services) without writing custom producer/consumer code.<br><br><strong>Architecture:</strong><ul><li><strong>Source Connector:</strong> reads from external system → publishes to Kafka (e.g., Debezium PostgreSQL source)</li><li><strong>Sink Connector:</strong> reads from Kafka → writes to external system (e.g., Elasticsearch sink, S3 sink)</li><li><strong>Workers:</strong> distributed or standalone mode, handle parallelism and fault tolerance</li></ul><br><strong>Debezium PostgreSQL Source</strong> (CDC):<br><pre>{\n  \"connector.class\": \"io.debezium.connector.postgresql.PostgresConnector\",\n  \"database.hostname\": \"postgres\",\n  \"database.port\": \"5432\",\n  \"database.dbname\": \"mydb\",\n  \"table.include.list\": \"public.orders\",\n  \"plugin.name\": \"pgoutput\"\n}</pre>Every INSERT/UPDATE/DELETE on the orders table → event on <code>mydb.public.orders</code> Kafka topic. Zero application code changes.<br><br><strong>When to use Connect:</strong><ul><li>Data pipeline / ETL (DB → Kafka → data warehouse)</li><li>CDC for microservice event sourcing</li><li>Log aggregation (file → Kafka → Elasticsearch)</li><li>Avoid writing boilerplate producer/consumer code for standard integrations</li></ul>"},

  {q:"How do you monitor Kafka consumer health in production?",d:"medium",
   a:"<strong>Key metrics to monitor:</strong><br><br><strong>Consumer Lag</strong> (most important):<br><pre>kafka-consumer-groups.sh --bootstrap-server :9092 \\\n  --describe --group my-group\n# Shows: PARTITION | CURRENT-OFFSET | LOG-END-OFFSET | LAG</pre>Alert when lag exceeds a threshold (e.g., &gt;10,000 or &gt;5 minutes of lag).<br><br><strong>JMX metrics via Prometheus (kafka-exporter or JMX exporter):</strong><ul><li><code>kafka_consumergroup_lag</code> — per group/topic/partition lag</li><li><code>kafka_consumer_fetch_rate</code> — consumption rate</li><li><code>kafka_consumer_records_consumed_rate</code> — messages/sec</li><li><code>kafka_consumer_records_lag_max</code> — max lag across partitions</li></ul><br><strong>Spring Boot Kafka metrics</strong> (via Micrometer):<br><pre>management.metrics.enable.kafka=true\nmanagement.endpoints.web.exposure.include=prometheus</pre>Automatically exposes: <code>kafka.consumer.fetch.manager.records-lag</code>, <code>kafka.consumer.records-consumed-rate</code>.<br><br><strong>Alerting strategy:</strong><ul><li>🟡 Warning: lag growing for &gt;5 minutes</li><li>🔴 Critical: lag &gt; SLO threshold (e.g., 1 hour of events)</li><li>📟 Page: consumer group has no active members (all instances down)</li></ul>"}
);

SEC_KAFKA.qs.push(
  {q:"What is the Confluent Schema Registry and why is it important?",d:"hard",
   a:"Schema Registry is a centralised service that stores and enforces Avro/Protobuf/JSON schema versions for Kafka topics — ensuring producers and consumers agree on data format.<br><br><strong>How it works:</strong><ol><li>Producer registers a schema with the registry</li><li>Registry returns a schema ID (integer)</li><li>Producer serialises the message as: <code>[magic byte][4-byte schema ID][avro bytes]</code></li><li>Consumer reads schema ID from the wire, fetches schema from registry, deserialises</li></ol><br><strong>Compatibility modes:</strong><ul><li><code>BACKWARD</code>: new schema can read data written with old schema. Consumer upgrades first.</li><li><code>FORWARD</code>: old schema can read data written with new schema. Producer upgrades first.</li><li><code>FULL</code>: both backward and forward compatible — safest.</li><li><code>NONE</code>: no compatibility enforcement.</li></ul><br><strong>Spring Boot with Schema Registry:</strong><br><pre>spring.kafka.producer.value-serializer=io.confluent.kafka.serializers.KafkaAvroSerializer\nspring.kafka.properties.schema.registry.url=http://schema-registry:8081</pre><br>Schema evolution rules: you can add nullable fields with defaults (safe), but cannot remove fields or change types (breaking)."},

  {q:"What are Kafka Connect and Kafka Streams?",d:"medium",
   a:"<strong>Kafka Connect:</strong> a framework for reliably moving data between Kafka and external systems without writing custom code.<br><ul><li><strong>Source connectors:</strong> pull data into Kafka — Debezium (PostgreSQL CDC), JDBC Source, S3 Source, HTTP Source</li><li><strong>Sink connectors:</strong> push data from Kafka — JDBC Sink, Elasticsearch, S3 Sink, BigQuery</li></ul><br>Run standalone or distributed (cluster). Configure via REST API. Handles serialisation, parallelism, error recovery automatically.<br><br><strong>Kafka Streams:</strong> a Java library for stream processing — runs inside your application (no separate cluster).<br><pre>StreamsBuilder builder = new StreamsBuilder();\nKStream&lt;String, Order&gt; orders = builder.stream(\"orders\");\n\norders\n  .filter((key, order) -&gt; order.getAmount() &gt; 1000)\n  .mapValues(order -&gt; new Alert(order.getId(), \"High value order\"))\n  .to(\"alerts\");\n\n// Aggregation with time window:\norders\n  .groupByKey()\n  .windowedBy(TimeWindows.ofSizeWithNoGrace(Duration.ofMinutes(5)))\n  .count()\n  .toStream()\n  .to(\"order-counts\");</pre><br><strong>Kafka Streams vs Kafka Consumer:</strong> Streams has stateful processing, windowed aggregations, joins between topics, and exactly-once semantics built in."},

  {q:"How do you monitor Kafka consumer health and set up alerting?",d:"medium",
   a:"<strong>Key metrics to monitor:</strong><br><br><strong>Consumer Lag</strong> (most critical):<br><pre>kafka-consumer-groups.sh --bootstrap-server kafka:9092 \\\n  --describe --group my-group\n# Shows: TOPIC PARTITION CURRENT-OFFSET LOG-END-OFFSET LAG</pre><br><strong>JMX metrics exposed by Kafka clients:</strong><ul><li><code>kafka.consumer:type=consumer-fetch-manager-metrics,client-id=X → records-lag-max</code> — maximum lag across all partitions</li><li><code>records-consumed-rate</code> — throughput</li><li><code>fetch-latency-avg</code> — time waiting for records from broker</li><li><code>commit-latency-avg</code> — offset commit time</li></ul><br><strong>Prometheus + Grafana setup:</strong><br><pre># application.yml\nspring.kafka.consumer.properties:\n  \"metric.reporters\": io.confluent.kafka.clients.plugins.MetricsReporter</pre><br>Or use JMX Exporter sidecar to scrape Kafka JMX metrics into Prometheus.<br><br><strong>Alerting rules:</strong><ul><li>Lag &gt; 10,000 messages → warning</li><li>Lag growing for &gt;5 minutes → critical</li><li>Consumer group has no active members → critical (consumer down)</li><li>Rebalance rate &gt; N/hour → investigate consumer stability</li></ul>"},

  {q:"What is auto.offset.reset — earliest vs latest and when does each matter?",d:"medium",
   a:"<code>auto.offset.reset</code> determines what a consumer does when it reads from a partition for the first time (no committed offset exists) or when the committed offset is invalid (e.g., expired, deleted by retention).<br><br><strong>earliest:</strong> start from the beginning of the partition (offset 0) — read all available historical data.<br><pre>spring.kafka.consumer.auto-offset-reset=earliest</pre><br><strong>latest (default):</strong> start from the end of the partition — only consume new messages produced after the consumer starts.<br><pre>spring.kafka.consumer.auto-offset-reset=latest</pre><br><strong>When each matters:</strong><br><ul><li><strong>earliest:</strong> new consumer joining an existing topic and needs to reprocess historical events (e.g., rebuilding a projection, backfilling a cache)</li><li><strong>latest:</strong> consumer should only see new messages (e.g., real-time alerts, notifications)</li></ul><br><strong>Gotcha:</strong> if your consumer commits offsets, <code>auto.offset.reset</code> only matters on the very first run or after a rebalance that invalidates offsets. After that, the consumer resumes from the last committed offset.<br><br><strong>Trigger scenarios:</strong><ul><li>New consumer group ID (no previous offset)</li><li>Partition data was deleted due to retention policy and your offset is now out of range</li></ul>"},

  {q:"What is Kafka's ISR (In-Sync Replicas) and how does it affect durability?",d:"hard",
   a:"<strong>ISR (In-Sync Replica set):</strong> the set of replica brokers that are fully caught up with the leader — they have replicated all messages up to the leader's high-water mark.<br><br><strong>How replication works:</strong><br><ol><li>Producer sends message to leader partition</li><li>Leader appends to its log</li><li>Followers fetch from leader and replicate</li><li>Once all ISR replicas have the message, the leader advances the high-water mark and acks the producer</li></ol><br><strong>acks=all (most durable):</strong> producer waits until all ISR replicas acknowledge. If a broker is slow or down and falls out of ISR, the leader doesn't wait for it — only waits for the remaining ISR.<br><pre>spring.kafka.producer.acks=all\nspring.kafka.producer.properties.min.insync.replicas=2</pre><br><strong>min.insync.replicas=2:</strong> requires at least 2 replicas (leader + 1 follower) to be in ISR before accepting a write. If ISR shrinks below this, writes are rejected — trades availability for durability.<br><br><strong>When a replica falls out of ISR:</strong> if it lags behind by <code>replica.lag.time.max.ms</code> (default 30s), the leader removes it from ISR. Once it catches up, it's added back automatically.<br><br><strong>Durability guarantee:</strong> with <code>acks=all</code> and <code>min.insync.replicas=2</code>, a message is only acked after being replicated to at least 2 brokers — survives single broker failure without data loss."},

  {q:"How do you implement Kafka transactions for exactly-once write to a database?",d:"hard",
   a:"Kafka transactions provide exactly-once semantics: consume a message, process it, and produce output messages atomically — either all succeed or all fail.<br><br><strong>Enable transactional producer:</strong><br><pre>spring.kafka.producer.transaction-id-prefix=tx-\nspring.kafka.producer.properties.enable.idempotence=true\nspring.kafka.producer.acks=all</pre><br><strong>Transactional consume-process-produce pattern:</strong><br><pre>@KafkaListener(topics = \"orders\", groupId = \"processor\")\npublic void process(ConsumerRecord&lt;String, Order&gt; record) {\n  kafkaTemplate.executeInTransaction(ops -&gt; {\n    // 1. Process the message\n    Order order = record.value();\n    Invoice invoice = invoiceService.generate(order);\n\n    // 2. Produce output messages\n    ops.send(\"invoices\", invoice.getId(), invoice);\n    ops.send(\"notifications\", order.getCustomerId(), new Notification(\"Invoice ready\"));\n\n    // 3. Kafka auto-commits the consumer offset within this transaction\n    return null;\n  });\n}</pre><br><strong>What happens:</strong><ul><li>Kafka starts a transaction</li><li>Produces messages (invoices, notifications)</li><li>Commits consumer offset</li><li>If any step fails (exception thrown), transaction aborts — messages not visible, offset not committed</li></ul><br><strong>Limitation — Kafka-to-database writes are NOT included:</strong> database writes are in a separate transaction. For true exactly-once to DB, use the Transactional Outbox pattern or change data capture (CDC)."}
);